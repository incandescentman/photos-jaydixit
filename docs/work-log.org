#+TITLE: Work Log - Jay Dixit Photography Portfolio
#+DATE: 2025-10-12

* 2026-09-06 - Keep contact-form senders on the page with an inline confirmation

** Changed
- The contact form in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/contact.astro][📄 contact.astro]] now submits to Formspree by ~fetch~ with an ~Accept: application/json~ header instead of a full-page POST, so a sender no longer lands on Formspree's generic page. The outcome appears in a ~role="status"~ line under the form; a failed send keeps the typed message and re-enables the button. Required fields, the honeypot, and the Formspree action are unchanged.
- The behaviour comes from ~enhanceFormspreeForm~ in ~@jaydixit/astro-utils/utils/formspree~, the helper the Socratic AI contact form adopted on 2026-09-06. To make it importable here, ~@jaydixit/astro-utils 1.0.7~ was published to npm from the monorepo (as the ~jaydixit~ npm account, which owns the scope) and this app's dependency was raised from ~^1.0.6~ to ~^1.0.7~.
- Status wording lives on the form's ~data-success-message~ and ~data-error-message~ attributes. The lines are Jay's own Socratic contact-form wording; on 2026-09-06 he chose to keep the same success line here (~Sent. I typically respond within 24 hours.~) over a shorter line assembled from the page intro.

** Verification
- Both contact Playwright tests pass against the production build (141 pages). Playwright's ~chromium-headless-shell v1228~ had to be installed for this repo's Playwright ~1.61~ before the suite could run at all.
- A scripted check with Formspree intercepted, so nothing was actually sent, confirmed: an empty submit posts nothing; a 200 shows the confirmation, clears the form, and leaves the button disabled as Sent; a 500 shows the failure line with the typed message intact and the button restored. The request carries the honeypot and subject fields. Focus moves to the status line in both cases, with no page errors.

* 2026-09-02 - Add the shared photo publication inventory

** Changed
- Added [[file+emacs:/Users/jay/Library/CloudStorage/Dropbox/github/photos-jaydixit/scripts/publication-inventory.js][📄 Publication Inventory Sync]], which maintains [[file+emacs:/Users/jay/Dropbox/roam/photography/20260902015339-photo_publication_inventory.org][📄 Photo Publication Inventory]] as the shared cross-destination registry. It mirrors the complete ~Jaydixit~ Commons upload log and separately records the current portfolio source, verified-live site state, Cloudinary configuration, and public surfaces while preserving hand-written notes outside its generated boundaries.
- Added [[file+emacs:/Users/jay/Library/CloudStorage/Dropbox/github/photos-jaydixit/src/pages/photo-inventory.json.ts][📄 photo-inventory.json endpoint]], which will expose a build-generated production manifest after the next authorized deployment. Until then, the initial site snapshot verifies existing production routes directly.
- The reviewed publisher refreshes the shared registry after every completed publication batch, and the canonical photography and Exwalker instructions route publication-history questions to it.

** Verification
- The live Commons source-contract gate imported 657 upload events as 652 distinct file entries; the generated Org entry count is 652 and the newest recorded upload log ID matches the live API.
- The site source-contract gate found 67 distinct photograph records after merging nine byte-identical gallery/highlight copies; the one-time production pass verified 63 records on live site surfaces and kept four source-configured records explicitly marked ~not-found~.
- Direct endpoint execution returned a valid 67-record JSON response with the expected content type and fingerprint and exposed no absolute local paths. Prettier and ESLint passed. The full Astro build reached static route generation, then failed in the unchanged ~exifr~ gallery code under Node 26 with ~ERR_INVALID_ARG_TYPE~ and a garbage-collected ~FileHandle~; the cause was not investigated in this inventory task.
- A local search for ~Jack Johnson~ found no matching filename, description, category, or upload comment in the synchronized registry.

* 2026-08-22 - Hang the homepage hero prints from varied clothespins

** Changed
- Replaced the identical top-left clothespin on every hero print with per-print pins declared in ~heroPrintLayout~ in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][📄 index.astro]] (commit ~68d1ff7~). Each pin counter-rotates its print via ~--pin-rot~ so it hangs near vertical, pins grew to ~18x46~, and placement varies: Apatow and Skarsgård one pin top-left; Kirby, Bateman–Law, Sweeney and Gilroy two pins each. Prints that bleed past the hero's right edge use viewport-proportional insets so their pins stay on canvas from 1024 to 3024 wide; pins on corners lapped by a higher-stacked print sit at the visible end of the top edge.
- Moved the Skarsgård print from ~31%~ to ~29.6%~ and the Kirby print from ~49%~ to ~46.3%~ so Kirby just covers the right edges of Skarsgård (~10px) and Apatow (~34px) at 1440 (commit ~6b7537d~).
- The comparison prototype (five variants; option 4 promoted) lives in ~src/pages/hero-pins-opus.astro~ and ~src/pages/hero-pins-opus/~, left untracked on disk per the prototype rule.

** Validation
- Production build 129 pages, lint clean, 21/21 Vitest. All ten pins verified on canvas and painting by pixel diff from 1024 to 3024 wide and 360 to 430 on mobile, with no caption collisions.
- PhotoSwipe verified on the built homepage at 1440 and 390: first print opens the correct image, arrows advance, Escape closes, no console errors. The dev server's PhotoSwipe was broken by a stale Vite optimize-dep at the time; see [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/codebase-wisdom.org][📄 codebase-wisdom.org]].
- Jay's reactions are recorded in the cross-repo experiment-reactions ledger.

* 2026-08-19 - Upgrade the portfolio to Astro 7

** Changed
- Upgraded Astro to ~7.2.3~, ~@astrojs/mdx~ to ~7.0.6~, ~@lucide/astro~ to ~1.32.0~, and the Tailwind packages to ~4.3.3~. Raised the Node.js floor to ~22.12.0~ for Astro 7's compiler runtime.
- Set ~compressHTML: true~ in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/astro.config.mts][📄 /Users/jay/Dropbox/github/photos-jaydixit/astro.config.mts]] to retain Astro 6 whitespace behavior.
- Kept the published ~@jaydixit/astro-utils 1.0.6~ package and added a pnpm peer override so the app resolves one Astro 7 installation instead of installing a second Astro 6 copy. This app uses the package's Astro-independent typography utility and stylesheet.
- Replaced obsolete ~<script hoist>~ tags in the filesystem gallery, blog-post route, and preserved Photo Wall route with Astro-processed ~<script>~ blocks. The Astro 7 build now bundles their Packery and PhotoSwipe dependencies instead of emitting raw TypeScript imports.
- Kept Playwright's Astro preview server attached by setting ~ASTRO_PREVIEW_BACKGROUND=1~ in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/playwright.config.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/playwright.config.ts]]. Astro 7 otherwise backgrounds preview automatically in the detected agent environment, causing Playwright to treat its managed server process as finished.

** Validation
- ~pnpm install --frozen-lockfile~, ~pnpm run lint~, and the production build pass. The build generates 123 pages.
- All 21 Vitest tests and all 25 Playwright tests pass, including desktop and mobile navigation plus PhotoSwipe opening and Escape dismissal.
- Astro 6 and Astro 7 homepage captures retain identical desktop and mobile document dimensions. The Astro 7 production preview reports 26 images, 25 PhotoSwipe links, no horizontal overflow, and no console or page errors at both review sizes.
- The package graph resolves a single Astro version, ~7.2.3~, across the app, MDX, Lucide, and ~@jaydixit/astro-utils~.

* 2026-08-11 - Create the Red Carpets copy-rewrite workbook

** Changed
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/red-carpets-copy-workbook.org][📄 the Red Carpets copy-rewrite workbook]] with every page-specific visible, metadata, image-description, and accessibility text grouped in page order.
- For each text block, recorded the current wording, the job its replacement must perform, and questions intended to elicit replacement language from Jay rather than generate it for him.
- Kept shared site navigation and footer language out of the workbook because those elements are not specific to the Red Carpets page.

** Validation
- Cross-checked the workbook against [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpets.astro][📄 the page source]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/worldTour.ts][📄 the World Tour data]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/worldTourVariants.ts][📄 the route data]].

* 2026-08-10 - Bring the Departures board into the Red Carpets opening

** Changed
- Replaced the plain six-city hero list on ~/red-carpets/~ with a large split-flap assignment board adapted from the Departures experiment.
- Preserved the original Stockholm-to-Toronto editorial order and the list’s navigation behavior: each animated row jumps directly to its city chapter.
- Gave the board a dedicated half of the desktop hero and reduced the embedded treatment to Destination, Assignment, and Status; mobile keeps Destination and Status.
- Added Space-key chapter advancement: each press aligns the next city below the fixed navigation, from Stockholm through Toronto, without intercepting typing or keyboard activation of controls.

** Validation
- Verified all six animated rows, city-anchor navigation, all six Space-key transitions, reduced-motion-safe initialization, desktop and mobile fit, and no horizontal overflow or browser errors.
- ~pnpm run build~ and the focused Playwright regression pass.

* 2026-08-10 - Remove the false Nobel portrait-session article

** Changed
- Removed ~/blog/nobel-portrait-session/~ after Jay identified its account of the shoot as AI-generated and false.
- Removed the fabricated lighting-diagram asset used only by that article.
- Removed the route smoke test and retargeted the generic related-essay and UTC-date checks to the remaining blog pages.

** Validation
- ~pnpm run lint~, 21 Vitest tests, 22 Playwright tests, and ~pnpm run build~ (101 pages) pass.
- The production build contains no ~/blog/nobel-portrait-session/~ route.

* 2026-08-10 - Remove repeated photographs from the mobile homepage

** Changed
- Kept the complete seven-print hero and the complete 17-card desktop wall, but hid the six wall cards whose Cloudinary public IDs already appear in the hero below ~820px~.
- Renumbered the 11 remaining mobile wall cards ~01~–~11~, preserved strict document order, and moved the full-width landscape beats to visible positions ~3~, ~8~, and ~11~ so every half-width card still has a partner.
- Excluded the hidden duplicates from the mobile PhotoSwipe data source as well as the rendered wall. Desktop PhotoSwipe still includes all 17 wall cards.

** Validation
- Browser-reviewed the settled wall at ~390 x 844~: 11 visible cards, no Cloudinary public ID shared with the seven-print hero, sequential ~01~–~11~ badges, full-width beats at ~03~, ~08~, and ~11~, no stranded half-row, and no horizontal overflow.
- Verified the responsive PhotoSwipe data source contains 11 slides on mobile while desktop retains all 17 wall cards and its ~01~–~17~ numbering.
- ~pnpm run lint~, 21 Vitest tests, 24 Playwright tests, ~pnpm run build~ (102 pages), and focused Prettier all pass.

* 2026-08-10 - Review the Williamsburg waterfront portrait edits

** Changed
- Compared the existing brighter Display P3 export with the newer, darker sRGB export from [[file+emacs:/Users/jay/Dropbox/Pics/headshots-canonical/jay-dixit-waterfront-horizontal.jpg][📄 the canonical portrait]].
- Jay selected the visually lighter existing export. [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][📄 The homepage]] and About page use it; the homepage portrait opens in PhotoSwipe.

** Validation
- ~pnpm run build~ passes.
- Production-preview checks at 1440px, 1181px, 820px, and 390px found no horizontal overflow or browser errors.

* 2026-08-01 - Rebuild the mobile homepage experience

** Why a second pass
The previous same-day pass verified the specific defects it had just repaired and
treated a green result as a healthy experience. Those are different jobs. A fresh
end-to-end review at ~390 x 664~ (iPhone 14, DPR 3) found first-order defects the
regression checks could not see, because nothing in the suite tests reading order,
caption legibility, or whether navigation exists.

** Problems found
- The mobile collage overlapped its own prints via ~.hero-print:nth-of-type(4)~
  ~margin-top: -40px~. The overlap landed exactly on the caption band, burying the
  end of ~Ana de Armas, Sydney Sweeney, and Ron Howard~ and striking the ~W~ in
  ~Judd Apatow at SXSW~ with the next print's clothespin.
- ~:nth-of-type(2)~ spanned both grid columns, so print 1 sat alone in a 2-column
  row with the entire right half empty; the final print was stranded centred in a
  full-width row.
- The photo wall printed its numbered badges out of order — ~08, 10, 09~ — because
  ~grid-auto-flow: dense~ let the card after a full-width card overtake it.
- Short landscape cards left roughly ~200px~ holes beside taller neighbours, and
  the wall ended on a lone half-width card.
- ~h1~ ran ~62.4px~ on a ~49.3px~ line-height; the descenders of ~Photography~
  touched ~for Wikipedia~ at both ~390px~ and ~320px~.
- The primary nav was ~display: none~ below *1420px* with no toggle, so every
  windowed laptop as well as every phone lost it. ~/red-carpet~ was reachable only
  from the closing panel, 12.5 screens down; jaydixit.com was unreachable.
- All 17 wall cards linked to ~/gallery~ — seventeen portraits, one destination —
  while the hero prints opened a lightbox.
- The profile portrait shipped its ~1280px~ derivative on a phone: ~sizes~ resolves
  to ~350~ CSS px, and at DPR 3 that needs ~1050~ device px. The earlier work-log
  claim that ~390px~ selects the ~720px~ derivative holds only at DPR 1-2.

** Changed
- Rebuilt the mobile collage as three lead prints at full column width followed by
  a four-up contact-sheet strip, inside a 4-column grid. Prints no longer overlap,
  so captions always have clear space and now wrap to a second line instead of
  being ellipsised. All seven photographs are preserved. Lead prints went from
  ~182px~ to ~350px~ wide while the stage grew only ~1519px~ to ~1593px~.
- Gave the mobile wall strict document order (~grid-auto-flow: row~) and a uniform
  ~4 / 5~ media ratio, with full-width beats at cards 3, 10 and 17 chosen so the
  half-width cards always pair evenly. The numbering defect and the holes both
  dissolve as a consequence.
- Added a compact menu toggle and panel to the homepage header below ~1420px~,
  rendering both it and the inline nav from one ~navLinks~ array so they cannot
  drift. Scoped the old ~.site-header nav~ rules to a new ~.site-nav-primary~ class
  so they stop leaking onto the panel.
- Pointed wall cards at PhotoSwipe with full ~data-pswp-*~ dimensions and an
  ~href~ of the full-size image, matching the hero prints and degrading without JS.
- Added a fixed mobile scroll cue that dismisses itself once the reader is ~28%~
  into the hero, replacing a cue that sat uselessly at the bottom of a ~2610px~ hero.
- Raised the mobile ~h1~ line-height to ~0.88~ / ~0.9~, moved the reading-progress
  rail to a horizontal bar along the top edge on phones (driven by a
  ~--reading-progress~ custom property so one script serves both axes), contained
  the decorative orbit, and raised the two sub-44px tap targets.
- Disabled the per-print parallax and the load-time fly-in on mobile; hero prints
  now reveal on scroll like wall cards. The reveal reads each element's base
  rotation via ~gsap.getProperty~ and animates around it, or it would have flattened
  the deliberate CSS tilt.
- Dropped italic weight 400 from the Google Fonts request. Requesting a single
  italic weight makes Google serve a static instance rather than a variable font:
  ~147KB~ to ~63KB~.
- Re-encoded the profile derivatives with mozjpeg q84 4:4:4.

** Validation
- Desktop is unchanged, verified rather than assumed: built the pre-change tree
  from a stash, captured ~1728px~ and ~1280px~ screenshots at four scroll
  positions, then pixel-diffed against the new build. Document heights identical
  (~8197~ and ~7365~). Differences: ~0px~ at ~1728/top~ and ~1728/wall~; ~3238px~
  at ~1728/profile~ (the re-encoded JPEG); ~88px~ at ~1728/closing~ and ~425px~ at
  ~1280/top~ (the new menu button).
- The font change has no visual effect anywhere: ~<em>~ appears in exactly one of
  102 built pages, and every italic in the built site — the closing wordmark, the
  contact intro, the blog blockquote, the WikiPortraits origin copy — computes to
  weight 300, which is still served.
- Image re-encode measured against a resampler-matched reference, after a first
  attempt compared sharp-encoded files to a magick-resized reference and was
  measuring resize mismatch rather than compression. q84 mozjpeg reaches ~35.4dB~
  PSNR where the shipped file reached ~35.3dB~ against its own reference, at 43%
  of the bytes.
- Initial mobile payload ~1173KB~ to ~931KB~. LCP ~132ms~. Three.js still absent on
  mobile. No horizontal overflow at ~390px~ or ~320px~, no console errors, no wall
  card left hidden after a fast flick, reduced motion leaves nothing invisible.
- ~npm run lint~, 21 Vitest tests, 23 Playwright tests, ~npm run build~ (102 pages),
  Prettier, and ~git diff --check~ all pass.

** Open
- Two Cloudinary highlights weigh ~290-299KB~ at ~w_760~ where comparable cards are
  ~50-90KB~. The built URLs already include ~f_auto,q_auto,w_760~ through
  [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/lib/cloudinary.ts][📄 cloudinary.ts]],
  so the extra weight is not caused by a missing global quality transform. The cause
  remains unverified. Any further reduction would require a separately measured and
  visually reviewed targeted or global compression change; none was made.
- Six of the seven hero prints reappear in the wall below, so mobile visitors scroll
  past the same faces twice.

* 2026-08-01 - Repair the Kinetic homepage on mobile

** Problems observed
- At ~390 x 844~, the phone collage's fixed ~128vw~ stage ended while five of its seven print cards still continued below it. The hero's ~overflow: hidden~ cut the collage off abruptly at the transition into Jay's profile.
- A fast mobile flick could cross a ScrollTrigger reveal window without playing it. Passed photo-wall cards remained at zero opacity, leaving a long blank paper grid.
- The homepage PhotoSwipe anchors did not provide full-image dimensions, so the shared lightbox fell back to the responsive thumbnail's natural dimensions and opened the portrait at only ~183px~ wide. The global dark backdrop also overrode the homepage's intended light editorial treatment.
- The complete brand plus Contact action overflowed at the supported ~320px~ width, and Contact's interactive height was below the ~44px~ mobile target.
- The profile portrait always delivered its ~3.1 MB~, ~3000px~-wide original even though it renders at roughly ~280–323px~ on a phone.

** Changed
- Let the mobile print grid determine its own height, removed the stale nonexistent ~hero-photo-*~ rules, and centered the final odd print. The complete seven-print sequence now remains inside the hero before the profile begins.
- Replaced the phone wall's fragile per-card ScrollTrigger with a request-animation-frame-coalesced reveal check that reveals every card whose top has passed the viewport threshold, including after a fast flick. Disabled ~content-visibility~ for the 17-card phone layout so offscreen placeholder geometry cannot invalidate the reveal positions; desktop keeps the original larger GSAP entrance.
- Added exact lightbox dimensions from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/image-metadata.json][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/image-metadata.json]] and scoped the Kinetic viewer to the light paper backdrop. The portrait now fills the available phone width while retaining its responsive source set.
- Tightened the narrow header, hid the small ~Photos~ label only below ~360px~, and made Contact a ~44px~-high target.
- Added ~720px~ and ~1280px~ responsive derivatives of Jay's profile image, supplied class-aware Cloudinary ~sizes~ for the mixed mobile wall, reduced eager hero loading to its first print, and made every below-fold wall image lazy. The ~390px~ profile selects the ~720px~, ~113 KB~ derivative instead of the ~3.1 MB~ original.
- Hid the WebGL atmosphere on phone layouts and moved Three.js behind a desktop-only dynamic import, so mobile visitors do not download that visual-effect dependency.
- Removed obsolete GSAP animations targeting the deleted ~editorial-bridge~ markup.

** Validation
- Browser-reviewed [[http://127.0.0.1:4322/][http://127.0.0.1:4322/]] at ~390 x 844~ and ~320 x 568~. The full collage-to-profile transition is intact, a ~1900px~ fast flick leaves no passed cards hidden, the lightbox portrait renders at ~335px~ wide on the ~390px~ viewport, and the ~320px~ header fits without clipping.
- ~pnpm run lint~, all 21 Vitest tests, all 23 Playwright browser tests, ~pnpm run build~ (102 pages), focused Prettier, and ~git diff --check~ passed. The build retains only the existing Vite chunk-size warning.

* 2026-07-31 - Redesign The Story of WikiPortraits as a feature essay

** Direction
- Bring [[http://localhost:4322/blog/wikiportraits-story][The Story of WikiPortraits]] into the same full-width editorial system as the Kinetic homepage and revised About page.
- Preserve the article's wording except for Jay's two explicit Pete Davidson sentence revisions and the TIFF application language supplied in his reference screenshot.

** Changed
- Added a ~feature~ presentation variant to the blog content schema and used it to give this essay a route-specific masthead, full-width section rhythm, large serif statements, warm evidence surface, reach statistics, Jeremy Strong climax, and broad closing mission section without changing the standard blog-post template.
- Extracted the Vanessa Kirby before-and-after comparison into [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/WikiPortraitsHero.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/components/WikiPortraitsHero.astro]] and retained PhotoSwipe behavior for its photographs.
- Rebuilt the Pete Davidson sequence as an editorial comparison and incorporated Jay's revised copy: “It was eventually replaced with this one. Not as bad as the first, but still really bad.”
- Updated the evidence lede with Jay's airport and blurry-iPhone wording.
- Recreated the audience fields from [[file+emacs:/Users/jay/Downloads/BB48104F-BFAC-4715-9538-CDF268EDF11F.jpg][📄 /Users/jay/Downloads/BB48104F-BFAC-4715-9538-CDF268EDF11F.jpg]] as a responsive TIFF media-accreditation artifact rather than committing the phone screenshot. It identifies the outlet as Wikipedia and preserves the original audience values: ~1500000000~ monthly unique visitors and ~24000000000~ monthly page views.
- Added a restrained red right-arrow pivot beneath the TIFF artifact's large left-hand sentence, then moved the complete ~1.5 billion people~ press-credential payoff out of the statistics and into the left panel, ending with Jay's smiley emoji.
- Stopped the origin-story prose grid from stretching its paragraph rows to match the taller left-hand statement; the four paragraphs now use their intended ~1.75rem~ vertical gap.

** Validation
- Browser-reviewed the complete essay at the default ~3360 x 1739~ viewport and at ~390 x 844~. The desktop and mobile layouts have no horizontal overflow; the masthead, TIFF artifact, statistics, Pete Davidson comparison, and Jeremy Strong comparison all reflow at the intended breakpoints.
- ~npm run lint~, all 21 Vitest tests, ~npm run build~ (102 pages), focused Astro/TypeScript Prettier, and ~git diff --check~ passed. The MDX formatter's mixed leading indentation was normalized after its formatting pass. The build retains only the existing Vite chunk-size warning.

** Publication
- Committed the final feature-essay redesign as ~db5b3c4~ (~Redesign WikiPortraits story as feature essay~), following the two committed About-page refinement passes ~8661c45~ and ~5672e20~.
- This documentation closeout publishes those three reviewed commits together from ~main~ to ~origin/main~.

* 2026-07-31 - Record the promoted portfolio as a loved cross-project reference

** Summary
- Recorded Jay's definitive reaction to the finished homepage and About page:
  “I really love the way my photos portfolio site turned out.”
- Preserved the more transferable preference separately: jaydixit.com and
  Socratic AI should not become equally photo-heavy, but should learn from the
  portfolio's “big splashy elegant look.”
- Updated the local experiment chooser and the
  [[file+emacs:/Users/jay/Dropbox/github/socratic-video/references/look-and-feel/experiment-reactions.org][📄
  cross-repo visual reaction ledger]].
- Added
  [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/experiment-intentions/kinetic-editorial.json][📄
  the Kinetic Editorial intention sidecar]] to preserve the artistic objective,
  the loved qualities, and the correct cross-project translation: preserve
  scale, elegance, asymmetry, real presence, and coherent spatial motion rather
  than merely increasing the number of photographs.

* 2026-07-31 - Link photography decisions to the cross-repo reaction ledger

** Summary
- Backfilled the explicit reactions to the photography homepage variants into
  [[file+emacs:/Users/jay/Dropbox/github/socratic-video/references/look-and-feel/experiment-reactions.org][📄
  the cross-repo visual experiment reaction ledger]].
- Preserved the original distinctions:
  - Kinetic Editorial was liked, then promoted to the canonical homepage.
  - Living Contact Sheet was liked and supplied the physical-print hero
    composition used by Kinetic Editorial.
  - Spatial Portrait Gallery was rejected: “No. Small photos, nothing
    special.”
  - The original Red Carpet Sequence was rejected: “No. Too boring, nothing
    really happening.” Its reconstructed cinematic Before & After was later
    promoted to the canonical ~/before-after/~ page.
  - Photo Wall was superseded and demoted to an intentionally unlinked,
    noindexed reference route rather than recorded as a wholesale rejection.
  - No explicit reaction was found for the older Living Archive and Contact
    Sheet / Public Record experiments, so they remain unreviewed.
- Added the global-ledger link to the photography experiment documentation so
  future sessions update both the local implementation record and the
  cross-project index.

* 2026-07-30 - Align Before & After with the new homepage

** Direction
- Give the Before & After introduction more visual weight and move it away from the far-right edge.
- Match the page's primary navigation to the approved Kinetic homepage navigation.

** Changed
- Moved the existing Wikipedia explanation beneath the oversized Before & After title in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro]], preserving Jay's exact wording while widening its measure from ~34rem~ to ~58rem~ and increasing the responsive serif size.
- Replaced the page's outdated Red Carpet / Galleries / About / Contact navigation with the Kinetic homepage set: Red Carpet, Before & After, About Me, Why WikiPortraits, Red Carpets Around the World, Contact, and jaydixit.com. The festival link returns to the named homepage section.
- Matched the approved Jay Dixit / Photos brand treatment and kept Contact available as the compact-screen navigation action.

** Validation
- Verified at ~1600 x 1000~, ~1280 x 900~, and ~390 x 844~. The desktop explanation begins at the same horizontal position as the title, the complete navigation fits at desktop and laptop sizes, compact screens retain Contact, all three sizes have zero horizontal overflow, and the page reports no browser console errors.
- Prettier and focused ESLint passed for the revised page.

* 2026-07-30 - Promote Kinetic Editorial to the canonical homepage

** Direction
- Replace the canonical ~/~ landing page with the approved Kinetic Editorial experience.
- Preserve the former Packery homepage only as an intentionally unlinked reference route at ~/photo-wall/~.
- Treat internal experiment URLs as disposable: remove promoted experiment routes instead of preserving or redirecting them.

** Changed
- Moved Kinetic Editorial from ~src/pages/experiments/kinetic-editorial.astro~ to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro]], making the approved cinematic design the canonical homepage source.
- Routed the promoted homepage through the chromeless [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][📄 main layout]] so it inherits canonical metadata, social metadata, analytics, the complete favicon set, and shared site foundations without adding the standard navigation or footer.
- Removed the experiment-only ~noindex~ metadata, “Current photography homepage,” and “All experiments” footer links. Replaced the experiment footer with a minimal public copyright footer.
- Moved the former Packery homepage to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/photo-wall.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/photo-wall.astro]]. Its canonical URL is ~/photo-wall/~, it is marked ~noindex, nofollow~, and no public page links to it.
- Removed both promoted internal routes, ~/experiments/kinetic-editorial/~ and ~/experiments/red-carpet-sequence/~, without redirects.
- Updated the noindexed experiment chooser so Kinetic Editorial appears only as the canonical homepage rather than as a duplicate experiment.
- Updated the shared PhotoSwipe integration so an Escape keypress during the longer Kinetic opening transition is queued until ~openingAnimationEnd~, then closes with the normal smooth transition instead of being ignored.
- Updated the route smoke tests, Packery path references, architecture documentation, legacy handoff, and canonical [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/CLAUDE.org::*Experiment URL Lifecycle][📄 experiment URL lifecycle rule]].

** Validation
- Verified the canonical homepage, noindexed Photo Wall, revised Before & After page, shared navigation, About page, and both PhotoSwipe surfaces in the production-browser suite: all 23 Playwright tests passed.
- Verified that ~/experiments/kinetic-editorial/~ and ~/experiments/red-carpet-sequence/~ return direct ~404~ responses without redirects, and that ~/photo-wall/~ is absent from the generated sitemap.
- Full ESLint passed, all 21 Vitest tests passed, and the production build generated 102 pages. Vite retained the existing large-chunk warning.

* 2026-07-30 - Promote the cinematic Before & After page

** Direction
- Replace the previous canonical ~/before-after/~ page with the revised cinematic Before & After design from the Red Carpet Sequence experiment.

** Changed
- Promoted the complete six-person cinematic comparison experience to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro]], preserving its approved sequence, large photographic presentation, editorial typography, red-carpet direction markers, responsive stacking, and GSAP entrance motion.
- Removed the experiment-only ~noindex~ treatment and “Compare every homepage experiment” link. Replaced the experiment navigation with public portfolio links.
- Rescaled the promoted page's header to match the rest of the portfolio more closely: the red-carpet seal grows from ~32px~ to ~72px~, the desktop wordmark matches the established ~2.35rem~ site wordmark, and the navigation gains a larger, less compressed treatment. Compact screens use a balanced ~48px~ seal and ~1.68rem~ wordmark.
- Extended [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro]] with an opt-in chromeless mode so the cinematic route keeps the site's canonical metadata, social metadata, favicon set, analytics, and global styles without inheriting the standard navigation and footer.
- Kept the first Before and After images eagerly loaded at high priority for the public route; the remaining ten comparison images remain lazy-loaded.
- Replaced the former experiment route with a permanent redirect to ~/before-after/~ and updated the experiment chooser, canonical documentation, and browser regression test to reflect the promotion.

* 2026-07-30 - Give Kinetic Editorial a darkroom print hero

** Direction
- Keep the Kinetic Editorial page's typography, copy, navigation, and animation system, but replace its original floating hero-photo collage with the physical-print composition from Living Contact Sheet.
- Shift the hero heading and supporting text left to make room for the larger print composition.
- Replace the red number tabs on the prints with small wooden clothespins so the photographs read as prints hanging to dry on a darkroom wall.

** Changed
- Replaced the six original hero figures in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/kinetic-editorial.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/kinetic-editorial.astro]] with the seven-photo layout and print selections used by the Living Contact Sheet hero.
- Reframed each photograph as a bordered paper print with a caption strip, tactile shadow, overlapping stack order, and a small CSS-drawn wooden clothespin gripping its upper-left edge. Added two restrained darkroom lines behind the composition.
- Spread the desktop print pile farther outward without reducing the print sizes, revealing more of Willem Dafoe, the three-person TIFF portrait, and the lower photographs while preserving the irregular overlapping stack.
- Fine-tuned the final desktop pile by shifting its left portrait position seven percentage points left and six points down, then lowering the top Vanessa Kirby print four points, without changing either print's size, angle, or stacking order.
- Replaced the Willem Dafoe hero print with Judd Apatow while preserving that print's final position, size, angle, stacking order, and motion behavior.
- Replaced the Emma Stone hero print with Jude Law while preserving the lower-left print's position, size, angle, stacking order, and motion behavior.
- Briefly tested the two replacement portraits in reversed slots, then restored Judd Apatow above and Jude Law below while keeping the slot geometry and motion unchanged.
- Shifted and slightly narrowed the Kinetic hero copy while giving the print stage the full hero canvas. After raising the Willem Dafoe and Emma Stone prints, tightened the final headline indent and supporting-copy measure to restore a deliberate gutter without shrinking the headline or photographs. The original headline wording and supporting prose were not changed.
- Replaced the repeated hero, bridge, and metadata description with Jay's approved WikiPortraits wording, ending with the factual purpose “for use on Wikipedia” rather than asking the copy to judge the quality of his own photographs.
- Updated that approved description to identify Jay as a “red carpet photographer for WikiPortraits” who photographs “celebrities, leaders, and scientists.”
- Replaced the repeated oversized mission statement below the hero with a responsive editorial profile section using Jay's exact festival-coverage and writing/AI biography copy, inline Socratic AI link, prominently sized “Why I Take Photographs” link, and a large presentation of the existing 3000 × 2000 Williamsburg portrait from the About page.
- Turned all seven hero prints into one PhotoSwipe sequence using the repo's shared lightbox component. Each print now opens a responsive Cloudinary source up to ~2400px~, exposes its caption, supports next/previous navigation and zoom, and returns to the unchanged hero position when closed. Updated the print microcopy from “View galleries” to “View full size.”
- Gave this experiment its own warm-canvas PhotoSwipe treatment: the editorial page remains visible beneath a light paper veil, the enlarged photograph lifts forward with a print-like shadow, and the controls and caption use the page's ink, paper, and canonical ~#96773f~ gold instead of PhotoSwipe's generic black presentation. Other portfolio lightboxes retain their existing treatment.
- Added a Kinetic Editorial-specific PhotoSwipe opening motion: the selected photograph now travels from the clicked print's actual position and size into the centered lightbox over ~650ms~, while the warm canvas fades in behind it. This uses PhotoSwipe's thumbnail-bound zoom transition rather than an arbitrary vertical offset. A zero-velocity ease-in-out curve softens the initial acceleration and final settling without sacrificing the smooth middle of the movement. The page-level reduced-motion rule shortens the transition for people who request it, and every other lightbox remains unchanged.
- Repositioned the Kinetic Editorial PhotoSwipe arrows from the far viewport edges to the outside edges of the centered photograph, with a compact-screen floor and vertical alignment that follows the lightbox's asymmetric top and bottom padding.
- Added the existing red-and-gold carpet seal to the Kinetic Editorial header beside “Jay Dixit Photos,” using the restrained ~32px~ scale and subtle hover tilt established by the Red Carpet Sequence experiment.
- Rescaled the Kinetic Editorial header to match the promoted cinematic page and the wider portfolio system: a ~72px~ desktop seal, ~2.35rem~ wordmark, larger navigation, and additional hero clearance, with a balanced compact treatment below ~820px~.
- Extended the animated headline masks upward without moving or shrinking the type, so the Newsreader letterforms are no longer clipped across their tops.
- Removed italics from “Red Carpet” and “for Wikipedia” in the main headline, preserving the red emphasis, scale, line breaks, and final-line indent while calming the typography against the angled photo pile.
- Replaced the headline's em dash after “Jay Dixit” with a colon in both the visible title and accessible label.
- Expanded the Kinetic Editorial desktop navigation with Why WikiPortraits, Red Carpets Around the World, Contact, and jaydixit.com. Why WikiPortraits uses the canonical story route; Red Carpets Around the World jumps to the new festival-coverage interlude; Contact now sits in the main navigation rather than only at the far-right edge. The compact Contact control remains visible when the full navigation collapses below ~1420px~.
- Removed Galleries from the Kinetic Editorial navigation and renamed About to About Me.
- Replaced the secondary hero action “Selected portraits” with “About Me” and linked it directly to the About page. Rebuilt both hero links as a coherent two-row editorial action list with larger serif labels, full-width rules, and matching restrained arrows so they no longer read as one malformed phrase; the lower “Selected portraits” marker remains as the label for the gallery content.
- Extended the Kinetic hero action list with The Story of WikiPortraits and Red Carpets Around the World, using the canonical story route and festival-coverage anchor. All four hero actions remain in one full-width editorial column; jaydixit.com and Socratic AI were removed from this list while remaining available elsewhere on the site.
- Replaced the first button pair on the About page with the same serif-and-rule editorial link treatment used in the Kinetic hero, routing to Visit jaydixit.com, Socratic AI, and Get in Touch. The two destination links retain directional arrows; Get in Touch uses a distinct gold envelope mark.
- Removed the redundant “Photography / Where I Shoot” card grid and the complete Recognition section from the About page, allowing the personal essay and editorial links to flow directly into the photography philosophy.
- Added [[file+emacs:/Users/jay/Downloads/jay-dixit_2024-10-04_telos_ryan-jina-jess_wong-kar-wai_05420.jpg][📄 /Users/jay/Downloads/jay-dixit_2024-10-04_telos_ryan-jina-jess_wong-kar-wai_05420.jpg]] beside the About page's philosophy quotation and copied it to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/images/about/jay-dixit_2024-10-04_telos_ryan-jina-jess_wong-kar-wai_05420.jpg][📄 the repo's About assets]]. Astro now emits responsive derivatives. The section spans the broad page canvas, the full 4:3 photograph dominates the desktop composition, and it becomes a large image above the quotation on tablet and mobile. The quotation scales up with the canvas so its typographic weight matches the enlarged photograph.
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/scripts/experiments/kineticEditorial.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/scripts/experiments/kineticEditorial.ts]] so all seven prints retain the Kinetic entrance choreography, pointer tilt, and scroll-depth movement.
- On compact screens the prints become the same two-column working-print arrangement rather than shrinking the desktop overlap into unreadable thumbnails.

** Validation
- Verified the live desktop route at [[http://127.0.0.1:4335/experiments/kinetic-editorial/][http://127.0.0.1:4335/experiments/kinetic-editorial/]] with seven loaded prints, seven visible clothespins, no browser errors, and no page-level horizontal overflow.
- Opened the revised lightbox in the live browser and confirmed the underlying editorial composition remains visible, the instance resolves the warm ~#f4efe5~ canvas and gold ~#96773f~ controls at ~0.28~ backdrop opacity, the caption reads correctly, and next/previous navigation advances the seven-photo sequence.
- Verified the compact ~390 x 844~ layout with all seven prints and clipped decorative transform overflow.
- Verified the revised About page at ~2048 x 1152~, ~1440 x 1000~, ~820 x 1100~, and ~390 x 844~. The responsive Telos photograph loaded through Astro's generated sources, the enlarged quotation balanced against it, the removed card and Recognition sections remained absent, and every viewport had zero horizontal overflow.
- Prettier and ESLint passed. All 21 Vitest tests passed. The production build generated all 103 pages; Vite retained the existing large-chunk warning.

* 2026-07-30 - Complete the revised Before & After experiment

** Direction
- Keep the revised route large, splashy, graphical, and photography-led. Do not constrain the comparison to the old page's ~68rem~ maximum width.
- Use the revised editorial treatment as the foundation while restoring the explanatory clarity and complete six-person series from the existing Before & After page.

** Changed
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/red-carpet-sequence.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/red-carpet-sequence.astro]] to render all six canonical comparisons in this sequence: Jason Bateman, Vanessa Kirby, Jeremy Strong, John Hopfield, Conan O'Brien, and Sydney Sweeney. The experiment-specific ordering leaves the shared data and existing page unchanged.
- Reused the existing WikiPortraits explanation verbatim and placed it beside the oversized title so the project is clear without sacrificing the editorial first screen.
- Changed the external link label from “Wikipedia” to “View on Wikipedia.”
- Matched the comparison numbers, event lines, and Wikipedia links to the typography and colors used on the canonical Before & After page: Newsreader for the softened gold indexes, and Instrument Sans for the muted event metadata and gold links.
- Reduced the complete desktop comparison pair to 90% of its available width and centered it; mobile pairs remain full width.
- Replaced the rejected line-drawn transition with [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/experiments/red-carpet-direction-arrow.png][📄 /Users/jay/Dropbox/github/photos-jaydixit/public/experiments/red-carpet-direction-arrow.png]], a Codex-built-in generated red-carpet floor-marker arrow with a transparent background. The source prompt and generation metadata live beside the generated source image under the current Codex thread's ~generated_images~ folder.
- Applied the existing Before & After gold, ~#96773f~, to the experiment's kicker, sequence numbers, and text-link hover states while keeping crimson for the red-carpet emblem and primary action.
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/index.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/index.astro]] so Jay's first impressions and subsequent Red Carpet Sequence feedback remain visible on the experiment chooser.

** Validation
- The live route at [[http://127.0.0.1:4335/experiments/red-carpet-sequence/][http://127.0.0.1:4335/experiments/red-carpet-sequence/]] renders six numbered comparisons in the requested order. All 12 portrait images loaded successfully in the browser.
- Prettier and ESLint passed. All 21 Vitest tests passed. The production build generated all 103 pages; Vite retained the existing large-chunk warning.
- Nothing was committed, pushed, or deployed.

* 2026-07-28 - Add branded HTML email signature

** Changed
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/jay-dixit-red-carpet-photography-email-signature.html][📄 /Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/jay-dixit-red-carpet-photography-email-signature.html]] as a standalone, copyable rich-text email signature.
- Reused the business card’s approved wording and the already-public [[https://photos.jaydixit.com/apple-touch-icon.png][red-carpet seal PNG]]. The signature uses an email-client-safe presentation table and inline styling, with no external font or local image dependency.
- Replaced the old signature’s Cannes photograph with the 72px red-carpet seal while retaining its useful destinations: portfolio, Wikimedia profile, and Commons Gallery.
- Replaced the old mixed emoji labels with one consistent row of text links so the signature renders more predictably across email clients.
- Linked the WikiPortraits credential to the official [[https://www.wikiportraits.org/][WikiPortraits website]].

** Validation
- Prettier passed. The revised HTML rendered at 347 × 101 CSS pixels, displayed the 180 × 180 production seal at 72 × 72 pixels, exposed the working WikiPortraits, portfolio, Wikimedia profile, Commons Gallery, and email links, and reported no browser errors.

* 2026-07-28 - Make Red Carpet Sequence show a coherent Before & After

** Problem
- The Red Carpet Sequence finale said “See the Before & After” but rendered four ordinary portfolio photographs. No “before” photographs or comparison interaction were present on the page; the only actual Before & After experience was behind a link.
- The first repair incorrectly overlaid each pair behind a reveal slider. These are separate photographs from different settings and years, not aligned versions of one underlying image, so a merged-image slider was visually incoherent.

** Changed
- Replaced the false payoff in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/red-carpet-sequence.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/red-carpet-sequence.astro]] with three real comparison pairs from the canonical [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts]] data: Jason Bateman, Jeremy Strong, and Vanessa Kirby.
- Removed the rejected opening manifesto and five-photo arrival sequence. The route now opens directly on the Before & After experience.
- Presented each pair as two complete, distinct photographs in a large side-by-side diptych, with a labeled, line-drawn Before → After transition showing that the first photograph was replaced by the second. A subtle moving dash gives the arrow direction without splicing the unrelated images together. On narrow screens the two photographs stack at full width and the arrow points downward instead of shrinking the images into small thumbnails.
- Extracted the business card’s existing inline red-carpet SVG geometry into the reusable transparent website asset [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/brand/red-carpet-seal.svg][📄 /Users/jay/Dropbox/github/photos-jaydixit/public/brand/red-carpet-seal.svg]]. Tested it only as a small editorial seal beside the route’s text wordmark, with a restrained hover/focus response, so the photographs remain the dominant visual.
- Kept the link to the full Before & After page, now labeled “See every Before & After.”
- Listed all four visual variants and their current review status in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/CLAUDE.org::*Experimental Visual Variants][📄 Experimental Visual Variants]]. Added the pending decision to [[file+emacs:/Users/jay/Dropbox/roam/velocity/goals/current-focus.org][📄 /Users/jay/Dropbox/roam/velocity/goals/current-focus.org]] under Active Priorities.

** Validation
- Prettier, focused ESLint, and ~pnpm run build~ passed. The build generated all 103 pages; Vite retained the expected warning about the experiment-only Three.js chunk.
- Verified the live route at [[http://127.0.0.1:4335/experiments/red-carpet-sequence/][http://127.0.0.1:4335/experiments/red-carpet-sequence/]]. The removed hero and arrival sequence are absent, the page begins with the Before & After heading, all six comparison images load, and the refined arrow points right between desktop pairs and downward between full-width compact stacks. The browser reported no console errors.

* 2026-07-28 - Record first impressions of cinematic variants

** Jay's response
- *Spatial Portrait Gallery*: Rejected. Jay's first impression was: “No. Small photos, nothing special.”
- *Red Carpet Sequence*: Rejected. Jay's first impression was: “No. Too boring, nothing really happening.”
- *Kinetic Editorial* and *Living Contact Sheet*: Jay said, “The other ones I really like.”

** Direction
- Do not advance Spatial Portrait Gallery or Red Carpet Sequence unless Jay explicitly asks to revisit them.
- Treat Kinetic Editorial and Living Contact Sheet as the promising directions for the next comparison or a possible hybrid.
- The reusable cross-project lesson is recorded in [[file+emacs:/Users/jay/Library/CloudStorage/Dropbox/github/vibecoding/preferences/UI-PREFERENCES.org][📄 /Users/jay/Library/CloudStorage/Dropbox/github/vibecoding/preferences/UI-PREFERENCES.org]].

* 2026-07-27 - Add four cinematic homepage experience variants

** Direction
- Jay asked for four separate, reversible visual directions whose first job is to make visitors think the photographs are amazing: Kinetic Editorial, Spatial Portrait Gallery, Living Contact Sheet, and Red Carpet Sequence.
- Kept the existing homepage wording intact in each concept. The live homepage and its deliberate Packery implementation were not changed.

** Changed
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/kinetic-editorial.astro][📄 Kinetic Editorial]] with oversized editorial typography, a GSAP-assembled portrait stage and photo wall, subtle Three.js atmosphere, native scrolling, keyboard focus, and reduced-motion behavior.
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/spatial-gallery.astro][📄 Spatial Portrait Gallery]] with a shallow Three.js portrait field, pointer and scroll depth, a functional HTML portrait archive, capped rendering resolution, renderer cleanup, and static fallbacks for mobile, reduced motion, data saver, or missing WebGL.
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/living-contact-sheet.astro][📄 Living Contact Sheet]] with an ivory photographic worktable, overlapping prints, GSAP choreography, keyboard-accessible large-image dialog, and a natural-scroll contact sheet.
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/red-carpet-sequence.astro][📄 Red Carpet Sequence]] with five full-scale portrait arrivals, native sticky scrolling, GSAP scrubbed framing and caption motion, restrained flash transitions, and a final Before & After path.
- Added route-specific scripts at [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/scripts/experiments/kineticEditorial.ts][📄 kineticEditorial.ts]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/scripts/experiments/spatialGallery.ts][📄 spatialGallery.ts]].
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/index.astro][📄 the experiment chooser]] so the current homepage, all four new concepts, and both earlier experiments can be compared from one page.
- Added ~gsap~ and ~three~ as route-bundled dependencies. The Three.js concepts load their heavier WebGL code only on their own experiment routes.

** Validation
- Prettier and focused ESLint passed for the chooser, all four new pages, and both new scripts.
- ~pnpm run test -- --run~ passed all 21 Vitest tests.
- ~pnpm run build~ generated 103 pages, including all four new experiment routes. Vite reports the expected warning that the dynamically loaded Three.js chunk is larger than 500 kB.
- Kinetic Editorial, Spatial Portrait Gallery, and Living Contact Sheet passed rendered desktop checks. Kinetic Editorial and Living Contact Sheet also passed mobile checks; Kinetic Editorial's reduced-motion mode disabled its Three.js canvas, and Spatial Portrait Gallery retains its static fallback for mobile, reduced motion, data saver, or missing WebGL.
- Red Carpet Sequence passed combined-server checks at 1440 × 1000 and 390 × 844 with natural scrolling, working reduced-motion flow, loaded imagery, and no horizontal overflow. The chooser passed desktop and mobile checks with all four new links visible and no console errors.

** Handoff
The variants are local and uncommitted. Compare them from [[http://127.0.0.1:4335/experiments/][http://127.0.0.1:4335/experiments/]]. Nothing was pushed or deployed.

* 2026-07-14 - Lead the Before & After page with Jason Bateman

** Changed
- Moved the existing Jason Bateman comparison to the first position in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts]]. No comparison copy, imagery, crop positioning, or metadata changed.

** Validation
- Confirmed the rendered [[http://127.0.0.1:4323/before-after][localhost Before & After page]] now leads with Jason Bateman and keeps Jeremy Strong second.

** Handoff
This ordering change is included in the 2026-07-14 Packery and Before & After publication batch.

* 2026-07-14 - Fix wide-screen Packery row-grid drift

** Changed
- Diagnosed the remaining live homepage holes in Jay's Chrome viewport at 4200 CSS pixels wide. The cold-load lifecycle fix was running, but Packery quantized only horizontal columns; responsive aspect-ratio heights plus the fixed 8px gutter made regular, wide, and wide-tall outer heights differ by a few pixels and prevented valid openings from being backfilled.
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/scripts/packeryGallery.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/scripts/packeryGallery.ts]] to measure ~rowHeight~ from the responsive ~.grid-sizer~ as well as measuring ~columnWidth~ from it.
- Updated both critical and settled CSS in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro]] so the hidden measurement sizer has the regular portrait aspect ratio, ~2 / 3~. The resulting packing geometry is responsive and does not assume eight columns is a permanent maximum.
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/debug/packery-responsive-row-grid-drift.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/docs/debug/packery-responsive-row-grid-drift.org]] and updated the Packery configuration and codebase-wisdom docs with the second failure mechanism and expanded verification contract.
- Added the current portfolio stack to [[file+emacs:/Users/jay/Dropbox/github/github-directory-tech-stack.org][📄 /Users/jay/Dropbox/github/github-directory-tech-stack.org]] after confirming the project was absent from the shared audit. This is a separate repository-level documentation change and is not part of the photography repo diff.

** Validation
- ~pnpm lint~ passed, ~pnpm exec vitest run~ passed all 21 tests, and ~pnpm build~ generated all 99 pages before the documentation closeout.
- Cold-load browser checks passed at both sides of every current desktop breakpoint from 769px through 2616px, then at 3440, 4200, 5120, and 7680px. Every run rendered all 34 items with zero internal holes, overlaps, or horizontal overflow after the initial transition settled.
- Temporary local-only breakpoints forced nine columns at 6000px and ten columns at 7680px; both also passed with zero holes, overlaps, or overflow. The test rules were removed afterward, so production still uses the existing three-through-eight-column design.
- The 390px mobile fallback remained a full-width flex stack with all 34 items and no horizontal overflow. The final localhost page also passed at the browser's restored default 3360px viewport.

** Handoff
The responsive row-grid fix is implemented and verified at [[http://127.0.0.1:4323/][http://127.0.0.1:4323/]] and is included in the 2026-07-14 Packery and Before & After publication batch.

* 2026-07-13 - Integrate the Bateman highlights into the saved Packery order

** Changed
- Added all three approved Jason Bateman highlights to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/saved-order.json][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/saved-order.json]] so the two landscape photographs no longer append together at the end and the saved sequence ends on portraits.
- Preserved the planned first insertion position immediately after ~dave-franco_sundance_2025.jpg~ because its original anchor, the solo Kaia Gerber highlight, had already been removed. Used the two other planned anchors exactly and did not reorder any existing entry.
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/debug/packery-cold-load-layout-race.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/docs/debug/packery-cold-load-layout-race.org]] with the Phase 3 result and the boundary between internal Packery gaps and an unavoidable incomplete final row.

** Validation
- ~pnpm build~ generated all 99 pages, ~pnpm exec vitest run~ passed all 21 tests, and ~pnpm lint~ passed.
- Fresh browser contexts with no ~photoOrder~ local-storage override showed zero internal gaps and zero horizontal overflow at both target desktop widths. The 1728px layout used five columns and ended with two columns occupied; the 2048px layout used six columns and ended with three columns occupied.
- No further landscape nudge was warranted. The residual whitespace is only the final incomplete row and cannot be removed by reordering the same tile inventory.

** Handoff
The Phase 3 saved-order closeout follows the lifecycle and highlight-selection commit ~e123323~. After reviewing the fresh-context results, Jay approved the complete batch for commit and push.

* 2026-07-13 - Remove the solo Kaia Gerber homepage highlight

** Changed
- Removed ~Kaia_Gerber_at_the_2024_Toronto_International_Film_Festival_03_(cropped).jpg~ from the homepage Packery configuration, saved order, and highlight metadata registry after Jay decided it was not a strong enough photograph for the highlights selection.
- Kept the separate Kaia Gerber and Cory Michael Smith Sundance highlight, the Kaia Gerber person record, and the underlying Cloudinary asset unchanged.

** Validation
- ~pnpm lint~ passed and ~pnpm build~ generated all 99 pages.
- The localhost homepage rendered 34 Packery items with no solo Kaia Gerber tile, while the separate Kaia Gerber and Cory Michael Smith photograph remained present.

** Handoff
The solo Kaia Gerber portrait removal is included in ~e123323~. Jay approved that commit for publication with the Packery lifecycle and ordering closeout on 2026-07-13.

* 2026-07-13 - Fix the Packery cold-load layout race

** Changed
- Changed [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/scripts/packeryGallery.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/scripts/packeryGallery.ts]] so desktop Packery reloads its internal item order and performs a full layout immediately after DOM-order restoration, before lazy images finish. Consolidated the desktop image lifecycle into one ~imagesLoaded~ instance, animation-frame-coalesced progress layouts, and one final safety layout; removed the initialization-time ~shiftLayout()~ and per-tile ~fit()~ sequence.
- Kept the first layout ahead of the optional Draggabilly import. During admin verification, corrected drag auto-save to store canonical ~data-filename~ values instead of extensionless Cloudinary URL basenames, allowing the saved sequence to restore exactly after reload.
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/debug/packery-cold-load-layout-race.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/docs/debug/packery-cold-load-layout-race.org]] with the live reproduction evidence, reviewed plan, implementation result, verification matrix, acceptance criteria, non-goals, and rollback boundary.
- Added the report to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/CLAUDE.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/docs/CLAUDE.org]] so future sessions can find the diagnosis and verified lifecycle contract before changing Packery behavior.

** Validation
- ~pnpm exec vitest run~ passed all 21 tests, ~pnpm lint~ passed, and ~pnpm build~ generated all 99 pages.
- Cold-cache browser checks began with only 2 of 37 page images complete but already showed five Packery columns at 1728px and six at 2048px. Tile positions and container heights remained unchanged as dozens of images completed; neither viewport had horizontal overflow or browser errors.
- The real TIFF filesystem gallery initialized and filtered correctly. The 390px mobile fallback remained full-width flex layout with no Packery instance. Admin mode dragged, auto-saved, reloaded, and restored the exact 35-item sequence.
- A settled 2048px visual check confirmed the giant transient blanks were gone. Two isolated one-column openings remain at the final edge because the two Jason Bateman and Jude Law landscapes each span two columns; that is a separate ordering question.

** Handoff
The Packery lifecycle fix and documentation are included in ~e123323~. Packery, lazy loading, edge-to-edge sizing, PhotoSwipe, filtering, and the mobile fallback remain in place. Jay approved that commit for publication with the Phase 3 ordering closeout on 2026-07-13.

* 2026-07-13 - Add Jay’s selected Jason Bateman photographs to Packery highlights

** Changed
- Opened the four TIFF 2025 photographs containing Jason Bateman one at a time in Preview and collected explicit Spokenly decisions. Jay selected the solo portrait, both Jason Bateman and Jude Law frames, and rejected the larger group frame.
- Copied the three approved, metadata-reviewed source files into [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights][📁 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights/]] without changing their bytes. Uploaded only those three files to Cloudinary as ~highlights/jason-bateman_tiff_2025~, ~highlights/jason-bateman-jude-law_tiff_2025~, and ~highlights/jason-bateman-jude-law-09836_tiff_2025~.
- Added the approved files to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/homepage-images.js][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/homepage-images.js]] as one portrait and two landscape tiles, reusing the factual TIFF captions. Added their exact source dimensions to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/image-metadata.json][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/image-metadata.json]]. Existing ~saved-order.json~ behavior appends them after the saved sequence; no homepage resequencing was requested.
- Updated [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/instructions.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/instructions.org]] with the one-at-a-time Preview selection protocol, approved-subset Cloudinary rule, saved-order behavior, and the current dimension workflow. Updated both AI handoffs to remove the completed TIFF publication task while retaining the source-photo work still needed for Russell Crowe, Rami Malek, Matt Damon, and Don Cheadle.
- Recorded in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/codebase-wisdom.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/docs/codebase-wisdom.org]] that the legacy ~scripts/generate-image-metadata.js~ scans only ~public/~, generates unrelated WebP siblings, and omits current ~src/gallery/highlights/~ assets. The unintended derivatives from the diagnostic run were removed and the two touched tracked files were restored before the exact dimensions were added manually.

** Validation
- SHA-1 checks confirmed that each highlight copy is byte-for-byte identical to its reviewed TIFF gallery source.
- All three transformed Cloudinary URLs returned HTTP ~200~.
- ~pnpm test -- --run~ passed all 21 Vitest tests, ~pnpm lint~ passed, and ~pnpm build~ generated all 99 pages.
- Playwright checked [[http://127.0.0.1:4323/][the existing localhost homepage]] after Packery settled: each selected filename appeared exactly once and all three Cloudinary images loaded with nonzero natural dimensions. The rejected group frame was not copied, configured, or uploaded.

** Handoff
The three Jason Bateman selections are live on Cloudinary and fully wired locally, but the repository changes have not been committed or pushed. The public homepage will not change until Jay explicitly requests a push and Vercel completes the deployment.

* 2026-07-13 - Publish the metadata-reviewed TIFF 2025 set locally and add Jason Bateman before/after

** Changed
- Copied the six Exwalker-reviewed photographs from [[file+emacs:/Volumes/Pantheon/lightroom/lightroom-exports/red-carpet-new][📁 /Volumes/Pantheon/lightroom/lightroom-exports/red-carpet-new/]] into [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/red-carpet/tiff][📁 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/red-carpet/tiff/]] under semantic publication filenames while preserving the embedded metadata and the original export files.
- Converted the TIFF gallery’s ~tags.json~ to the documented structured format with factual captions, people, year, and shared event tags. Added backward-compatible structured ~tags.json~ support to the gallery, photo-detail, Most Liked, and red-carpet person routes through [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/lib/galleryMetadata.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/lib/galleryMetadata.ts]].
- Added Jason Bateman, John Slattery, and Ben Jackson to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/people.json][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/people.json]] and explicitly mapped every group photograph to all pictured people. Jude Law’s existing page now includes the new TIFF 2025 photographs.
- Added the user-supplied previous Jason Bateman photograph at [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/images/wikiportraits/jason-bateman-before.jpg][📷 /Users/jay/Dropbox/github/photos-jaydixit/src/assets/images/wikiportraits/jason-bateman-before.jpg]] and added the new solo TIFF portrait as the second comparison in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts]]. Reused the canonical local gallery file for the after image and omitted unverified Commons/license claims.
- Documented Exwalker as the human truth gate and separated the filesystem-gallery, person-page, Packery-highlight, and before/after lanes in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/instructions.org][📄 /Users/jay/Dropbox/github/photos-jaydixit/instructions.org]], both AI-facing handoff files, the README, and codebase wisdom. Mirrored the portfolio handoff in the Exwalker instructions and work log.
- Left [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights][📁 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights/]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/homepage-images.js][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/data/homepage-images.js]] unchanged. No images were uploaded to Cloudinary; Jay will choose the main Packery highlights separately.

** Validation
- ~pnpm run photo:validate -- ./src/gallery/photos/red-carpet/tiff~ reported ~7/7~ valid photographs from merged embedded metadata and ~tags.json~.
- ~pnpm test -- --run~ passed all 21 Vitest tests, ~pnpm lint~ passed, and ~pnpm build~ generated 99 pages including all six new photo-detail routes and the new person routes.
- Playwright confirmed the TIFF gallery renders seven photographs with the human-reviewed captions, all lazy images load after scrolling, the before/after page renders six complete comparison pairs, and the red-carpet index now renders 22 qualifying solo-subject cards.
- HTTP checks returned ~200~ for the TIFF gallery, Jason Bateman, Jude Law, John Slattery, Ben Jackson, and before/after routes. Rendered screenshots confirmed the new Jason Bateman pair and all seven TIFF gallery photographs.

** Handoff
The local site is ready for Jay to choose any photographs that should also appear on the main Packery highlights page. Those selected files will need highlight copies, Cloudinary ~highlights/~ uploads, and entries in ~src/data/homepage-images.js~. The complete TIFF gallery does not require Cloudinary. Nothing in this entry has been pushed or deployed.

* 2026-07-13 - Refine experiment subhead

** Changed
- Replaced ~My favorite way to avoid doing any actual work~ with Jay's exact revised subhead, ~My favorite way to avoid doing my actual work~, on both experimental landing pages.
- Added the final period to Jay's locked headline, ~The Opposite of Writing.~, on both experimental landing pages.

** Validation
- Confirmed both local experiment routes return HTTP ~200~ and render the exact revised subhead.

* 2026-07-12 - Replace experiment headline and subhead

** Changed
- Replaced ~Portraits made to be seen.~ on both experimental landing pages with Jay's locked headline, ~The Opposite of Writing~.
- Replaced both supporting lines with Jay's locked subhead, ~My favorite way to avoid doing any actual work~.
- Preserved the Contact Sheet's three-line headline composition as ~The~ / ~Opposite of~ / ~Writing~.

** Validation
- Ran Prettier, ~pnpm run lint~, ~pnpm run test~, and ~pnpm run build~.
- Confirmed both local experiment routes return HTTP ~200~ and render the exact locked copy.

* 2026-07-12 - Replace grayscale Contact Sheet with completed color version

** Changed
- Replaced [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/contact-sheet.astro][📄 Contact Sheet / Public Record]] with the completed color version recovered from the separate ~codex/wikipedia-portrait-variants~ worktree.
- Restored the curated color portrait set, full-color resting images, color-enhancing hover treatment, magnified color detail strip, truthful filename labels, and the refined contact-sheet layout.
- Left [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/living-archive.astro][📄 The Living Archive]] unchanged.

** Validation
- Ran Prettier, ~pnpm run lint~, ~pnpm run test~, and ~pnpm run build~.
- Confirmed the local route returns HTTP ~200~, its route-specific resting-image rule is ~filter: none~, and the Contact Sheet source contains no grayscale rule.

* 2026-07-12 - Add homepage experiment chooser

** Changed
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/index.astro][📄 experiments index]] as a noindexed review page linking to the canonical homepage, The Living Archive, and Contact Sheet / Public Record.

** Validation
- Ran ~pnpm run lint~, ~pnpm run test~ (21 tests), and ~pnpm run build~; all passed, and the chooser plus all three linked routes built successfully.
- Ran Prettier across all three experiment routes.
- Confirmed the chooser links resolve from the local site.

* 2026-07-11 - Import ChatGPT Work Wikipedia portrait experiments

** Changed
- Imported the =codex/wikipedia-portrait-variants= designs for [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/living-archive.astro][📄 The Living Archive]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/experiments/contact-sheet.astro][📄 Contact Sheet / Public Record]] into the local main working tree.
- Kept both designs as standalone, noindexed review routes under ~/experiments/~; the existing homepage at [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][📄 src/pages/index.astro]] was not changed.

** Validation
- Ran ~pnpm run build~ in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit][📁 /Users/jay/Dropbox/github/photos-jaydixit]]; both experiment routes built successfully.
- Confirmed the built HTML for both routes contains ~noindex, nofollow~.
- Confirmed both routes return HTTP ~200~ from the existing local dev server at [[http://127.0.0.1:4323/experiments/living-archive/][The Living Archive]] and [[http://127.0.0.1:4323/experiments/contact-sheet/][Contact Sheet / Public Record]].

* 2026-07-09 - Record pending portfolio image additions

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/CLAUDE.org][docs/CLAUDE.org]]
  - Added an important pending TODO to add images of Jason Bateman, Russell Crowe, Rami Malek, Jude Law, Matt Damon, and Don Cheadle to the portfolio site.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/CLAUDE.org][CLAUDE.org]]
  - Mirrored the TODO in the legacy handoff file while docs are still maintained in both places.

** Validation
- Docs-only change; no build run.

* 2026-07-08 - Tune about CTA and self-host WikiPortraits story examples

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about.astro][src/pages/about.astro]]
  - Changed "starlet" to "movie star" in the red-carpet process paragraph.
  - Changed the hero "View My Work" CTA from the same-page ~#photography~ anchor to the dedicated ~/before-after~ page.
  - Removed the duplicate ~Writing & Teaching~ / ~Academic & Editorial~ card section because the lead bio already covers Yale, publications, Psychology Today, and OpenAI.
  - Removed the résumé-style ~AI Work~ section and replaced it with a single routing line to [[https://socraticai.co/][socraticai.co]] under the existing warm cross-sell paragraph.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/content/blog/wikiportraits-story.mdx][src/content/blog/wikiportraits-story.mdx]]
  - Replaced the Pete Davidson Wikimedia hotlinks and the Emil Wakim Reddit preview hotlink with local ~/images/blog/...~ paths.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/images/blog/][public/images/blog/]]
  - Added local copies of the three third-party WikiPortraits story examples so the post no longer depends on expiring Reddit preview URLs or mutable Wikimedia file paths.

** Validation
- ~file public/images/blog/pete-davidson-former-wikipedia-photo-2015.jpg public/images/blog/pete-davidson-wikipedia-portrait-cropped.png public/images/blog/emil-wakim-wikipedia-photo-reddit.png~ confirmed valid local image files.
- Built HTML for ~/blog/wikiportraits-story/~ references the local ~/images/blog/...~ files and no longer contains the Reddit preview or Wikimedia upload URLs.
- Built HTML for ~/about/~ includes the Socratic AI routing line and no longer contains the deleted ~AI Work~, ~Writing & Teaching~, or ~Academic & Editorial~ section headings.
- Local preview at [[http://127.0.0.1:4323/about/][http://127.0.0.1:4323/about/]] confirms the same about-page content; server is running in tmux session ~photos-jaydixit-4323~.
- ~pnpm exec prettier --check src/pages/about.astro~ passed.
- ~pnpm lint~ passed.
- ~pnpm build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.
- ~pnpm exec prettier --check src/content/blog/wikiportraits-story.mdx~ still reports a formatting warning that also reproduces on the HEAD version of the file; left the prose-heavy MDX file unformatted to avoid unrelated prose churn.

* 2026-07-08 - Tighten homepage intro-to-grid transition

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][src/pages/index.astro]]
  - Tried the subtle transition-band option for the wide-monitor intro issue: kept the intro text readable and centered, but reduced the intro bottom margin so the edge-to-edge image grid begins closer to the CTA.
  - Reduced the CTA top margin slightly so the intro reads as one compact editorial block.

** Validation
- Browser check against [[http://localhost:4324/][http://localhost:4324/]] confirmed the intro-to-grid gap is now about 20px on both 1440px desktop and 390px mobile, with no horizontal overflow.
- Screenshots reviewed from ~/tmp/photos-home-tight-intro-1440.png~ and ~/tmp/photos-home-tight-intro-390.png~.

* 2026-07-08 - Promote edge-to-edge Packery homepage

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][src/pages/index.astro]]
  - Promoted the successful edge-to-edge Packery masonry experiment to the homepage.
  - Kept the homepage metadata canonical: ~/~, ~Red Carpet Highlights~, and no ~noindex~ flag.
  - Changed desktop/tablet masonry sizing from fixed historical widths to gutter-aware fluid formulas so visible columns fill the viewport left-to-right.
  - Added an ~overflow-x: hidden~ guard on the Packery container so the staggered first layout cannot expose a transient horizontal scrollbar.
  - Kept the 768px-and-below mobile flex fallback intact.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/archive/source-snapshots/index-before-edge-to-edge-packery-2026-07-08.astro][docs/archive/source-snapshots/index-before-edge-to-edge-packery-2026-07-08.astro]]
  - Archived the previous homepage source outside [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/][src/pages/]] so it can be restored without exposing a backup route.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/packery-edge-to-edge-configuration.org][docs/packery-edge-to-edge-configuration.org]]
  - Added the current working Packery configuration, column formulas, failure explanation, verification checklist, and revert path.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/codebase-wisdom.org][docs/codebase-wisdom.org]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/CLAUDE.org][docs/CLAUDE.org]]
  - Recorded the reusable edge-to-edge Packery lesson and updated the docs map so future agents see the current Packery contract.
- Removed temporary experiment route [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/masonry-centered.astro][src/pages/masonry-centered.astro]] after promotion.

** Solution
- The earlier empty right band was caused by trying to center or pad a fixed-width Packery stage. That can move the grid, but it cannot make fixed 320px / 648px columns fill an arbitrary viewport.
- The working layout keeps the stage full-width and makes the columns themselves fluid: regular item width is ~100% / N - 8px * (N - 1) / N~, and wide item width is ~200% / N - 8px * (N - 2) / N~.
- The formulas live in both the critical inline CSS and the settled page CSS because Packery reads item widths from first-rendered DOM geometry.

** Validation
- ~pnpm exec prettier --check src/pages/index.astro~ passed.
- ~pnpm build~ passed: Astro built 84 pages.
- ~pnpm lint~ passed.
- ~pnpm test -- --run~ passed: 21 Vitest tests; existing ~unknown.jpg~ fixture warnings appeared.
- ~git diff --check~ passed.
- Browser check against [[http://localhost:4324/][http://localhost:4324/]] passed at 390, 800, 1024, 1200, 1300, 1366, 1440, 1728, 1960, 2288, and 2616px after waiting for Packery's staggered first layout to settle. Assertions covered: no meaningful left/right gaps, no horizontal overflow, no overlapping masonry items, no visible broken images, canonical ~/~, no robots ~noindex~, Packery enabled on desktop/tablet, and Packery disabled on mobile.

* 2026-07-06 - Update about-page WikiPortraits credit

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about.astro][src/pages/about.astro]]
  - Changed the lead bio line from "founding photographer" to "first photographer" and named WikiPortraits founders Jennifer 8. Lee and Kevin Payravi.

** Validation
- Browser check against [[http://localhost:4327/about/][http://localhost:4327/about/]] confirmed the updated sentence renders.
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.

* 2026-07-06 - Tighten before-after standfirst copy

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Updated the hero standfirst to say the page replaces "low-quality or outdated portraits" with professional images Jay shoots at film festivals and cultural events.

** Validation
- Browser check against [[http://localhost:4327/before-after/][http://localhost:4327/before-after/]] confirmed the updated standfirst renders.
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.

* 2026-07-06 - Lead before-after page with Jeremy Strong

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][src/data/beforeAfter.ts]]
  - Moved the Jeremy Strong comparison to the top of the before/after sequence so [[http://localhost:4327/before-after/][the before/after page]] opens with a different pair than the WikiPortraits story hero.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Added a focused assertion that the first before/after card is Jeremy Strong.

** Validation
- Browser check against [[http://localhost:4327/before-after/][http://localhost:4327/before-after/]] confirmed the visible order starts Jeremy Strong, Vanessa Kirby, John Hopfield, Conan O'Brien, Sydney Sweeney.
- ~PLAYWRIGHT_BASE_URL=http://localhost:4327 pnpm exec playwright test tests/e2e/portfolio.e2e.ts -g "before-after page renders source captions"~ passed.
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.

* 2026-07-06 - Shrink WikiPortraits Emil screenshot

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/[slug].astro][src/pages/blog/[slug].astro]]
  - Reduced the ~article-image-medium~ max width from ~38rem~ to ~24rem~ so the Emil Wakim screenshot on the WikiPortraits story renders smaller without changing the post copy.

** Validation
- Browser check against [[http://localhost:4327/blog/wikiportraits-story/][http://localhost:4327/blog/wikiportraits-story/]] confirmed the Emil screenshot renders at 384px wide on a 1280px viewport.
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.

* 2026-07-06 - Add WikiPortraits hero and tighten caption layout

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/content/blog/wikiportraits-story.mdx][src/content/blog/wikiportraits-story.mdx]]
  - Made the second Pete Davidson image use the same small inline figure treatment as the first image.
  - Shortened the transition sentence and added Jay's caption for the current Wikipedia photo.
  - Added a Vanessa Kirby before/after hero using the existing WikiPortraits before/after assets.
  - Set the Vanessa hero metadata to mirror left/right order: ~Before~ then source, ~After~ then photo description.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/[slug].astro][src/pages/blog/[slug].astro]]
  - Gave article captions a consistent readable max width and decoupled small-image width from caption width.
  - Added responsive styling for the WikiPortraits Vanessa Kirby hero.
  - Prevented MDX paragraph-wrapped captions and hero metadata from inheriting the article body typography.
  - Kept the Vanessa hero before/after image frames top-aligned.
  - Added a medium-width treatment for the Emil Wakim screenshot.

** Validation
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.
- Browser check against [[http://localhost:4327/blog/wikiportraits-story/][http://localhost:4327/blog/wikiportraits-story/]] confirmed both Pete Davidson figures use the small 288px treatment, the replacement line now reads "It was eventually replaced with this:", and the new caption appears below the second Pete image.
- Browser check against [[http://localhost:4327/blog/wikiportraits-story/][http://localhost:4327/blog/wikiportraits-story/]] confirmed the Vanessa hero before/after image frames have a top-position difference of 0px, the metadata reads ~Before~ / ~Wikipedia portrait, 2018~ and ~After~ / ~Vanessa Kirby - Wikipedia photo by Jay Dixit~, the hero no longer contains "upgraded", the Emil image renders at 608px wide, and all article captions render at 14.08px / 21.12px instead of inheriting article body type.

* 2026-07-06 - Fix before-after standfirst italics and diagnose local homepage lightbox

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Removed italic styling from the hero standfirst beginning "Wikipedia deserves better photographs."

** Notes
- Investigated homepage PhotoSwipe on [[http://localhost:4327/][http://localhost:4327/]] versus [[https://photos.jaydixit.com/][https://photos.jaydixit.com/]].
- The first local browser probe failed to dynamically import ~photoswipe.js~ with Vite's ~504 (Outdated Optimize Dep)~ error, while the live production homepage opened PhotoSwipe successfully. After Vite reoptimized dependencies during a fresh build, the local homepage lightbox opened successfully too.

** Validation
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.
- Browser check against [[http://localhost:4327/before-after][http://localhost:4327/before-after]] confirmed the standfirst renders with ~font-style: normal~.
- Browser check confirmed homepage PhotoSwipe opens on both [[http://localhost:4327/][http://localhost:4327/]] and [[https://photos.jaydixit.com/][https://photos.jaydixit.com/]] after local Vite dependency reoptimization.

* 2026-07-06 - Align blog article typography with editorial pages

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/[slug].astro][src/pages/blog/[slug].astro]]
  - Replaced the old Tailwind Typography-driven blog article treatment with page-scoped editorial typography using the same ~Newsreader~ and ~Instrument Sans~ system as the about page.
  - Restyled blog post title, metadata, tags, excerpt, body copy, headings, links, blockquotes, figures, and captions.

** Validation
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- ~git diff --check~ passed.
- Browser check against [[http://localhost:4327/about][http://localhost:4327/about]] and [[http://localhost:4327/blog/wikiportraits-story/][http://localhost:4327/blog/wikiportraits-story/]] confirmed matching body typography: ~Newsreader~, 24px, weight 300, 36px line height, and matching text color.
- Browser check against [[http://localhost:4327/blog/hello-world/][http://localhost:4327/blog/hello-world/]] and [[http://localhost:4327/blog/nobel-portrait-session/][http://localhost:4327/blog/nobel-portrait-session/]] confirmed the same blog article typography applies to other posts.

* 2026-07-06 - Replace WikiPortraits story draft

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/content/blog/wikiportraits-story.mdx][src/content/blog/wikiportraits-story.mdx]]
  - Replaced the short WikiPortraits essay body with Jay's new longform draft.
  - Added the Pete Davidson Wikipedia-image examples, the Reddit screenshot example, and the Jeremy Strong before/after image pair.
  - Added Jay's caption to the Emil Wakim Wikipedia-photo example.
  - Used the section heading ~“For the love of God, please take down that photo.”~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/[slug].astro][src/pages/blog/[slug].astro]]
  - Added a small responsive helper for inline before/after grids inside blog content.
  - Constrained the first Pete Davidson example figure to a smaller inline width.
  - Added a responsive arrow treatment for the Jeremy Strong before/after image pair.

** Validation
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- Browser check against [[http://127.0.0.1:4330/blog/wikiportraits-story/][http://127.0.0.1:4330/blog/wikiportraits-story/]] confirmed the quoted subhead, copy fixes, four article figures, no broken article images, the first Pete Davidson figure at 288px wide, image-arrow-image desktop before/after layout, one-column mobile layout with a rotated arrow, and the Jeremy Strong caption text.

* 2026-07-06 - Remove accidental italic styling from about-page note

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about.astro][src/pages/about.astro]]
  - Removed the one-off ~ed-essay-emph~ class from the final paragraph of the opening essay and deleted its italic CSS rule.
  - Left the paragraph text unchanged.

** Validation
- ~pnpm lint~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.
- Browser computed-style check against [[http://127.0.0.1:4330/about/][http://127.0.0.1:4330/about/]] confirmed the final essay paragraph now has ~font-style: normal~.

* 2026-07-06 - Add person-level Wikipedia provenance examples

** Status
- Added person-level Wikimedia provenance for confirmed current lead-photo examples with existing local red-carpet pages: Lisa Gilroy and Sydney Sweeney.
- Verified 2026-07-06 via Wikipedia and Commons APIs: [[https://en.wikipedia.org/wiki/Lisa_Gilroy][Lisa Gilroy]] currently uses [[https://commons.wikimedia.org/wiki/File:Lisa_Gilroy_at_SXSW_in_2025.jpg][File:Lisa Gilroy at SXSW in 2025.jpg]], artist Jay Dixit, license CC BY 4.0.
- Verified 2026-07-06 via Wikipedia and Commons APIs: [[https://en.wikipedia.org/wiki/Sydney_Sweeney][Sydney Sweeney]] currently uses [[https://commons.wikimedia.org/wiki/File:Sydney_Sweeney_at_the_2024_Toronto_International_Film_Festival_01_(3x4_cropped_and_rotated).jpg][File:Sydney Sweeney at the 2024 Toronto International Film Festival 01 (3x4 cropped and rotated).jpg]], artist Jay Dixit, license CC BY-SA 4.0.
- Verified 2026-07-06 via Wikipedia and Commons APIs: [[https://en.wikipedia.org/wiki/Gabriel_LaBelle][Gabriel LaBelle]] currently uses [[https://commons.wikimedia.org/wiki/File:Gabriel_LaBelle_at_the_2024_Toronto_International_Film_Festival_03_(cropped)(2).jpg][File:Gabriel LaBelle at the 2024 Toronto International Film Festival 03 (cropped)(2).jpg]], artist Jay Dixit, license CC BY-SA 4.0. Left this out of [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/people.json][src/data/people.json]] for now because there is no local gallery/highlight asset under [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/][src/gallery/]], so adding a person record would generate a dead/404 person route until the local asset coverage task is done.
- Verified 2026-07-06 via Wikipedia and Commons APIs: [[https://en.wikipedia.org/wiki/Nicholas_Braun][Nicholas Braun]] currently uses [[https://commons.wikimedia.org/wiki/File:NicholasBraun-byPhilipRomano.jpg][File:NicholasBraun-byPhilipRomano.jpg]], artist PhilipRomanoPhoto. Did not mark Nicholas Braun as current Jay-photo usage.

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/people.json][src/data/people.json]]
  - Added ~commonsUrl~, ~articleUsageStatus~, ~sourceCredit~, ~license~, and ~licenseUrl~ to Lisa Gilroy and Sydney Sweeney.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/[person].astro][src/pages/red-carpet/[person].astro]]
  - Extended the person schema with the same optional provenance fields used by before/after data.
  - Added a compact ~photo-credit~ provenance row in person-page headers when confirmed data exists.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Added a focused browser test confirming Lisa Gilroy renders provenance and Nicholas Braun does not render a current Jay-photo claim.

** Validation
- ~pnpm lint~ passed.
- ~pnpm test -- --run~ passed: 21 Vitest tests; existing missing fixture warnings for ~unknown.jpg~ appeared.
- ~pnpm exec playwright test tests/e2e/portfolio.e2e.ts -g "red-carpet person pages render confirmed Wikimedia provenance only"~ passed.
- ~pnpm run build~ passed: Astro built 84 pages.

* 2026-07-05 - Session handoff: pause point, next steps, and Jay's action items

** Where we stopped
- The design track (from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][📄 site-wide-improvements-implementation-plan.org]]) is through its first three slices: before/after data extraction + EditorialPhotoFrame, shared editorial text classes (~.eyebrow~ / ~.photo-caption~ / ~.photo-credit~ / ~.editorial-link~), and Wikimedia provenance on /before-after.
- Working pattern for this repo: Claude (Fable) investigates, verifies, and writes instruction blocks; Codex executes and commits; Jay pushes.

** ⚠ Jay's action items (only Jay can do these)
- *Push the 4 queued commits* when ready: ~a9661a3~ (before/after extraction), ~89bd5fb~ (red-carpet card enlargement), ~fe7a745~ (editorial classes), ~e4840ca~ (provenance). All verified; nothing else is uncommitted.
- *Conan Commons file*: Jay's Sundance Conan shot could not be found on Wikimedia Commons (his Commons Conan uploads are only the SXSW 2024 Office Space set; the 2025 Sundance set there is Adam Chitayat's). Find the Commons filename or confirm it was never uploaded — then it's a one-line ~commonsUrl~ edit in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][📄 src/data/beforeAfter.ts]].
- *Supabase dashboard session*: the RLS verification on ~photo_user_likes~ and the /popular aggregation view need dashboard access. Ask Claude to prep the SQL first; then it's paste-and-run.
- *Decide the /gallery/ content story*: either move homepage highlight photos into real folders under src/gallery/photos/ so galleries are substantive, or drop "Photo Galleries" from the nav until there's content.
- *Decide the unused image folders*: ~78 MB of committed originals in unused top-level src/gallery/ folders (nature, street, travel, cannes-2024, …) — wire them in or remove them.
- *FYI, verified 2026-07-05*: the Wikipedia articles for Jeremy Strong and Conan O'Brien no longer use Jay's photos (current leads by Bryan Berlin and Adam Chitayat respectively). Vanessa Kirby, John Hopfield, and Sydney Sweeney still lead with Jay's photos. The site's ~articleUsageStatus~ data reflects this; only positive statuses render publicly.

** Next steps (AI-executable, roughly in order)
1. Photo-detail-page provenance blocks — extend the beforeAfter.ts provenance pattern to individual photo pages (per-photo ~commonsUrl~ data source needed, likely per-folder json or people.json extension). Review item + plan §5 second slice.
2. Supabase SQL prep (schema + RLS policies + /popular aggregation view/RPC as ~supabase/schema.sql~) so Jay's dashboard session is paste-and-run; then fix the unbounded query in src/pages/popular.astro:267.
3. Dead-code sweep + shared photoNaming module + real tests (review Tier 4): fslightbox removal, dead YAML gallery subsystem, starter-project clutter, extracting the copy-pasted caption/filename logic into src/lib/photoNaming.ts with Vitest coverage. NOTE: packery/imagesloaded/draggabilly and isotope-layout STAY (see decisions below).
4. Design track continuation: homepage first-viewport edit + contextual captions (plan §2 — data/caption changes only, no layout-engine changes), then discovery indexes (events, Wikipedia upgrades, recently added — plan §4).
5. Small items: hover/transition standardization, alt-text audit, Gallery/MasonryGallery dedup, neutral-color-system consolidation, albums index design pass.

** Standing decisions (do not relitigate)
- Packery stays as the homepage/folder masonry engine (Jay, 2026-07-05; recorded in CLAUDE.org). Never propose replacing it.
- No "formerly on Wikipedia" / replacement language ever renders on the public site; ~'formerly-lead'~ is internal bookkeeping only.
- prototype-documentary.astro and salient-photography-demo.astro are kept (noindexed, out of sitemap); the two about-prototypes are deleted.

* 2026-07-05 - Add before/after Wikimedia provenance

** Status
- Completed the first Section 5 provenance slice from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]] for the ~/before-after/~ comparisons without changing existing prose or touching Packery-related files.

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][src/data/beforeAfter.ts]]
  - Populated the five comparison entries with Wikimedia provenance fields: ~commonsUrl~ where available, ~uploadStatus~, ~articleUsageStatus~, ~sourceCredit~, ~license~, and ~licenseUrl~.
  - Tightened ~articleUsageStatus~ to ~'lead' | 'formerly-lead' | 'unknown'~.
  - Left Conan O'Brien's ~commonsUrl~ omitted because the Sundance Commons page is unsettled.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Added one quiet ~photo-credit~ provenance row beneath each comparison's after-photo column.
  - Rendered the exact visitor-facing status only for current lead photos, license links for all five entries, and Commons links only when a Commons URL exists.
  - Kept ~EditorialPhotoFrame.astro~ and the existing before/after figure caption markup unchanged.

** Validation
- ~pnpm lint~ passed.
- ~pnpm test~ passed: 21 Vitest tests; the existing missing fixture warnings for ~unknown.jpg~ appeared.
- ~pnpm run build~ passed: Astro built 84 pages; only the existing Node ~DEP0205~ warning appeared.
- ~pnpm exec playwright test tests/e2e/portfolio.e2e.ts~ passed: 19 Playwright tests.
- Source URL checks confirmed the four Commons URLs and the Creative Commons license URL return HTTP 200.
- Local dev route [[http://127.0.0.1:4330/before-after/][http://127.0.0.1:4330/before-after/]] returned 200, rendered 5 ~.ba-pair~ articles, and rendered 5 provenance rows.
- Rendered-content checks confirmed Vanessa Kirby, John Hopfield, and Sydney Sweeney show "Currently the lead photo on Wikipedia"; Jeremy Strong shows only the license and Commons links; Conan O'Brien shows only the license link.
- Every Commons/license href extracted from the rendered local HTML returned HTTP 200 via ~curl -sI -L~.
- Desktop and mobile screenshots after a scroll-through were captured against local dev and production; comparison artifacts live in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/test-results/wikimedia-provenance/][test-results/wikimedia-provenance/]].

* 2026-07-05 - Land shared editorial text classes

** Status
- Completed Section 1's shared editorial typography pass from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]] without changing public-facing copy or touching Packery-related files.

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/styles/global.css][src/styles/global.css]]
  - Added the shared ~.eyebrow~, ~.photo-caption~, ~.photo-credit~, and ~.editorial-link~ component classes.
  - Derived the typography from the before/after page's existing kicker, caption source, and quiet-link treatments.
  - Chose an AA-safe muted value for ~.photo-credit~ so the credit line remains readable on the site's warm paper background.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/index.astro][src/pages/red-carpet/index.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about.astro][src/pages/about.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/contact.astro][src/pages/contact.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/index.astro][src/pages/blog/index.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/press.astro][src/pages/press.astro]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/PhotoComments.astro][src/components/PhotoComments.astro]]
  - Rewired local kicker classes to compose with ~.eyebrow~ and left page-local rules responsible only for spacing/layout.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/gallery/index.astro][src/pages/gallery/index.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/gallery/photo][src/pages/gallery/photo]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet][src/pages/red-carpet]]
  - Applied ~.photo-caption~, ~.photo-credit~, and ~.editorial-link~ to the requested card, person, gallery-name, photo-detail, EXIF, and Wikipedia-link surfaces while preserving layout classes and visible words.

** Validation
- ~pnpm lint~ passed.
- ~pnpm test~ passed: 21 Vitest tests; the existing missing fixture warnings for ~unknown.jpg~ appeared.
- ~pnpm run build~ passed: Astro built 84 pages; only the existing Node ~DEP0205~ warning appeared.
- ~pnpm exec playwright test tests/e2e/portfolio.e2e.ts~ passed: 19 Playwright tests.
- ~git diff --check~ passed.
- Local dev route checks on ~http://127.0.0.1:4330/~ returned 200 for ~/red-carpet/~, ~/gallery/~, ~/before-after/~, ~/about/~, ~/red-carpet/conan-obrien/~, and ~/gallery/photo/red-carpet/sundance/conan-obrien_sundance_2024/~.
- Desktop and mobile screenshots for those routes were captured against both local dev and production; comparison artifacts live in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/test-results/editorial-step-2/][test-results/editorial-step-2/]]. The only large production mismatch is from earlier local, unpushed work: the red-carpet 3-card layout and the before/after refactor state.
- Computed-style spot checks confirmed shared-class usage on the requested routes, including ~.photo-credit~ at ~rgb(110, 103, 92)~ and ~.eyebrow~ at the before/after kicker treatment.
- Source grep confirmed local kicker/card leftovers no longer duplicate the shared typography rules.

* 2026-07-05 - Enlarge red-carpet index cards to 3 per row

** Status
- Enlarged the portrait cards on [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/index.astro][src/pages/red-carpet/index.astro]] by changing the desktop grid from 4 cards per row to 3 cards per row.

** Changed
- Desktop grid now renders 3 cards per row with slightly larger gaps.
- Tablet grid steps down to 2 cards per row; small screens use 1 card per row.
- Card images now request responsive 480/720/960px candidates so the larger cards stay sharp.

** Validation
- Local dev route [[http://127.0.0.1:4330/red-carpet/][http://127.0.0.1:4330/red-carpet/]] returned 200 and rendered 3 desktop columns, 2 tablet columns, and 1 mobile column.
- ~pnpm lint~ passed.
- ~pnpm exec playwright test tests/e2e/portfolio.e2e.ts -g "red-carpet"~ passed: 3 tests.

* 2026-07-05 - Extract before/after data and EditorialPhotoFrame

** Status
- Completed the Section 3 pure refactor from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]] without changing the rendered ~/before-after/~ page.

** Changed
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/beforeAfter.ts][src/data/beforeAfter.ts]]
  - Moved the before/after comparison entries and 10 wikiportrait image imports into a typed data module.
  - Preserved the existing entry order, values, and hand-tuned ~beforePos~ / ~afterPos~ crop positions exactly.
  - Added empty optional Wikimedia provenance fields for future data entry: ~commonsUrl~, ~uploadStatus~, ~articleUsageStatus~, ~sourceCredit~, ~license~, and ~licenseUrl~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/EditorialPhotoFrame.astro][src/components/EditorialPhotoFrame.astro]]
  - Added the reusable editorial image frame for the PhotoSwipe anchor, Astro ~Image~, crop style, Before/After label, and source credit.
  - Computes the full-size lightbox href locally with ~getImage()~ so callers do not need page-level lightbox mapping.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Rewired the page to import ~comparisons~ and render two ~EditorialPhotoFrame~ instances per comparison.
  - Kept the hero, visible copy, comparison rows, arrow SVG, context section, CTA, empty fallback, and ~PhotoSwipeLightbox~ usage unchanged.
  - Converted only the component-owned frame/caption ~ba-*~ selectors to ~:global(...)~ so the existing page style declarations still apply inside the child component.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]]
  - Added the requested Current Applicability note that the Section 3 extraction landed.

** Validation
- ~pnpm lint~ passed.
- ~pnpm test~ passed: 21 Vitest tests.
- ~pnpm run build~ passed: Astro built 84 pages; only the existing Node ~DEP0205~ warning appeared.
- ~pnpm exec playwright test tests/e2e/portfolio.e2e.ts~ passed: 19 Playwright tests.
- Dev server verification on ~http://127.0.0.1:4330/before-after/~ returned 200, rendered 5 ~.ba-pair~ articles, kept only the first pair's two images at ~loading="eager"~ / ~fetchpriority="high"~, preserved object-position styles on all 10 comparison images, and opened the first PhotoSwipe image at full size.
- Desktop and mobile first-pair screenshots in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/test-results/before-after-refactor/][test-results/before-after-refactor/]] matched production [[https://photos.jaydixit.com/before-after/][photos.jaydixit.com/before-after]] with 0 differing pixels at 1% fuzz.

* 2026-07-05 - Second business card design: Ghost Disc Hero (print artifact)

** Status
- Built and exported a second two-sided card design, "Ghost Disc Hero" — the light/cream counterpart to the Night Premiere card, adapted from variant 05 of [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/og-card-explorations/contact-sheet-og-card.html][docs/og-card-explorations/contact-sheet-og-card.html]] (the finalized Ghost Disc favicon as monumental hero on cream, three-line lockup level beside it). The Night Premiere card files were not touched.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/business-card-ghost-disc-hero.html][docs/business-card/business-card-ghost-disc-hero.html]]
  - Both faces at 1050 × 600 CSS px (3.5 × 2 in @ 300dpi), same conventions as the Night Premiere source. Cream #FAF6EF ground; mark geometry verbatim from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/favicon.svg][public/favicon.svg]] (45%-opacity cream disc — near-invisible on cream by design, as on the contact sheet — + red-carpet runner + gold hairline ring, stroke 1.8).
  - FRONT: per the sheet variant, the mark leads on the LEFT — 380px SVG box, vertically centered, fully contained (visible ring ~320px = the OG variant's 53% ring-to-canvas proportion; ring edge ~90px from the left trim, ~140px from top/bottom). Lockup level beside it: "Jay Dixit" (Cormorant 600, ink) / "Red Carpet & Portrait Photographer" (Cormorant 500 at 32px with nowrap so the descriptor holds one line, italic ampersand) / "WIKIPORTRAITS · WIKIPEDIA" (Karla caps in the light-ground deep gold #A88434) / hairline deep-gold rule / contacts "jay@jaydixit.com" and "photos.jaydixit.com" in muted gray-brown Karla (#6b6459). Gold confined to badge, rule, and ring.
  - BACK: the mark large (320px, mirroring the Night Premiere back) and centered on cream, nothing else.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/business-card-ghost-hero-front.png][docs/business-card/business-card-ghost-hero-front.png]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/business-card-ghost-hero-back.png][docs/business-card/business-card-ghost-hero-back.png]]
  - 2100 × 1200 PNGs (front ~78 KB, back ~31 KB), same pipeline: Playwright chromium at deviceScaleFactor 2, ~document.fonts.ready~ wait, element screenshots, ~oxipng -o max --strip safe~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/export.mjs][docs/business-card/export.mjs]]
  - Extended to take a design argument: ~node export.mjs [night|ghost] [front|back ...]~ (defaults to night, all faces).

** Print notes
- Same print profile as the Night Premiere card: 300dpi proportions, 2× export, no bleed (a print shop may want +0.125 in bleed — solid cream ground, trivial to extend), all type and the full ring inside the safe zone.

* 2026-07-05 - Business card export (Night Premiere print artifact)

** Status
- Built and exported a two-sided US business card (3.5 × 2 in, landscape) in the Night Premiere brand style. This is a PRINT artifact, not a site asset — nothing in ~public/~ or ~src/~ was touched.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/business-card-night-premiere.html][docs/business-card/business-card-night-premiere.html]]
  - Standalone editable source rendering both faces at true print proportion (each face 1050 × 600 CSS px = 3.5 × 2 in @ 300dpi). Reuses the OG card's design language verbatim: tuxedo-black ground, ghost-disc mark rebuilt for dark ground (cream 6%-opacity disc + red-carpet runner + gold hairline ring), Cormorant Garamond cream type, Karla letterspaced gold caps for the badge line, same Google Fonts load.
  - FRONT (revised per Jay's review — first pass had a small mark at top-left): text block left-aligned and vertically centered on the left — "Jay Dixit" (Cormorant 500, dominant, cream) / "Red Carpet & Portrait Photographer" (Cormorant, ampersand in Cormorant italic) / hairline gold rule / "WIKIPORTRAITS · WIKIPEDIA" (Karla gold letterspaced caps) / two quiet soft-cream contact lines "jay@jaydixit.com" and "photos.jaydixit.com". The mark is LARGE on the right — 437px SVG box, vertically centered, FULLY CONTAINED inside the card (Jay rejected an earlier edge-cropped version — the complete outer gold ring must be visible, nothing clipped — then asked for two successive size trims, 500px → 460px → 437px). The ring's visible edge sits ~69px from the right trim and ~118px from top/bottom. Gold is confined to the badge line, the rule, and the mark ring.
  - BACK: the mark large and centered on tuxedo black, nothing else.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/business-card-front.png][docs/business-card/business-card-front.png]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/business-card-back.png][docs/business-card/business-card-back.png]]
  - 2100 × 1200 PNGs (front ~88 KB, back ~33 KB). Render method: Playwright (~@playwright/test~ chromium, driven by [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/business-card/export.mjs][docs/business-card/export.mjs]]) at deviceScaleFactor 2 so each 1050 × 600 face renders to 2100 × 1200 (600dpi-class for print sharpness); waits for ~document.fonts.ready~, screenshots the ~#front~ / ~#back~ elements; then optimized with ~oxipng -o max --strip safe~.

** Print notes
- Proportions are print-true at 300dpi (1050 × 600 CSS px), exported at 2× (2100 × 1200) for 600dpi-class sharpness.
- Safe zone honored: all text sits inside 80px padding, well past the 1/8 in (37px) minimum from every edge, and the front mark is fully contained — the complete outer gold ring is visible with margin on all sides; nothing touches or crosses the trim line.
- NO BLEED: faces are trimmed exactly at 3.5 × 2 in with no bleed margin. A print shop will likely want a bleed version (+0.125 in / 37px per edge, background extended). Because the ground is solid tuxedo black and nothing sits near the trim edge, a bleed build is trivial — just extend the black.

* 2026-07-05 - Hygiene batch: CI, head/meta, accent, empty states

** Status
- Completed the pnpm CI repair, head/meta cleanup, prototype search-visibility cleanup, bundled Alpine migration, accent/contrast sweep, and visitor-facing empty-state copy pass.
- Preserved the uncommitted OG / social share card work already present in this tree and layered these changes on top of it.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/.github/workflows/][.github/workflows/]]
  - Deleted the obsolete GitHub Pages deploy workflow and converted the remaining workflows from ~npm ci~ / Node 23 to pnpm, Node 22, and ~pnpm install --frozen-lockfile~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/package.json][package.json]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/pnpm-lock.yaml][pnpm-lock.yaml]]
  - Added a Node ~>=22~ engine pin and bundled ~alpinejs~ as a dependency.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/NavBar.astro][src/components/NavBar.astro]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][src/pages/index.astro]]
  - Added ~initial-scale=1~, removed the unpinned Alpine CDN load, started Alpine from a hoisted bundled script, and deduped homepage OG/Twitter tags while preserving the homepage's multi-image OG metadata and JSON-LD.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about-prototype.astro][src/pages/about-prototype.astro]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about-prototype-editorial.astro][src/pages/about-prototype-editorial.astro]]
  - Deleted obsolete about prototypes.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/prototype-documentary.astro][src/pages/prototype-documentary.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/salient-photography-demo.astro][src/pages/salient-photography-demo.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/astro.config.mts][astro.config.mts]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/robots.txt][public/robots.txt]]
  - Noindexed the surviving prototypes, excluded them from the sitemap, added a robots file, and kept the salient prototype alive while bundling its existing imagesLoaded/Isotope dependencies instead of serving them from unpkg.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tailwind.config.js][tailwind.config.js]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/][src/pages/red-carpet/]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/gallery/index.astro][src/pages/gallery/index.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][src/pages/index.astro]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/SocialShare.astro][src/components/SocialShare.astro]]
  - Added a Tailwind ~brand~ color because the existing shadcn-style ~accent~ key is occupied, replaced remaining live hardcoded blues with the editorial accent, bumped weak gray captions/icons to ~text-gray-500~, and replaced developer-facing empty-state copy with visitor-facing copy.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/eslint.config.js][eslint.config.js]]
  - Ignored [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/][docs/]] artifacts so local contact sheets/export scripts do not break the source lint command.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-review-2026-07-03.org][docs/site-review-2026-07-03.org]]
  - Marked the GitHub Actions, head/meta cleanup, and accent-color sweep items done; noted the contrast and empty-state accessibility sub-items as done while leaving the alt-text audit open.

* 2026-07-05 - OG / social share card export (Night Premiere)

** Status
- Exported the winning "Night Premiere" share card (tuxedo black, gold-ring mark rebuilt for dark ground, cream + gold three-line lockup) from the OG contact-sheet explorations and wired it in as the site-wide og:image / twitter:image fallback.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/og-card-explorations/og-card-night-premiere.html][docs/og-card-explorations/og-card-night-premiere.html]]
  - New standalone render page and editable source of record: the Night Premiere variant extracted from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/og-card-explorations/contact-sheet-og-card.html][contact-sheet-og-card.html]], rendered at true 1200×630 with zero scale/margin transforms. Same Google Fonts load (Cormorant Garamond + Karla). Copy: "Jay Dixit" / "Red Carpet & Portrait Photographer" (italic ampersand) / "WIKIPORTRAITS · WIKIPEDIA" gold letterspaced caps.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/og-card.png][public/og-card.png]]
  - New 1200×630 PNG. Render method: Playwright (~@playwright/test~ chromium) at a 1200×630 viewport, device scale factor 1, waits for ~document.fonts.ready~, screenshots the ~#og-card~ element; then optimized with ~oxipng -o max --strip safe~. Final size 37 KB.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]]
  - Changed the ~resolveOgImage~ fallback from ~/jaydixit-photos-logo.png~ to ~/og-card.png~, and updated the default ~ogImageAlt~ to "Jay Dixit — Red Carpet & Portrait Photographer, WikiPortraits · Wikipedia".
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/image-metadata.json][src/data/image-metadata.json]]
  - Added an ~og-card.png~ entry (1200×630), mirroring the ~jaydixit-photos-logo.png~ listing.

** Notes
- ~src/pages/index.astro~ photo-based Cloudinary og:images untouched; only the head fallback changed.
- MainLayout emits no ~og:image:width/height~ meta today, so no dimension tags needed updating.

** Validation
- ~npm run build~ (passes). Confirmed ~dist/about/index.html~ og:image → ~/og-card.png~ and ~dist/og-card.png~ present.
- Visual verification of the PNG: black ground, gold ring + red-carpet mark, three crisp correctly-spelled lines, no clipping.

* 2026-07-05 - Brand echo cleanup after favicon replacement

** Status
- Tightened the brand-color echoes after the Ghost Disc favicon replacement so browser/PWA surfaces and OG metadata match the new cream icon ground.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/site.webmanifest][public/site.webmanifest]]
  - Changed ~theme_color~ and ~background_color~ from white to ~#FAF6EF~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]]
  - Added ~<meta name="theme-color" content="#FAF6EF" />~ near the favicon and manifest links.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/data/image-metadata.json][src/data/image-metadata.json]]
  - Corrected the ~jaydixit-photos-logo.png~ metadata from ~2048x2048~ to the current ~1200x1200~ file.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/jaydixit-photos-logo.png][public/jaydixit-photos-logo.png]]
  - Losslessly optimized the 1200x1200 logo PNG in place.

* 2026-07-04 - Replace favicon with Ghost Disc mark

** Status
- Shipped a new production favicon: the "Ghost Disc" mark — a bright-gold hairline ring with the red-carpet runner fused to its inner edge, floating over a 45%-opacity cream backing disc.
- Chosen through a five-round contact-sheet process preserved in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/favicon-explorations/][docs/favicon-explorations/]] (red-carpet directions, ribbon/flash riffs, ring-and-field families, carpet-meets-ring refinements, then a Bright Hairline fine-tuning round; final pick was round-5 variant 16).

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/favicon.svg][public/favicon.svg]] — new canonical vector mark (32x32 viewBox, ring r12.6 stroke ~#C6A15B~ at 1.8, runner ~#B3272D~ clipped to r11.8, disc ~#FAF6EF~ at 45%).
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/favicon-16x16.png][public/favicon-16x16.png]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/favicon-32x32.png][public/favicon-32x32.png]] — regenerated from the SVG via rsvg-convert.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/favicon.ico][public/favicon.ico]] — rebuilt with 16/32/48 layers via ImageMagick.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/apple-touch-icon.png][public/apple-touch-icon.png]] — 180x180 on a solid full-bleed ~#FAF6EF~ ground (iOS composites icons on arbitrary backgrounds and applies its own mask, so no ghost transparency there).
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]] — added a ~rel="icon" type="image/svg+xml"~ link ahead of the ico so modern browsers prefer the vector.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/android-chrome-192x192.png][public/android-chrome-192x192.png]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/android-chrome-512x512.png][public/android-chrome-512x512.png]] — regenerated on the same solid cream ground (Android masks/composites manifest icons, so no transparency); filenames and sizes match what ~site.webmanifest~ expects, manifest unchanged.
- Previous favicon set archived to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/archive/favicons/2026-07-04-previous-favicon/][src/assets/archive/favicons/2026-07-04-previous-favicon/]] (ico, 16/32 PNGs, apple-touch, android-chrome 192/512), mirroring the logo-archive convention.
- Validation: ~npm run build~ passes (86 pages).

* 2026-07-04 - Match about-page essay copy to larger lead size

** Status
- Updated the About page so the later opening essay paragraphs match the larger font size used by the first two paragraphs.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about.astro][src/pages/about.astro]]
  - Changed ~.ed-essay p~ from ~1.3rem~ to ~1.5rem~ on desktop and from ~1.2rem~ to ~1.3rem~ on mobile, matching the ~.ed-lead p~ scale.

* 2026-07-04 - Prune stale site-wide improvement plan items

** Status
- Reviewed [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]] against the current repo state and removed items that are no longer actionable.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]]
  - Added a current-applicability summary separating still-current work from already-addressed first-pass items.
  - Removed completed homepage lead, eager image loading, red-carpet name search, before/after smoke coverage, and initial album population from the suggested implementation order.
  - Kept the still-relevant recommendations for editorial captions, typed before/after data, shared components, richer discovery indexes, and Wikimedia impact metadata.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-review-2026-07-03.org][docs/site-review-2026-07-03.org]]
  - Updated the stale site-review order so it no longer lists eager image loading, typography cleanup, homepage story block, red-carpet search, or initial albums as remaining work.

** Validation
- =git diff --check= passed.
- =pnpm lint= passed.
- =pnpm test= passed (21/21).
- =pnpm exec playwright test= passed (19/19).

* 2026-07-04 - Enact site-review feature items (hero, search, albums, eager images, typography)

** Status
- Enacted the remaining actionable items from [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-review-2026-07-03.org][docs/site-review-2026-07-03.org]] Tier 5 + eager-images, plus the typography unification. Executed via four parallel Opus subagents; verified on a local dev server. Packery/masonry left untouched by design.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/index.astro][src/pages/index.astro]]
  - New ~home-intro~ editorial header above the grid: ~<h1>~ "Jay Dixit — Red Carpet Photography for Wikipedia", one-sentence WikiPortraits lede, "See the Before & After →" accent link. Fixes the missing-h1 accessibility issue.
  - First 6 images in DOM order now emit ~loading="eager" fetchpriority="high"~ (handles stacked pairs correctly); rest stay lazy.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/index.astro][src/pages/red-carpet/index.astro]]
  - Client-side search input filtering person cards by name + bio (~data-search~ attribute, vanilla JS, no-matches message, accent focus ring, visually-hidden label).
  - First 4 card images eager + high fetchpriority.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/gallery/index.astro][src/pages/gallery/index.astro]]
  - First-row card images (first category) eager + high fetchpriority.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - First comparison pair (before + after) eager + high fetchpriority.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/content/albums/][src/content/albums/]]
  - Three curated albums replacing ~_placeholder.json~: ~tiff-2024~ (9 photos), ~sxsw-2024~ (6), ~sundance-film-festival~ (4). Photos referenced by Cloudinary publicId sourced from homepage-images.js; all 19 publicIds verified HTTP 200. Album pages needed no fixes.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tailwind.config.js][tailwind.config.js]]
  - ~font-sans~ now maps to Karla (was Inter, never loaded); added ~serif~ → Cormorant Garamond; deleted unused Playfair entry (zero ~font-playfair~ usage confirmed).
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/RecognitionSection.astro][src/components/RecognitionSection.astro]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/[person].astro][src/pages/red-carpet/[person].astro]]
  - Removed ~font-sans~ from headings so they inherit the global Cormorant Garamond serif; body ~font-sans~ kept (now correctly Karla).

** Validation
- ~pnpm lint~ passed.
- Dev server: all touched routes return 200 (/, /red-carpet/, /gallery/, /before-after, /albums/ and all three album slugs).
- Rendered HTML spot-checks: h1 + lede + link present on homepage; 6/4/1/2 eager+fetchpriority images on home / red-carpet / gallery (first category has one card) / before-after; search input, ~data-search~ on 21 cards, and no-matches node present on /red-carpet/; 3 albums listed with Cloudinary-served images.

** Follow-up fix: site-wide nav clearance
- The same-day nav-logo enlargement (44px → 88px) made the fixed navbar ~132px tall (desktop) / ~120px (mobile), taller than the top padding every page reserved, so page tops were clipped under the nav site-wide.
- Bumped clearance on all affected pages, then consolidated the values into a single token (next item).

** Follow-up refactor: single --nav-clearance token
- All per-page hardcoded nav-clearance values are replaced by one CSS variable, ~--nav-clearance~ (10.5rem desktop, 9.5rem at ≤640px), defined in a ~<style is:global>~ block in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/NavBar.astro][src/components/NavBar.astro]] right next to the nav sizing it must track. If the nav is resized again, change only that block.
- Consumers: homepage ~.portfolio-wrap~ (both style blocks), custom-CSS heroes via ~padding: var(--nav-clearance) …~ (about, before-after, red-carpet index, blog index, contact, and press including its mobile override), and Tailwind pages via the v4 class ~pt-(--nav-clearance)~ (albums index, popular, collections, gallery index, gallery folder, photo detail, red-carpet person).
- Now-redundant per-page mobile padding overrides were deleted (the variable's own media query handles mobile); sibling rules in those media blocks were preserved.
- Not converted (intentionally): blog posts (~pt-44 md:pt-56~ is deliberately more generous) and upload-to-commons (standalone page without the fixed nav).
- Verified: all live routes 200 on dev server with the token present in served HTML; ~npm run lint~ clean.

** Notes / follow-ups
- /albums/ index page renders correctly but is stylistically plain (doesn't yet use the editorial design system or cover thumbnails) — worth a design pass if albums go prominent.
- Prototype pages intentionally skipped in the font-sans sweep (slated for deletion per review Tier 4).
- @fontsource self-hosting deliberately deferred.

* 2026-07-04 - Enlarge nav logo lockup

** Status
- Increased the site nav logo and wordmark so the new generated mark reads clearly beside the site title.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/NavBar.astro][src/components/NavBar.astro]]
  - Doubled the displayed nav logo from ~44px~ to ~88px~ on desktop, with matching larger scrolled and mobile sizes.
  - Increased the ~Jay Dixit Photos~ wordmark from ~1.45rem~ to ~2.35rem~ on desktop, with smaller scrolled and mobile values to preserve fit.
  - Added a global ~--nav-clearance~ token for pages that need to clear the fixed nav.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/logo-nav.webp][public/logo-nav.webp]]
  - Regenerated the nav logo derivative at ~176x176~ so the enlarged display stays sharp.
- Fixed-nav top padding was increased across the homepage, editorial pages, gallery/photo pages, albums, collections, and popular routes so the larger nav lockup does not overlap page content.

* 2026-07-04 - Try new generated site logo

** Status
- Replaced the active site logo assets with Jay's selected generated logo while preserving the previous logo family in an archive folder.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/archive/logos/2026-07-04-previous-logo/][src/assets/archive/logos/2026-07-04-previous-logo/]]
  - Archived the previous nav, modal, social-preview, and original logo files before overwriting public derivatives.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/logos/logo-source-2026-07-04-chatgpt.png][src/assets/logos/logo-source-2026-07-04-chatgpt.png]]
  - Copied the selected source logo from [[file+emacs:/Users/jay/Downloads/ChatGPT Image Jul 4, 2026, 04_32_54 AM.png][/Users/jay/Downloads/ChatGPT Image Jul 4, 2026, 04_32_54 AM.png]].
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/logo-nav.webp][public/logo-nav.webp]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/logo-large.webp][public/logo-large.webp]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/logo.webp][public/logo.webp]], [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/jaydixit-photos-logo.webp][public/jaydixit-photos-logo.webp]], and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/jaydixit-photos-logo.png][public/jaydixit-photos-logo.png]]
  - Regenerated the public logo derivatives from the new source while preserving the existing URLs used by the NavBar, logo modal, and default social image metadata.

** Validation
- Confirmed the new header asset is ~112x112~ and ~4.3 KB~.
- Confirmed the new modal asset is ~512x512~ and visually inspected it.
- =git diff --check= passed.
- =pnpm lint= passed.
- =pnpm test= passed (21/21).
- =pnpm exec playwright test= passed (19/19).

* 2026-07-04 - Tighten navigation labels

** Status
- Shortened three primary nav labels while preserving their existing URLs.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/NavBar.astro][src/components/NavBar.astro]]
  - Renamed ~Why I Photograph~ to ~Why I Shoot~.
  - Renamed ~Red Carpet Photos~ to ~Red Carpet~.
  - Renamed ~The Story of WikiPortraits~ to ~Why WikiPortraits~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Updated the nav smoke test to assert the new labels and unchanged destinations.

* 2026-07-04 - Fix blog date timezone drift

** Status
- Fixed the one-day-early blog date display caused by formatting UTC-midnight frontmatter dates in the local timezone.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/index.astro][src/pages/blog/index.astro]]
  - Added UTC date formatting for blog index dates so ~publishDate: 2024-10-01~ renders as October 1, not September 30.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/[slug].astro][src/pages/blog/[slug].astro]]
  - Added the same UTC date formatting for blog detail pages.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/RelatedPosts.astro][src/components/RelatedPosts.astro]]
  - Added UTC date formatting for related essay cards while preserving the shorter month style.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Added assertions for UTC-correct blog index, blog detail, and related essay dates.

** Validation
- =pnpm exec prettier --write src/pages/blog/index.astro src/pages/blog/[slug].astro src/components/RelatedPosts.astro tests/e2e/portfolio.e2e.ts= passed.
- Targeted =pnpm exec eslint= passed for the changed files.
- =pnpm lint= passed.
- =pnpm test= passed (21/21).
- =pnpm exec playwright test= passed (19/19).

* 2026-07-04 - Restyle site chrome and replace public logo asset

** Status
- Restyled the shared site chrome so the navigation and footer now match the editorial page language.
- Replaced the 5.7 MB public ~logo.png~ route with right-sized WebP derivatives.
- Fixed the long-standing nav active-state, social-icon, duplicate-font, and logo-trigger accessibility issues.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/logo-nav.webp][public/logo-nav.webp]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/logo-large.webp][public/logo-large.webp]]
  - Generated compressed WebP logo assets from the old 2048px PNG.
  - Header logo now uses ~logo-nav.webp~; the logo modal now uses ~logo-large.webp~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/logo-original.png][src/assets/logo-original.png]]
  - Moved the original 5.7 MB PNG out of [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/public/][public/]] so ~/logo.png~ is no longer served.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/NavBar.astro][src/components/NavBar.astro]]
  - Kept the Alpine mobile-menu mechanics but changed the surface to ~--paper~ with a ~--hairline~ bottom border.
  - Updated the wordmark/menu typography to the editorial Newsreader/Instrument Sans system.
  - Made section links route-aware and kept the WikiPortraits story link exact-match only.
  - Replaced inline social SVG duplication with the shared social icon component.
  - Replaced the text external-arrow glyph and menu/close drawings with Lucide icons.
  - Kept the logo modal trigger as a real button with a clearer ~View logo full size~ label.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/SocialIcon.astro][src/components/SocialIcon.astro]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/site.config.mts][site.config.mts]]
  - Moved social rendering into one shared component for nav and footer.
  - Kept brand SVGs in the shared component because the installed ~@lucide/astro~ package does not ship Twitter/LinkedIn/Instagram brand icons; the shared styling matches the Lucide/editorial treatment and uses Lucide for the generic website icon.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/Footer.astro][src/components/Footer.astro]]
  - Replaced the old glassmorphism footer with a ~--paper-deep~ band, ~--hairline~ border, small ~--sans~ copyright line, and shared muted social icons.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/styles/global.css][src/styles/global.css]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]]
  - Removed the unused Dancing Script load and old ~#logo~ rule.
  - Added a global ~a:focus-visible~ / ~button:focus-visible~ outline using ~--accent~.

** Validation
- Browser-verified the chrome on =http://127.0.0.1:4323/=, =http://127.0.0.1:4323/before-after/=, =http://127.0.0.1:4323/red-carpet/=, and =http://127.0.0.1:4323/contact/=.
- Confirmed nav active state on ~/before-after/~, ~/red-carpet/~, ~/contact/~, and exact-match behavior on ~/blog/wikiportraits-story/~.
- Confirmed rendered pages use ~/logo-nav.webp~ and no rendered image references ~/logo.png~.
- Confirmed =curl http://127.0.0.1:4323/logo-nav.webp= returns ~4432~ bytes and =curl http://127.0.0.1:4323/logo.png= returns ~404~.
- Confirmed the footer renders with ~rgb(244, 241, 234)~ background, ~rgb(235, 230, 220)~ top border, small copyright copy, and aria-labeled social links.
- Noted that the homepage still has the pre-existing masonry horizontal overflow tracked in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-review-2026-07-03.org][docs/site-review-2026-07-03.org]]; this chrome pass did not change homepage layout.
- =pnpm build= passed.
- =pnpm lint= passed.
- =pnpm test= passed (21/21).
- =pnpm exec playwright test= passed (18/18).

** Commits
- ~f2c4885~ Replace 5.7MB logo.png with compressed webp derivatives
- ~c6ae95c~ Restyle NavBar in editorial language; fix active state, icons, a11y
- ~07c45ba~ Restyle Footer to match editorial design

* 2026-07-04 - Adopt Claude Design top navigation

** Status
- Adopted Jay's redesigned top navigation from [[file+emacs:/Users/jay/Downloads/NavBar.astro][/Users/jay/Downloads/NavBar.astro]].
- Kept the redesign aligned with the shared editorial design layer instead of reintroducing page/component-local font loading.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/NavBar.astro][src/components/NavBar.astro]]
  - Replaced the older Inter/Tailwind-styled nav with the editorial Newsreader/Instrument Sans version.
  - Removed the downloaded component's duplicate Google Fonts links so the nav relies on [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/styles/global.css][src/styles/global.css]].
  - Added route-aware active states, including descendant matching for sections like ~/red-carpet/[person]~.
  - Swapped the old emoji/text social markers for inline monochrome SVG icons.
  - Used nav-specific class names so the old global ~.nav-link~ component styles cannot leak into the redesigned header.
  - Made the logo preview trigger a real button with focus states.
  - Set the mobile drawer to an opaque paper panel after visual inspection showed the page title ghosting through the translucent first pass.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Added a direct smoke test for the redesigned nav's desktop active state, external link attributes, social links, and mobile drawer active state.

** Validation
- Browser-verified the redesigned nav at =http://127.0.0.1:4323/about/= and =http://127.0.0.1:4323/before-after/=.
- Playwright browser probe confirmed no desktop link wrapping, no horizontal overflow, and correct active states at 1440px desktop and 390px mobile widths.
- =pnpm exec prettier --write src/components/NavBar.astro tests/e2e/portfolio.e2e.ts= passed.
- Targeted =pnpm exec eslint src/components/NavBar.astro tests/e2e/portfolio.e2e.ts= passed.
- =pnpm test= passed (21/21).
- =pnpm lint= passed.
- =pnpm exec playwright test= passed (18/18).

* 2026-07-04 - Adopt Claude Design editorial redesigns

** Status
- Adopted the Claude Design editorial redesigns for the contact, blog index, red-carpet index, and about pages.
- Hoisted the shared editorial font and color language so the redesigned pages reuse one site-level layer rather than duplicating page-local font links and tokens.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/layouts/MainLayout.astro][src/layouts/MainLayout.astro]]
  - Added the shared Newsreader and Instrument Sans font link in the site head.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/styles/global.css][src/styles/global.css]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tailwind.config.js][tailwind.config.js]]
  - Added shared editorial tokens for ~--paper~, ~--paper-deep~, ~--ink~, ~--muted~, ~--hairline~, ~--accent~, ~--serif~, and ~--sans~.
  - Kept Tailwind's HSL color helpers working by moving the older muted/accent values to ~--muted-hsl~ and ~--accent-hsl~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Removed duplicated page-level font links and local editorial token definitions now supplied globally.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/contact.astro][src/pages/contact.astro]]
  - Adopted the editorial contact layout from [[file+emacs:/Users/jay/Downloads/uploads/contact.astro][/Users/jay/Downloads/uploads/contact.astro]] while preserving the Formspree endpoint and honeypot field.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/blog/index.astro][src/pages/blog/index.astro]]
  - Adopted the editorial journal layout from [[file+emacs:/Users/jay/Downloads/uploads/index-1f5d5db0.astro][/Users/jay/Downloads/uploads/index-1f5d5db0.astro]] while preserving blog collection, Cloudinary, and smart-quote logic.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/red-carpet/index.astro][src/pages/red-carpet/index.astro]]
  - Adopted the editorial portrait-archive layout from [[file+emacs:/Users/jay/Downloads/uploads/index.astro][/Users/jay/Downloads/uploads/index.astro]] while preserving generated people/photo data.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/about.astro][src/pages/about.astro]]
  - Adopted the editorial about layout from [[file+emacs:/Users/jay/Downloads/uploads/about.astro][/Users/jay/Downloads/uploads/about.astro]].
  - Corrected the inaccurate WikiPortraits card copy from "Founded initiative..." to a contributor phrasing.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Added direct smoke coverage for ~/contact/~, ~/blog/~, ~/red-carpet/~, and ~/about/~.
  - Added focused assertions for generated red-carpet cards, blog thumbnails, Formspree/honeypot preservation, about-page editorial sections, and broken-image checks.

** Validation
- =pnpm exec prettier --write= and targeted =pnpm exec eslint= passed on each redesigned page as it landed.
- Browser-verified the redesigned pages at =http://127.0.0.1:4323/contact/=, =http://127.0.0.1:4323/blog/=, =http://127.0.0.1:4323/red-carpet/=, and =http://127.0.0.1:4323/about/=.
- =pnpm test= passed (21/21).
- =pnpm lint= passed.
- =pnpm exec playwright test= passed (17/17).

* 2026-07-04 - Document Claude Design before-and-after replacement

** Status
- Backfilled the missing handoff entry for the Claude Design-generated ~/before-after/~ redesign that is now committed as ~cea24f1~.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Replaced the previous utility-grid/editorial hybrid with the Claude Design artifact from [[file+emacs:/Users/jay/Downloads/uploads/before-after.astro][/Users/jay/Downloads/uploads/before-after.astro]].
  - Preserved the real Astro route plumbing: ~MainLayout~, local image imports, Astro ~Image~ optimization, ~getImage~ lightbox hrefs, and ~PhotoSwipeLightbox~.
  - Introduced the page-scoped ~ba-*~ editorial system: warm paper palette, Newsreader/Instrument Sans typography, numbered case-study pairs, hairline captions, circled before-to-after arrows, pull-quote context section, and compact CTA.
  - Added per-image ~beforePos~ / ~afterPos~ crop tuning so portrait faces stay framed inside the new fixed-aspect comparison cards.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Updated the first-class ~/before-after/~ smoke test from the old ~comparison-*~ selectors to the new ~ba-*~ markup.
  - Kept coverage for comparison count, arrow count, source-caption presence, lazy image loading after scroll, and broken-image detection.

** Validation
- This entry is a docs-only backfill after the already-pushed redesign commit.
- =git diff --check= passed for the work-log update.

* 2026-07-03 - Add before-and-after transition arrows

** Status
- Added directional arrows between each before/after image pair so the comparison reads more explicitly as a transformation.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Inserted a neutral, decorative arrow between each Before and After figure.
  - Kept the arrow monochrome and compact so it supports the editorial case-study design without becoming a loud UI control.
  - Rotated the arrow downward on mobile, where the comparison images stack vertically.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Extended the ~/before-after/~ smoke test to assert one arrow per comparison card.

** Validation
- =pnpm exec prettier --check src/pages/before-after.astro tests/e2e/portfolio.e2e.ts= passed.
- =pnpm lint= passed.
- =pnpm build= passed.
- =PLAYWRIGHT_PORT=4330 pnpm exec playwright test -g "before-after page renders source captions" --workers=1= passed (1/1).
- =PLAYWRIGHT_PORT=4331 pnpm test:e2e= passed (9/9).

* 2026-07-03 - Add before-and-after e2e coverage and site improvement plan

** Status
- Added first-class Playwright coverage for the before-and-after page.
- Created a site-wide improvement implementation plan for editorial design, homepage identity, technical consolidation, discovery, and Wikimedia impact metadata.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Added a direct ~/before-after/~ smoke test.
  - Verifies 5 comparison cards, 10 source captions, specific Sydney/Jay credit text, image loading after scroll, and no completed broken images.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/docs/site-wide-improvements-implementation-plan.org][docs/site-wide-improvements-implementation-plan.org]]
  - Documents approach, implementation steps, acceptance criteria, suggested order, and verification checklist for the site-wide improvement tracks.

** Validation
- =pnpm exec prettier --check tests/e2e/portfolio.e2e.ts= passed.
- =pnpm lint= passed.
- =pnpm build= passed.
- =PLAYWRIGHT_PORT=4329 pnpm exec playwright test -g "before-after page renders source captions" --workers=1= passed (1/1).
- =pnpm test:e2e= passed (9/9).

* 2026-07-03 - Refine before-and-after page editorial presentation

** Status
- Made the before-and-after comparisons feel more editorial and less like a utility grid.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Replaced colored overlay labels with consistent monochrome Before/After label rows.
  - Added source/credit captions for each before and after image.
  - Tightened comparison spacing and converted repeated inline utility styling into dedicated comparison classes.
  - Preserved the existing explanatory page prose and PhotoSwipe behavior.

** Validation
- =pnpm exec prettier --check src/pages/before-after.astro= passed.
- =pnpm lint= passed.
- =pnpm build= passed.
- Browser-verified =http://127.0.0.1:4328/before-after/= at desktop and mobile widths.
- Confirmed 5 comparison cards, 10 source captions, no broken loaded images after scroll-through, no mobile horizontal overflow, and PhotoSwipe opens from the comparison grid.

* 2026-07-03 - Add Sydney Sweeney before/after comparison

** Status
- Added a Sydney Sweeney before/after pair to the public before-and-after page.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/before-after.astro][src/pages/before-after.astro]]
  - Imported the new Sydney Sweeney before/after image assets.
  - Added a comparison entry linking to the Sydney Sweeney Wikipedia article.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/images/wikiportraits/sydney-sweeney-before-2019.jpg][sydney-sweeney-before-2019.jpg]]
  - Copied from [[file+emacs:/Users/jay/Downloads/Sydney_Sweeney_2019_by_Glenn_Francis.jpg][Downloads source image]].
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/assets/images/wikiportraits/sydney-sweeney_tiff_2024.jpg][sydney-sweeney_tiff_2024.jpg]]
  - Copied from [[file+emacs:/Users/jay/Downloads/Sydney_Sweeney_at_the_2024_Toronto_International_Film_Festival_01_(3x4_cropped_and_rotated).jpg][Downloads source image]].

** Validation
- =pnpm exec prettier --check src/pages/before-after.astro= passed.
- =pnpm lint= passed.
- =pnpm build= passed.
- Verified the built ~/before-after/~ page includes the Sydney Sweeney section and both optimized image assets.
- Verified in local Astro preview at =http://127.0.0.1:4327/before-after/= that the Sydney Sweeney images load and PhotoSwipe opens/closes.

* 2026-06-24 - Align Vercel deploy settings with pnpm

** Status
- Updated both repo-local and remote Vercel build settings so the next Git deployment uses pnpm instead of stale npm commands.
- Added Playwright artifact directories to the ESLint flat-config ignore list after a parallel lint/e2e run exposed a transient ~test-results~ scan failure.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/vercel.json][vercel.json]]
  - Changed ~buildCommand~ to ~pnpm build~.
  - Changed ~installCommand~ to ~pnpm install~.
  - Changed ~devCommand~ to ~pnpm run dev~.
- Remote Vercel project ~photos-jaydixit~
  - Updated Framework Settings to ~Build Command: pnpm build~, ~Install Command: pnpm install~, and ~Development Command: pnpm run dev~.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/eslint.config.js][eslint.config.js]]
  - Ignored ~playwright-report/**~ and ~test-results/**~ so generated e2e artifacts are never lint targets.

** Validation
- =vercel project inspect photos-jaydixit= confirmed remote settings are now ~pnpm build~ and ~pnpm install~.
- =pnpm exec prettier --check vercel.json= passed.
- =pnpm build= passed.
- =pnpm test= passed (21/21).
- =pnpm test:e2e= passed (8/8).

* 2026-06-24 - Add Playwright portfolio smoke tests

** Status
- Added a Playwright e2e layer for production-like portfolio verification through Astro preview.
- Covered the visual smoke paths that matter after dependency and image-pipeline changes: homepage, gallery index, Sundance gallery, Conan photo detail, Nobel blog post, PhotoSwipe open/close, and the Vanessa Kirby related-essay thumbnail.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/package.json][package.json]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/pnpm-lock.yaml][pnpm-lock.yaml]]
  - Added ~@playwright/test~ and a ~test:e2e~ script.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/playwright.config.ts][playwright.config.ts]]
  - Launches ~pnpm build && pnpm exec astro preview --host 127.0.0.1 --port 4326~ unless ~PLAYWRIGHT_BASE_URL~ is provided.
  - Uses Chromium, list + HTML reporters, screenshots on failure, and traces on first retry.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tests/e2e/portfolio.e2e.ts][tests/e2e/portfolio.e2e.ts]]
  - Checks expected titles/headings, minimum image/lightbox counts, completed broken images, PhotoSwipe first-click/Escape behavior, and the corrected Vanessa Kirby thumbnail source.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/.gitignore][.gitignore]]
  - Ignores Playwright report and test-result artifacts.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/tsconfig.json][tsconfig.json]]
  - Includes the Playwright config and e2e test files so project-backed ESLint can parse them.

** Validation
- =pnpm exec playwright install chromium= installed the local browser cache under ~/Users/jay/Library/Caches/ms-playwright/~.
- =PLAYWRIGHT_BASE_URL=http://127.0.0.1:4325 pnpm exec playwright test -g "PhotoSwipe" --workers=1= passed (2/2).
- =pnpm test:e2e= passed (8/8).

* 2026-06-24 - Post-pnpm visual spot-check and stale Cloudinary ID fix

** Status
- Ran a local browser visual pass against the pnpm/Astro 6 site at =http://127.0.0.1:4325/= after the package-manager and Astro-integration commits.
- Found and fixed a stale Cloudinary public ID in the sample blog post that broke the related-essay thumbnail on the Nobel portrait session page.
- Made the ESLint flat-config package import explicit for pnpm strict dependency resolution.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/package.json][package.json]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/pnpm-lock.yaml][pnpm-lock.yaml]]
  - Added direct dev dependency ~@eslint/js~ because ~eslint.config.js~ imports it directly and pnpm does not expose undeclared transitive dependencies.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/content/blog/hello-world.mdx][src/content/blog/hello-world.mdx]]
  - Changed the Vanessa Kirby image public ID from the missing ~events/red-carpet/tiff/vanessa-kirby_tiff_2024~ asset to the existing ~highlights/vanessa-kirby_tiff_2024~ asset in both frontmatter and the inline image URL.

** Validation
- Browser-checked =/=, =/gallery/=, =/gallery/red-carpet/sundance/=, =/gallery/photo/red-carpet/sundance/conan-obrien_sundance_2024/=, and =/blog/nobel-portrait-session/= in system Chrome via Playwright.
- Verified homepage and Sundance gallery ~PhotoSwipe~ open on first click and close with ~Escape~.
- Confirmed the stale Cloudinary URL returned =404= before the fix.
- Verified the fixed related-essay thumbnail loads after scrolling the section into view.
- =pnpm build= passed.
- =pnpm test= passed (21/21).
- =pnpm lint= passed after declaring ~@eslint/js~ directly.
- =pnpm exec prettier --check package.json src/content/blog/hello-world.mdx= passed.

* 2026-05-26 - Make homepage PhotoSwipe open on the first click and close with Escape

** Status
- Fixed the Packery homepage lightbox path so the first click or keyboard activation initializes ~PhotoSwipe~ and opens the intended slide instead of only priming the lazy loader.

** Shipped
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/scripts/packeryGallery.ts][src/scripts/packeryGallery.ts]]
  - Replaced the lazy ~pointerdown~ initializer with a first-activation handler that prevents navigation, loads ~PhotoSwipe~, and calls ~loadAndOpen~ for the clicked slide.
  - Kept modifier-click behavior intact so browser open-in-new-tab shortcuts still work.
  - Made ~escKey~ explicit in the Packery lightbox options.
- [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/PhotoSwipeLightbox.astro][src/components/PhotoSwipeLightbox.astro]]
  - Made ~escKey~ explicit in the shared lightbox defaults.

** Validation
- =npm run build= passed.
- =npm run lint= passed.
- Verified in a clean Chrome DevTools Protocol session against =http://127.0.0.1:4322/= that the first homepage photo click opens ~PhotoSwipe~ and ~Escape~ closes it.
- Verified =http://127.0.0.1:4322/before-after/= still opens ~PhotoSwipe~ and closes with ~Escape~.

* 2026-04-27 - Standardize live lightbox surfaces on bundled PhotoSwipe and improve cropped/responsive metadata

** Status
- Converted the remaining live gallery surfaces onto bundled PhotoSwipe patterns and tightened the metadata we give the lightbox about cropped thumbnails and capped responsive sources.

** Shipped
- Added a shared local initializer at ~src/components/PhotoSwipeLightbox.astro~ so static gallery surfaces can use one bundled PhotoSwipe path instead of CDN snippets or GLightbox.
- ~src/components/Gallery.astro~
  - Removed CDN PhotoSwipe assets.
  - Switched to the local lightbox wrapper.
  - Added ~=data-pswp-srcset=~ so album lightboxes can load capped responsive Cloudinary images.
- ~src/components/PhotoGrid.astro~ and ~src/scripts/photo-grid.ts~
  - Replaced the GLightbox path with PhotoSwipe for collection/featured grids.
  - Marked those cover-cropped previews with ~=data-cropped="true"=.
- ~src/pages/before-after.astro~
  - Moved the comparison page onto the shared local PhotoSwipe wrapper.
  - Marked the cover-cropped previews with ~=data-cropped="true"=.
- ~src/pages/gallery/[...folder].astro~
  - Stopped pointing the folder-gallery lightbox at raw imported metadata URLs.
  - Generated a served lightbox target with ~=getImage()=~ and marked the cover-cropped thumbnails appropriately.
- ~src/pages/index.astro~ and ~src/scripts/packeryGallery.ts~
  - Capped homepage lightbox images to a saner maximum width.
  - Added ~=data-pswp-srcset=~, ~=data-cropped=~, and lightbox dimensions that match the largest delivered slide instead of the uncapped original.
- ~src/layouts/MainLayout.astro~
  - Removed the now-unused live GLightbox CSS import.
- ~docs/codebase-wisdom.org~
  - Added the durable note about cropped thumbnails and capped responsive lightbox metadata.

** Validation
- Pending final =npm run build= after the full cleanup set

* 2026-04-27 - Fix imported-image lightbox hrefs so Astro dev stops opening dead /@fs paths

** Status
- Fixed the remaining places where PhotoSwipe or GLightbox was opening imported Astro image metadata ~=.src=~ directly as the lightbox target.

** Shipped
- ~src/pages/before-after.astro~
  - Switched before/after lightbox hrefs from ~=comparison.beforeImage.src=~ and ~=comparison.afterImage.src=~ to served URLs generated with ~=getImage()=~.
- ~src/components/PhotoGrid.astro~
  - Switched GLightbox hrefs from ~=image.src.src=~ to served URLs generated with ~=getImage()=~.
- ~docs/codebase-wisdom.org~
  - Added a debugging note explaining the Astro dev trap where the thumbnail can render from ~=srcset=~ while the lightbox opens a dead =/@fs/...= href.

** Validation
- =cd /Users/jay/Library/CloudStorage/Dropbox/github/photos-jaydixit && npm run build= passed
- The code paths now generate lightbox hrefs with =getImage()=, so imported Astro assets no longer rely on raw metadata =.src= as the lightbox target.

* 2026-02-19 - Fix Nav Item Wrapping

** Status
- Fixed bizarre wrapping behavior in navigation where menu items and wordmark were breaking onto multiple lines.

** Shipped
- ~src/components/NavBar.astro~
  - Added ~flex-shrink-0~ to the brand container to prevent compression.
  - Combined "Jay Dixit" and "Photos" into a single span with ~whitespace-nowrap~.
  - Added ~flex-nowrap~ to the desktop menu container.
  - Added ~whitespace-nowrap~ to each menu link.
  - Added ~flex-shrink-0~ to the social links container.

** Validation
- Visual inspection confirms nav items no longer wrap.

* 2026-02-18 - About Positioning + Footer Link/Icon Polish

** Status
- Reordered the About conversion flow and polished nav/footer behavior to align with the latest messaging direction.

** Shipped
- ~src/pages/about.astro~
  - Moved the pull quote and "Most people land here..." CTA block above the ~Writing & Teaching~ (~Academic & Editorial~) section.
  - Updated both ~Get In Touch~ buttons to link to ~/contact~.
  - Changed Psychology Today card copy to: ~Senior editor for four years...~
  - Updated body copy line to: ~But it's a completely different part of my brain.~
- ~src/components/NavBar.astro~
  - Renamed nav item ~About~ to ~Why I Photograph~ (link remains ~/about~).
- ~site.config.mts~
  - Replaced the footer GitHub repo URL with ~https://jaydixit.com~.
  - Updated the footer website icon to Lucide ~PawPrint~.
- Cleanup
  - Removed accidental empty ~src/content/blog/wikiportraits-story.md~ so only the real ~wikiportraits-story.mdx~ entry remains.

** Validation
- ~npm run build~ passed.

* 2026-02-18 - About Funnel Rewrite + Nav Cleanup + WikiPortraits Story Refresh

** Status
- Reworked About page messaging to better convert photography visitors into writing/AI workshop leads, simplified nav, and replaced the WikiPortraits story draft.

** Shipped
- About route restored and re-linked
  - Restored ~src/pages/about.astro~ after temporary deletion.
  - Re-added ~About~ in ~src/components/NavBar.astro~.
- Navigation typography and cleanup
  - Switched nav + wordmark to ~Inter~.
  - Reduced wordmark weight from extra-heavy to ~font-bold~.
  - Increased nav item size for desktop/mobile legibility.
  - Removed ~Most Liked~ nav item.
- About page content updates
  - Replaced opening narrative with first-person funnel-oriented copy.
  - Updated photography subtitle to emphasize red carpet/Wikipedia context.
  - Removed the Writing & Teaching pull quote and tightened card copy:
    - Yale card now references ~"The Art of Storytelling"~ and distinctive teaching angle.
    - Psychology Today card now states role and scope directly.
  - Updated pullquote to:
    - ~"The feeling I'm chasing is that moment when I show someone a photo I just took and they say, 'Oh my God, I love this picture of me, you really captured who I am!'"~
  - Rewrote CTA copy to explicitly bridge photo discovery -> AI workshop/writing/consulting inquiry.
- Blog article refresh
  - Replaced ~src/content/blog/wikiportraits-story.mdx~ body with the new longform draft.
  - Normalized links to Markdown format for MDX rendering.

** Validation
- ~npm run build~ passed after each major update.

* 2026-02-18 - Supabase Likes + Popular Page + PhotoSwipe Ratio Fix

** Status
- Shipped Supabase-backed likes, a new Most Liked page, and fixed distorted PhotoSwipe rendering caused by incorrect dimension metadata.

** Shipped
- Likes integration
  - Added Supabase client dependency (~@supabase/supabase-js~).
  - Reworked ~src/components/PhotoLikeButton.astro~ to read/write likes from ~photo_user_likes~ using ~photo_id~ + ~visitor_id~.
  - Preserved localStorage fallback when Supabase env/policies are unavailable.
- New Most Liked route
  - Added ~src/pages/popular.astro~.
  - Aggregates likes from Supabase and sorts photos by like count.
  - Added nav link in ~src/components/NavBar.astro~ to ~/popular~.
- Gallery and photo detail wiring
  - Added like buttons in ~src/pages/gallery/[...folder].astro~.
  - Added like button in ~src/pages/gallery/photo/[...photo].astro~.
- PhotoSwipe distortion fix
  - Root cause: PhotoSwipe was using cropped masonry thumbnail dimensions instead of true source dimensions.
  - Added ~data-pswp-width~ / ~data-pswp-height~ and natural dimension attrs in ~src/pages/gallery/[...folder].astro~.
  - Updated ~src/scripts/packeryGallery.ts~ dimension resolution order to prefer full-source metadata.
- Documentation
  - Added gotcha entry in ~docs/codebase-wisdom.org~ to prevent regressions.

** Supabase Setup Notes
- Required public env vars:
  - ~PUBLIC_SUPABASE_URL~
  - ~PUBLIC_SUPABASE_ANON_KEY~
- Required RLS behavior on ~photo_user_likes~:
  - public read
  - public insert (non-empty ~photo_id~ and ~visitor_id~)
  - public delete

** Validation
- ~npm run build~ passed.
- ~npm run test -- --run~ passed (21/21).
- ~npm run lint~ has pre-existing unrelated failures in:
  - ~src/pages/index.astro~
  - ~src/scripts/packeryGallery.ts~
  - ~src/scripts/photo-grid.ts~

** Commits
- ~725fe5d~ Add Supabase likes and popular photos page
- ~8057a5d~ Update Astro dependencies and test gallery fixture order
- ~4592032~ Fix PhotoSwipe aspect ratio in gallery lightbox

* 2026-02-18 - EXIF Display + Formspree Contact

** Status
- Added EXIF display on per-photo pages and launched a dedicated contact page with Formspree.

** Shipped
- EXIF display on photo detail pages in ~src/pages/gallery/photo/[...photo].astro~
  - Reads EXIF at build time with ~exifr~.
  - Shows Camera, Lens, Aperture, Shutter, ISO, Focal Length, and Capture Date when available.
- Contact page in ~src/pages/contact.astro~
  - Form posts to ~https://formspree.io/f/xeelzjpn~.
  - Added hidden honeypot field and direct email fallback text.
- Navigation update in ~src/components/NavBar.astro~
  - Contact menu item now points to ~/contact~ instead of ~mailto:~.

** Validation
- ~npm run build~ passed.
- ~npx eslint src/pages/contact.astro src/components/NavBar.astro~ passed.

** Commits
- ~2e62933~ Display EXIF metadata on per-photo pages
- ~b0142d2~ Add Formspree contact page and nav link

* 2026-02-18 - SEO/Sharing Rollout + Canonical Per-Photo Pages

** Status
- Implemented approved roadmap items for SEO, social sharing, sitemap, analytics wiring, and per-photo pages.

** Shipped
- Site-wide metadata and social cards in ~src/layouts/MainLayout.astro~
  - Canonical, OpenGraph, Twitter cards, RSS discovery link, optional GA bootstrap.
- Structured data
  - WebSite (site-wide), Blog/BlogPosting, ImageGallery, Photograph.
- Social sharing
  - New reusable ~src/components/SocialShare.astro~.
- Sitemap
  - Added ~@astrojs/sitemap~ and enabled it in ~astro.config.mts~.
- Per-photo pages
  - New route: ~src/pages/gallery/photo/[...photo].astro~.
  - Route shape: ~/gallery/photo/<folder>/<photo-slug>~.
  - Added prev/next, social share, and per-photo Giscus thread.
- Canonicalized photo URLs across formats
  - Deduped .jpg/.webp variants to one slug URL per photo subject.
  - Gallery “Details” links point to canonical slug URLs.

** Validation
- ~npm run build~ passed.
- ~npm run test -- --run~ passed (21/21 tests).

** Commits
- ~e3bf67f~ Implement SEO, sharing, analytics, and sitemap features
- ~5f34cd3~ Add per-photo gallery pages with navigation and comments
- ~db8dba9~ Canonicalize per-photo URLs across image formats

* 2025-10-21 - KNOWN GOOD STATE ✅ (Production-Ready Baseline)
** Status: ✅✅✅ STABLE - VERIFIED WORKING - USE THIS AS REFERENCE
** Tag: stable-known-good
** Commit: ceb58ab

This is THE canonical known good state. Masonry is confirmed working perfectly.
All design enhancements are applied and stable. Use this as the baseline for
any future experiments or changes.

** What's Working
- Masonry grid layout fully functional (Packery with 8px gaps)
- Edge-to-edge layout (no container padding)
- Square corners on images (no border-radius)
- Captions displaying correctly as overlays on hover

** Design Enhancements Applied
*** Color Palette
- Warmer background: #fafafa (neutral-50) instead of pure white
- Neutral color scale: neutral-50 through neutral-900
- Better foreground colors: deeper, richer blacks

*** Shadows & Depth
- Refined shadow system: shadow-sm-refined through shadow-xl-refined
- Layered, subtle shadows with reduced opacity
- Applied to nav, search inputs, interactive elements

*** Interaction Refinements
- Hover overlay: Gradient (0% → 30% → 70%) instead of solid
- Zoom effect: Gentler scale(1.03) instead of scale(1.07)
- Transitions: Smoother 0.6s cubic-bezier easing
- Caption typography: clamp() fluid sizing, lighter weight (500)

*** UI Components
- Navigation: Enhanced glassmorphism (backdrop-blur-xl)
- Search input: shadcn-style rounded-xl with better focus states
- Tag pills: Black active state (neutral-900) instead of blue
- Footer: Subtle background with backdrop-blur
- Social icons: Rounded hover backgrounds

*** Typography
- Improved hierarchy with proper tracking-tight
- Better line-height and letter-spacing
- Fluid typography using clamp()

** Spacing (Reverted to Original)
- Masonry gap: 8px (tight, original spacing)
- Container: Edge-to-edge (no side padding)
- Images: Square corners (no border-radius)

** Files Modified
- src/styles/global.css
- src/pages/index.astro
- src/pages/gallery/[...folder].astro
- src/components/NavBar.astro
- src/components/Footer.astro
- src/components/SocialIcon.astro

** Reference
See docs/feature-ideas/design-enhancements-2025-10.org for full proposal.

* 2025-10-12 - Failed Attempt at Dynamic Aspect Ratios
** Goal
- Refactor the homepage gallery to use dynamic, natural aspect ratios for all images, enabling a true edge-to-edge layout.

** Changes Made
- A script was created to generate a metadata file (`image-metadata.json`) containing the true dimensions of all images.
- The homepage was modified to use this metadata to drive the layout, replacing the old fixed-height CSS with a dynamic aspect-ratio system.
- This involved passing the real image dimensions to the Astro `<Image>` component and removing several layers of conflicting CSS.

** Outcome: FAILURE
- The refactoring has resulted in a broken layout.
- Captions, intended as overlays, are now visible by default and rendered below their corresponding image.
- This incorrectly increases the height of each masonry item, creating large vertical gaps in the Packery grid and making the layout unusable.

** Analysis
- The root cause is a complex and persistent CSS layout issue. Despite multiple attempts, the caption overlay (`.caption-overlay`) is not being correctly positioned over the image.
- It is behaving as if it is part of the normal document flow rather than being absolutely positioned relative to its container.
- The interaction between the Astro `<Image>` component (which generates its own wrapper elements) and the parent `<a>` tag (which must act as the positioning context) is proving difficult to resolve.
- Further attempts to fix this with small CSS changes have failed. A more fundamental solution is required.
- *Update*: A subsequent attempt to fix the overlay using a modern CSS Grid approach (`display: grid` and `grid-area`) also failed, with no change in the broken behavior. This indicates the issue is not a simple positioning context problem.

* 2025-10-05 - Packery Bundle Regression
** Symptoms
- Vercel build succeeded, but production requested ~/src/scripts/packeryGallery.ts~ directly and hit a 404, leaving the gallery empty.
- Browser console showed ~GET https://photos-jaydixit.vercel.app/src/scripts/packeryGallery.ts net::ERR_ABORTED 404 (Not Found)~ on both the homepage and folder routes.
** Fix
- Replaced inline ~<script type="module">~ imports with hoisted scripts so Astro includes ~src/scripts/packeryGallery.ts~ in the build output.
- Updated ~src/pages/index.astro~ and ~src/pages/gallery/[...folder].astro~ to ~import initPackeryGallery from '../scripts/packeryGallery.ts'~ and ~../../scripts/packeryGallery.ts~ respectively.
- Confirmed ~npm run build~ now emits ~dist/_astro/packeryGallery.*.js~, and masonry renders again in production.
** Notes
- Avoid ~Astro.resolve~ for client helpers; use hoisted scripts or frontmatter imports so the bundler owns the dependency graph.
- Keep an eye on future refactors that move shared scripts—mixed absolute and relative imports can mask bundling gaps during dev.
- Follow-up: redeploy to Vercel after each bundling-related change to confirm the CDN serves the hydrated assets.

* 2025-10-05 - Edge-to-Edge Attempts Retrospective
** Goal
- Make the homepage masonry appear full-bleed without breaking the stable Packery layout (four 320px columns + gutters)
** Attempts (All Reverted)
- *Clamp padding on `.portfolio-wrap`* (e.g., `padding: 120px clamp(16px, 4vw, 40px)`): kept four columns centered but always left ~24px white on the right at large viewports because Packery still anchored columns to 320px widths.
- *Enable `fitWidth` + flex centering*: Packery shrank the grid to a single column, producing massive black gutters; unusable on desktop.
- *Pseudo-element curtains on `.gallery-stage`*: Added black panels with `::before/::after`, but a white sliver persisted (column width still fixed). Tweaking widths shifted the white space around without achieving true edge-to-edge.
- *Dynamic padding-inline stage* (`padding-inline: max(0, (100vw - 1304px) / 2)`): created mismatched white/black margins (e.g., 350px white right gutter + 212px black) since Packery columns remained 320px wide.
- *Black background on `.portfolio-wrap`* with white masonry inset: magnified the visible white border (372px) because the grid width never changed.
** Takeaway
- Packery’s fixed column widths + hard-coded heights are the blocking constraint. Without recomputing heights from real image aspect ratios, any attempt to widen columns or stretch the container will crop heads or deform the layout.
- Next viable path: precompute aspect ratios (e.g., via `sharp`) and use them to set per-item heights before Packery initializes; once height/width pairs reflect actual assets, we can revisit edge-to-edge or add an extra column.
** Recommendation
- Prototype aspect-aware sizing in a feature branch, validate against live data, then update documentation if the approach succeeds.

* 2025-10-05 - Homepage Framing Tweaks
** Motivation
- With Packery widths restored, the homepage still felt slightly offset compared to the nav + footer chrome.
- Goal: add symmetric gutters without shrinking the masonry columns back down or reintroducing the head-cropping issue.
** Changes
- Wrapped the gallery in a ~clamp(12px, 3vw, 28px)~ horizontal padding so large screens see just a sliver of whitespace while tablets get comfortable breathing room.
- Left column widths untouched (320px/648px) so Packery layouts and saved admin order remain 1:1 with the historical baseline.
- Confirmed ~npm run build~ after the tweak.
** Notes
- The clamp values live in ~src/pages/index.astro:432~; drop the minimum below ~12px~ if you truly want edge-to-edge.
- Any future responsive experiments should tweak this padding first before touching Packery column math.

* 2025-10-03 - Vercel Deployment & Mobile Optimization

** Vercel Configuration
*** Initial Setup
- Configured project for Vercel static deployment
- Fixed build issues:
  - Changed API endpoint =save-order.ts= from =prerender: false= to =prerender: true=
  - Removed empty placeholder image files that broke build
  - Resolved conflicting gallery routes (=[folder].astro= vs =[...folder].astro=)
  - Fixed =getStaticPaths= to return string paths instead of arrays for catch-all routes
  - Temporarily disabled =wikiportraits-story.mdx= (image import issue)

*** Git Integration
- Connected Vercel project to GitHub repository =incandescentman/photos-jaydixit=
- Enabled automatic deployments on push to =main= branch
- Added custom domain =photos.jaydixit.com= (DNS configuration pending)

** Mobile Display Optimization
*** Problem
Mobile view had significant issues:
- Photos were cropped (=object-fit: cover=)
- Large white gaps between photos
- Fixed heights didn't respect image aspect ratios

*** Solution
Changed mobile layout from Packery masonry to flexbox:
- Disabled Packery absolute positioning on mobile (<480px)
- Switched to =display: flex= with =flex-direction: column=
- Set images to =object-fit: contain= with =height: auto=
- Result: One photo per row, full width, no cropping, no gaps

** Performance Optimization
*** Problem
Initial page load was very slow (5-10 seconds of blank screen):
- =imagesLoaded()= waited for ALL images to fully download
- Only then did photos fade in with staggered animation
- Poor perceived performance

*** Solution - Progressive Image Loading
Implemented progressive reveal strategy:
- Show images as soon as each one loads (not after all load)
- Use =imagesLoaded().on('progress')= to trigger individual animations
- Reduced stagger timing from 60ms to 40ms
- Layout updates incrementally with =pckry.shiftLayout()=
- Result: First images visible in ~1 second instead of 5-10 seconds

*** Technical Implementation
#+begin_src javascript
// Before: Wait for all images, then animate
imagesLoaded(container, function() {
  allItems.forEach((item, i) => {
    setTimeout(() => item.classList.add('animated-in'), i * 60);
  });
});

// After: Animate each image as it loads
imagesLoaded(container).on('progress', function(instance, image) {
  const item = image.img.closest('.masonry-item');
  setTimeout(() => item.classList.add('animated-in'), loadedCount * 40);
  pckry.shiftLayout();
});
#+end_src

** Key Decisions
1. *Static deployment over SSR* - Chose Vercel static output for simplicity and performance
2. *Flexbox on mobile* - Abandoned Packery masonry on mobile in favor of natural document flow
3. *Progressive loading* - Prioritized perceived performance over perfect layout timing
4. *Object-fit contain* - Full image display over cropped composition on mobile

** Files Changed
- =src/pages/index.astro= - Mobile CSS and progressive loading logic
- =src/pages/api/save-order.ts= - Changed to =prerender: true=
- =src/pages/gallery/[...folder].astro= - Fixed getStaticPaths
- =vercel.json= - Deployment configuration (already existed)
- =.vercel/= - Auto-generated deployment metadata

** Deployment URLs
- *Production*: https://photos-jaydixit-79enk9mo3-incandescentmans-projects.vercel.app
- *Custom Domain*: https://photos.jaydixit.com (pending DNS configuration)
- *Vercel Dashboard*: https://vercel.com/incandescentmans-projects/photos-jaydixit

* 2025-10-03 - Masonry Implementation Review

** Overview
- Home page masonry lives in ~src/pages/index.astro~ with ~Packery~ orchestrating absolute-positioned ~.masonry-item~ articles.
- Each item carries a variant class (~regular~/~wide~/~wide_tall~) and a ~.grid-sizer~ of 320px anchors Packery's column math.
- Mobile (<480px) forces a flex-column fallback with ~object-fit: contain~ so small screens bypass Packery entirely.
- Catch-all gallery page (~src/pages/gallery/[...folder].astro~) implements a CSS-grid masonry variant with ~grid-auto-rows: 10px~ and span-based sizing.

** Index Page Details (~src/pages/index.astro~)
- Layout CSS assigns fixed heights (~480px~ regular, ~968px~ wide_tall) using ~!important~, so Packery crops via ~object-fit: cover~ rather than respecting natural ratios.
- The script initializes ~Packery~ directly (~initLayout: false~, ~stagger: 30~) and drives progressive reveal with ~imagesLoaded().on('progress')~ plus ~pckry.shiftLayout()~.
- Admin mode injects a toolbar, toggles edit affordances, and wires ~Draggabilly~ handles for drag-to-resequence with autosave into ~localStorage~ and POSTs to ~/api/save-order~.
- Observed quirks:
  - Inline helpers test ~window.pckry~ but never assign ~window.pckry = pckry~; use the closure-bound instance instead.
  - ~applySize(newSize)~ never runs because ~sizeInput~ was removed; replace the guard with the segmented control (~sizeGroup~) so variant classes update when saving.
  - Fixed-height CSS conflicts with any future height normalization attempts (inline JS cannot override ~!important~ styles).

** Gallery Catch-all (~src/pages/gallery/[...folder].astro~)
- Uses CSS grid for pseudo-masonry: variant classes (~small~/~medium~/~tall~/~wide~…) come from array index patterns, not actual asset metadata.
- Search + tag filters hide items by toggling a ~hidden~ class; likes persist in ~localStorage~ (Supabase TODO noted).
- GLightbox powers detail view; no Packery/Isotope present here.

** Shared Masonry Script (~src/scripts/masonry.ts~)
- Provides an ~initMasonry~ helper mirroring Salient's Isotope+Packery combo with elastic column tiers, gutter fudge, staggered reveals, and filter hooks.
- Currently unused on the main page; consider consolidating bespoke ~index.astro~ logic into this module for reuse across galleries.

** Follow-ups / Questions
- Fix admin size editor so saved variants immediately reflow (~applySize~ guard) and consider exposing inline width/height adjustments without ~!important~ locks.
- Decide whether Packery should remain page-specific or whether to adopt the shared ~initMasonry~ helper for consistency.
- Evaluate if gallery CSS-grid variant should derive spans from actual image dimensions or YAML metadata for predictable layout.
- When enabling admin mode, ensure PhotoSwipe links remain disabled during drags (current pointer-event toggle works, but confirm after fixes).

** Verification Checklist
- ~npm run dev~ → visit the home page masonry and inspect Packery column widths, staggered reveal, and admin drag mode.
- Resize below 480px to confirm flex fallback removes absolute positioning and preserves full-image display.
- Visit a folder route (~/gallery/<slug>~) to confirm CSS-grid masonry, tag filters, and lightbox behaviour.
- Run ~npm run build~ periodically to catch any Packery/Draggabilly SSR warnings before deployment.

* 2025-10-03 - Packery Consolidation & Mobile Fixes

** Shared Helper
- Extracted bespoke homepage logic into ~src/scripts/packeryGallery.ts~, exposing ~initPackeryGallery~ for reuse across pages.
- Module handles mobile fallback, order restoration, admin toolbar + popover editing, Draggabilly binding, and PhotoSwipe setup.
- Assigned instance to ~window.pckry~ inside the helper so legacy checks remain valid.
- Lazy-loaded Draggabilly inside ~setupPackeryLifecycle~ so admin drag features ship only when needed, keeping baseline bundle smaller.

** Page Integrations
- Homepage now imports the helper via ~import initPackeryGallery from '/src/scripts/packeryGallery.ts';~ (
  absolute path ensures Vite bundles correctly), replacing the 800+ line inline script.
- Catch-all gallery route renders the same ~.masonry-item~ markup/styling and invokes the helper with admin tooling disabled.
- Search + tag filters call Packery ~layout()/shiftLayout()~ after toggling ~display~, keeping the grid gapless.

** Mobile Improvements
- Fallback branch forces flex-column layout on the container and overrides desktop ~!important~ heights using ~style.setProperty(..., 'important')~ so cards expand to full height without whitespace.
- Images switch to ~object-fit: contain~ at narrow widths; gap tightened to 8px for consistent vertical rhythm.
- Auto-applied ~animated-in~ class on mobile so cards become visible even without Packery transitions.

** DX Notes
- Added `/src/...` import guidance and mobile height override lessons to ~docs/codebase-wisdom.org~ to prevent future 404s/gaps.
- New checklist item: verify frontend helpers use absolute imports and pass ~npm run build~ before landing.
- Replaced admin toolbar inline styles with CSS injected via ~injectAdminToolbarStyles()~, making future tweaks easier.

* 2025-10-04 - Journal / Blog Rollout

** Content Pipeline
- Added ~blog~ content collection with schema for Cloudinary public IDs so posts inherit the existing `t_gallery` transform.
- Seeded `hello-world.mdx` sample entry that references real red-carpet imagery hosted on Cloudinary.

** Templates
- New listing view at ~src/pages/blog/index.astro~ mirrors the Socratic layout: hero card, smart-quoted excerpts, responsive Cloudinary hero via ~@jaydixit/astro-utils/components/Image.astro~.
- Detail page (~src/pages/blog/[slug].astro~) renders hero figure, SEO-friendly excerpt, tags, and clears the fixed nav with generous top padding.

** Shared Utilities
- Imported ~@jaydixit/astro-utils/styles/blocks.css~ for blockquote/prompt styling parity and wired ~smartQuotes~ for frontmatter excerpts.
- Cloudinary helpers from ~src/lib/cloudinary.ts~ now service the journal, complementing the gallery system.

** Navigation & Styling
- NavBar adds a “Journal” link; blog sections receive top padding so the fixed header no longer obscures the intro label.

** Validation
- ~npm run build~ (passes; existing album collection warnings unchanged).

* 2025-10-05 - Gallery Asset Pipeline Alignment

** Asset Migration
- Relocated filesystem-driven galleries from ~public/photos/~ into ~src/gallery/photos/~ so Astro’s optimizer can fingerprint originals and emit responsive variants.
- Catch-all gallery page now glob-imports the new source tree, derives captions/tags centrally, and renders each item through ~Image~ metadata with hashed lightbox targets.

** Listing & Tooling
- Gallery index rebuilds its cards from the updated metadata pool and serves optimized thumbnails instead of raw ~img~ tags.
- Upload-to-Commons admin view swaps to Astro-provided preview URLs while generating absolute source paths under the new directory for uploader scripts.

** Validation
- ~npm run lint~
- ~npm run build~
* 2026-08-10 - Restore Red Carpets concept lab to the experiment chooser

** Change
- Restored the six-city Red Carpets concept lab from the unmerged ~codex/around-the-world~ branch without importing that branch’s superseded navigation or portrait-route changes.
- Restored the Full Bleed, Passport, Departures, Contact Sheet, Editorial Journey, and map-led Route review surfaces under ~/red-carpets/~.
- Added the concept chooser at ~/red-carpets/variants/~ to the main ~/experiments/~ page.

** Source history
- The concept was originally developed in twelve commits on 2026-07-22 and 2026-07-23, then remained isolated on the remote feature branch rather than being merged into ~main~.

* 2026-08-11 - Build the 2024 Nobel coverage page

** Page and photographs
- Added a dedicated editorial page at ~/nobel-2024~ using the photographs Jay selected from the Commons Gallery download.
- Organized the page around the Nobel Lectures in Stockholm and the Nobel Peace Prize Torchlight Procession in Oslo, with every photograph used once and a single-column mobile sequence.
- Added the new photographs under [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/nobel-prizes-2024/][📁 Nobel Prizes 2024]] with explicit tags and Wikimedia Commons links.
- Added the award presentation at Oslo City Hall as the lead Oslo frame, connecting the ceremony described in the text to the photographed event.
- Verified the current Wikipedia uses of the ceremony, James A. Robinson, and John Hopfield photographs, then removed the placement section from the public page at Jay’s direction.
- Replaced the initial Demis Hassabis selection with Jay’s supplied Lightroom export and removed the old Commons link because it referred to a different frame.
- Added Jay’s new John Jumper and Daron Acemoglu exports, replacing both ~TK~ cards, and replaced the initial David Baker frame with Jay’s new selection.
- Made John Hopfield the lead photograph because his Physics prize recognized work enabling machine learning with artificial neural networks; moved Baker into the three-person Chemistry sequence so no photograph repeats.

** Completed selections
- John Jumper and Daron Acemoglu initially appeared as visible ~TK~ placeholders; Jay supplied both photographs on 2026-08-11.

** Final copy direction
- Replaced the initial introduction with Jay’s three-paragraph account of the two 2024 Nobel committees recognizing work in AI, the distinct Physics and Chemistry achievements, and his travel from Stockholm to Oslo.
- Expanded the Hopfield passage with Jay’s account of Hopfield’s 1982 work, his age, his use of two canes, and Jay’s reaction to the 42-year wait for recognition.
- Removed the separate Hinton-versus-Hopfield observational paragraph and the entire Wikipedia-placement section at Jay’s direction.

** Discovery
- Linked the Stockholm chapter of ~/red-carpets~ to the new Nobel page without adding another item to the global navigation.
- Updated the homepage menu, homepage hero action, and Before & After navigation so every “Red Carpets Around the World” link opens the canonical ~/red-carpets~ page instead of the former homepage section anchor.

** Verification
- ~npm run build~ passes and generates ~/nobel-2024~ plus the associated optimized image derivatives.

* 2026-08-21 - Refine the homepage photo wall and closing section

** Homepage photographs
- Replaced the weak top-right hero image with the existing Jason Bateman and Jude Law portrait from TIFF 2025.
- Replaced the former bottom-left Jude Law frame with Lisa Gilroy, then completed Jay’s requested two-way position swap so Lisa Gilroy occupies Jason Bateman’s former slot and the Jason Bateman solo portrait occupies the bottom-left slot.
- Preserved the desktop wall order and the separate 11-card mobile wall sequence, including its existing duplicate suppression.

** Photo presentation and lightbox
- Removed visible captions from both the hero collage and the main photo wall while preserving semantic alt text, accessible labels, and PhotoSwipe caption metadata.
- Verified that wall photographs open in PhotoSwipe over the still-visible homepage, navigate between images, and close back to the same wall position rather than opening a raw image or standalone page.

** Closing section and numbering
- Rebuilt the previously skeletal closing section as a full editorial composition with an oversized Jay Dixit Photos wordmark, a red rule, a subtle paper-toned background, and large responsive navigation links.
- Removed the useless section counters entirely and removed leading-zero formatting from the remaining photo-wall numbers.
- Added dedicated desktop and mobile layouts for the closing navigation without changing the approved top of the homepage.

** Typography
- Applied smart apostrophes and quotation marks to rendered homepage prose, editorial pages, album captions, person names, alt text, PhotoSwipe captions, page metadata, and structured data.
- Confirmed that the production output contains no straight quotation marks in rendered prose or metadata; code syntax, URLs, filenames, and identifiers remain unchanged.

** Verification
- ~npm run build~ passes and generates all 123 static pages.
- ~npm run lint~ passes.
- ~npm run test -- --run~ passes all 21 tests.
- Browser verification passes at desktop and a true ~390x844~ mobile viewport with no console errors, no visible wall captions, no section counters, and working PhotoSwipe behavior.

* 2026-08-22 - Refine the homepage hero-print selection

** Hero photographs
- Swapped Lisa Gilroy and Sydney Sweeney between their existing hero-print positions.
- Added Jay’s Alexander Skarsgård portrait from NYFF as the canonical local highlight [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights/alexander-skarsgard_nyff_2025.jpg][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights/alexander-skarsgard_nyff_2025.jpg]], uploaded it to Cloudinary under ~highlights/alexander-skarsgard_nyff_2025~, and replaced only the solo Jason Bateman hero print with it.
- Kept the Jason Bateman and Jude Law two-shot in the hero and kept the solo Jason Bateman portrait in the desktop wall. Explicit mobile duplicate suppression preserves the existing 11-card mobile wall sequence.

** Design reaction
- Jay said of the hero prints and clothespins: “I really like it, but maybe it could be even better. or maybe it's great as is.” No clothespin styling changed in this pass.
- Jay then found Alexander’s initial angle strange. After trying ~-20deg~, his instruction to rotate it 15 degrees to the right was initially misread as an absolute ~+15deg~; subsequent visual review set Alexander to ~-2deg~, Judd Apatow to ~-1deg~, and Vanessa Kirby to ~0deg~.

** Verification
- ~pnpm run test -- --run~ passes all 21 tests, and ~pnpm run build~ generates all 123 static pages.
- Desktop and ~390x844~ mobile browser checks confirm the seven hero prints, Alexander in the intended slot, the 11-card mobile wall, no browser errors, and working PhotoSwipe open, next-image navigation, and Escape close.

** Second hero-layout pass
- Brought John Hopfield to the front layer and moved him slightly right without changing his approved rotation.
- Added Steven Yeun as an eighth hero print between Alexander Skarsgård and Sydney Sweeney, then set his reviewed position to ~x: 47%~, ~y: 64%~, and ~-1.7deg~.
- Swapped Sydney Sweeney into the large central slot and Vanessa Kirby into Sydney’s former lower slot, then brought Vanessa to the front layer.
- Changed the mobile contact-sheet strip from four to five columns so all five smaller prints remain on one row. The production build passes, and PhotoSwipe opens Steven as item 5 of 8, navigates to the next image, and closes with Escape.

* 2026-08-22 - Apply Arno Pro to long-form reading text

** Scope and font loading
- Loaded Adobe Fonts kit ~lvx1ktg~ only on the homepage, About Me, Before & After, Red Carpets Around the World, Nobel 2024, and individual blog posts; the blog index, gallery, photo pages, and other utility surfaces do not load it.
- Applied ~arno-pro~ to running prose, ~arno-pro-subhead~ to leads and pullquotes, and kept Newsreader display titles plus Instrument Sans interface text unchanged.
- Used the system fallback stack ~"Iowan Old Style", "Palatino Linotype", Palatino, Georgia, serif~ for every Arno family.

** Typography
- Homepage hero intro: ~arno-pro-subhead~, weight ~400~, ~28px~, line-height ~1.4~.
- Essay body paragraphs and list items: ~arno-pro~, weight ~400~, ~21px~, line-height ~1.55~, approximately ~23px~ paragraph spacing, maximum measure ~68ch~; ~strong~ uses weight ~600~ and ~em~ uses Arno italic.
- Essay leads: ~arno-pro-subhead~, weight ~400~, ~25px~, line-height ~1.4~, retaining existing italic treatment where present.
- Pullquotes: ~arno-pro-subhead~, weight ~400~, ~30px~, line-height ~1.35~; attributions retain their existing sans styling.
- Links inside prose inherit only the reading face; their existing colours and hover behaviour remain unchanged.

** Deployment follow-up
- After the first production deploy, confirm the kit CSS loads for ~photos.jaydixit.com~ at fonts.adobe.com or in the browser Network tab. If it is blocked, add ~photos.jaydixit.com~ to the kit's domain allowlist at fonts.adobe.com.

* 2026-08-22 - Set the homepage hero in Garamond Premier Pro Light Display

** Change
- Jay said “ok let's do garamond.” Replaced Adobe Fonts kit ~lvx1ktg~ with ~mvc8kcq~, which carries the existing Arno families and ~garamond-premier-pro-display~.
- Changed only the homepage ~#hero-title~ to ~garamond-premier-pro-display~ at weight ~300~, letter-spacing ~-0.034em~, line-height ~0.82~, and each existing responsive font-size clamp multiplied by ~1.04~. Newsreader remains the first fallback and remains loaded for the rest of the site.
- Preserved the red ~Red Carpet~ span and the indented ~for Wikipedia~ line without changing its ~0.78em~ size or responsive margins.
- Codex ranked Premier over Kepler over Newsreader for this display treatment.

** Deployment follow-up
- After the first deploy, confirm the Adobe Fonts kit domain allowlist includes ~photos.jaydixit.com~.

* 2026-08-22 - Expand and refine Before & After

** Homepage photographs
- Replaced Lisa Gilroy with Steven Yeun in the homepage hero while retaining Lisa as the final desktop photo-wall card and a centered mobile wall card.

** Comparison sequence
- Removed Conan O’Brien, added Lisa Gilroy after Sydney Sweeney, added Sebastian Stan after Jeremy Strong, and added Fondazione Prada, Milan as the final comparison.
- Kept John Hopfield after Sebastian Stan and moved Jeremy Strong immediately below Lisa Gilroy.
- Added the supplied historical photographs and reused the existing canonical Sebastian Stan gallery portrait for his 2024 after image.

** Route and navigation
- Renamed the canonical route to ~/before-and-after/~ and updated every live internal link, navigation item, font-loading route, and browser test.
- Preserved ~/before-after/~ as a permanent redirect so existing links continue to work.

** Page experience
- Restored PhotoSwipe across every comparison photograph.
- Added an eight-item editorial table of contents beneath the introduction, with anchor navigation, a four-column desktop grid, and a horizontally scrollable mobile strip.
- Removed leading zeroes from visitor-facing counters and chapter labels across the homepage, Nobel page, experiment routes, and Red Carpets concepts, including counters updated at runtime.

** Verification
- Source audit found no remaining leading-zero formatting in visitor-facing counters; literal filenames, dates, coordinates, and media identifiers remain unchanged.
- Desktop and ~390x844~ mobile browser checks confirmed the comparison order, TOC navigation, Fondazione Prada crops, no page overflow, no broken images, and working PhotoSwipe open and Escape-close behavior.
- ~npm run lint~, all 21 Vitest tests, the 124-page production build, and all 25 Playwright browser tests pass.

* 2026-08-22 - Replace the John Jumper Nobel photograph

** Photograph
- Replaced the John Jumper image on the Nobel 2024 page with Jay’s supplied Wikimedia Commons portrait of Jumper presenting his Nobel Lecture.
- Preserved the existing caption, alt text, Nobel-page position, and PhotoSwipe integration, and linked the image to its Commons source page.
- Added John Sears’s supplied Stockholm portrait beneath “Photographing The Nobel Lectures in Stockholm,” using the embedded creator credit and preserving PhotoSwipe.

** Physics photograph pairing
- Changed the Nobel-page hero to Demis Hassabis and placed John Hopfield first in a two-photo Physics pair with Geoffrey Hinton, so the existing Hopfield passage leads into Hopfield’s photograph while preserving Hinton on the page.
- Replaced the repeated hero photograph in the Chemistry section with Jay’s supplied second Hassabis photograph. Baker and Hassabis share the first row; Jumper is centered alone below, so three photographs never occupy one row.

** Body typography
- Increased the Nobel-page introduction and section body copy to ~25px~, matching the size of the hero introduction without changing Jay’s prose.

** Economic Sciences
- Added Jay’s supplied Simon Johnson portrait to the Economic Sciences section.
- Used orientation-specific editorial layouts: Jumper anchors the right side of the Chemistry composition beside stacked Baker and Hassabis landscapes; Robinson and Johnson share the first Economic Sciences row, with Acemoglu’s landscape spanning the row below.

** Oslo section
- Moved the Nobel Peace Prize ceremony award-presentation photograph into its own “The ceremony” subsection and kept only procession photographs under “The Torchlight Procession.”
- Filled the sixth Torchlight Procession grid slot with Jay’s supplied wide crowd photograph.
- Replaced the existing ~oslo-21~ procession photograph with Jay’s stronger alternate frame and removed the old frame’s no-longer-accurate Commons link.

** Copy
- Added Jay’s supplied paragraph about Hopfield as a speaker and Hinton reading from his notes, verbatim.
- Removed the sentence about Baker being a great speaker and Hassabis being nice and friendly.
- Replaced “An hour with each laureate.” with “Photographing The Nobel Lectures in Stockholm”.
- Replaced “The institutions behind prosperity” with “The Nobel Prize in Economics”.

** WikiPortraits story
- Added “See my other before and after photos” links beneath both the Vanessa Kirby and Jeremy Strong comparisons, pointing to the canonical ~/before-and-after/~ page.
- Extracted the opening beat into [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/WikiPortraitsOpening.astro][WikiPortraitsOpening.astro]] and moved the centered Emil Wakim example and its smaller caption above the “Wikipedia’s articles” explanation.
- Rebuilt the Pete Davidson comparison as a centered single-column sequence and added the 2013 Wikipedia photograph with Jay’s setup and explanation, followed by Jay’s supplied mall image and both existing photographs.
- Replaced the commentary following the Pete Davidson comparison with Jay's supplied passage about Wikipedia photographs becoming unofficial portraits of record.
- Added Jay’s current Kyle Bartley Wikipedia-photo example immediately after the linked “blurry thumbnail” passage.
- Added print-specific article reflow rules and exported a visually verified, 23-page letter-size PDF to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/output/pdf/wikiportraits-story.pdf][wikiportraits-story.pdf]].
- Placed Jay’s supplied TIFF group photograph full-width beneath independently sized origin columns, added Jay’s caption, and turned the travel paragraph into a large full-width transition above it.
- Added the two existing SXSW 2024 Conan O’Brien photographs as a compact pair beneath the origin story’s right-column copy.
- Expanded and rebalanced the Ana de Armas technique passage beside Jay’s TIFF portrait, added Jay’s caption, and kept the opening sentence as body text.
- Added the supplied Jennifer 8. Lee and Kevin Payravi portrait beneath the shortened team passage, kept the photograph in its true orientation, enlarged it, and added Jay’s revised caption.
- Raised undersized narrative copy throughout the feature while keeping captions visually subordinate.
- Replaced the global Arno ~21px~ WikiPortraits rule with the Jenny/SXSW passage’s large narrative minimum and prevented the generic mobile blog rule from shrinking feature-essay copy.
- Standardized feature captions in sans serif, moved the accreditation arrow between the explanatory copy and the form it points toward, and corrected Jeremy Strong’s caption from “sat” to “posed.”
- Updated the reach stat with Jay’s 10-million-view line and its parenthetical contrast.
- Updated the collective reach statistic to almost 400 million monthly views, framed as more than 1 out of every 100 Wikipedia pageviews globally.
- Replaced the article description, corrected “Wikipedia’s” to a typographic apostrophe, and revised the Emil Wakim caption with Jay’s exact copy.
- Enlarged every narrative transition in the Pete Davidson photo sequence to a consistent display scale, including the inner paragraphs that MDX generates inside the transition wrappers.
- Removed “He’s just Pete, chilling at the mall.” from the mall-selfie beat.
- Added the current Kyle Bartley example after the linked “blurry thumbnail” passage, moved the two bad-Wikipedia-photo Instagram links into the same all-caps ongoing-situation paragraph, and removed the redundant general sentence about editors deleting bad photographs.
- Repaired the large full-width travel transition’s MDX wrapper so the visible paragraph receives the intended display scale instead of leaving that class on an empty generated paragraph.

** Verification
- Desktop and ~390x844~ mobile browser checks confirmed the final image order and layouts, ~25px~ body copy, no overflow or error overlays, the ceremony/procession separation, and all 17 distinct photographs loading.
- PhotoSwipe opened the Stockholm introduction photograph as ~2 / 17~ and the final procession photograph as ~17 / 17~, and closed with Escape.
- Formatting, lint, all 21 Vitest tests, the 128-page production build, and all 25 Playwright browser tests pass.

* 2026-08-22 - Add Stephen Root, Ella Hunt, Demis Hassabis, and two Oslo photographs to the homepage photo wall

** Photographs
- Added Jay’s Stephen Root portrait from SXSW 2024 and the supplied Ella Hunt portrait from TIFF 2024 to ~src/gallery/highlights/~ and the Cloudinary-backed homepage image manifest.
- Added the existing Demis Hassabis Nobel Lectures portrait to the Cloudinary-backed homepage image manifest without duplicating its canonical gallery source.
- Added two separate existing Oslo Nobel Peace Prize torchlight-procession photographs to the same manifest without duplicating their canonical gallery sources.
- Added all five photographs after Lisa Gilroy in the photo wall. Stephen Root is mobile position ~13~, Ella Hunt is position ~14~, Demis Hassabis is position ~15~, and the Oslo photographs are full-width positions ~16~ and ~17~.
- Preserved the existing six hero-duplicate exclusions and extended the established full-width mobile-beat pattern to both new final landscape cards.

** Nobel page
- Reordered the Physics-prize sentence to name Geoffrey Hinton before John Hopfield, exactly as requested.
- Replaced the Nobel Lectures introduction with Jay’s revised sentence describing each laureate’s hour on stage.
- Removed ~.prose-section~ from the global Arno reading-measure selector. The selector had constrained the entire two-column Nobel intro grid to ~68ch~, leaving only a one-word-wide text column at desktop widths.

** Verification
- Updated the mobile-wall regression test for 17 visible photographs and a ~1 / 17~ PhotoSwipe sequence.
- Desktop and ~390x844~ mobile browser checks confirmed all 23 wall cards, distinct ~oslo-21~ and ~oslo-15~ images, the final two full-width mobile beats, no horizontal overflow, and working PhotoSwipe open and Escape-close behavior.
- Desktop and mobile browser checks confirmed the repaired Nobel introduction width and both requested copy changes.
- ~npm run lint~, all 21 Vitest tests, the 124-page production build, and all 25 Playwright browser tests pass.

* 2026-08-22 - Make Prettier and Git whitespace checks agree for MDX

- Added a narrow ~*.mdx whitespace=-space-before-tab~ rule to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/.gitattributes][.gitattributes]]. Prettier’s MDX printer emits Markdown indentation followed by JSX tabs and still reports that output as correctly formatted; Git now accepts that formatter-owned indentation without weakening whitespace checks for other source files.

* 2026-08-23 - Add the private reviewed photo publisher

** Workflow
- Added [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/scripts/photo-publish.js][📄 /Users/jay/Dropbox/github/photos-jaydixit/scripts/photo-publish.js]], an agent-facing planner and resumable executor for Lightroom exports.
- Kept quality, favorite status, and publication destinations independent. Quality is ~unreviewed~, ~reject~, ~fine~, or ~great~; favorite is a separate boolean and is valid only for a ~great~ photograph.
- Default plans include a local portfolio copy, Wikimedia Commons, and Cloudinary. Public execution requires an approved manifest plus the literal ~--confirm PUBLISH~ gate.
- Added the noninteractive Pywikibot ~UploadRobot~ adapter at [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/scripts/commons-upload.py][📄 /Users/jay/Dropbox/github/photos-jaydixit/scripts/commons-upload.py]]. Upload warnings abort rather than being silently ignored.
- Added local gallery metadata and person-index wiring, including an explicit exclusion for group or unresolved-person photographs so they do not masquerade as solo portraits on the red-carpet index.
- Made the batch gallery a per-photo review default rather than a fixed execution path. Mixed exports can override each photograph’s gallery, and execution rebuilds its local path and default Cloudinary public ID from that reviewed value.
- Added resumable destination receipts and updates to [[file+emacs:/Users/jay/Dropbox/roam/photography/20260823235900-celebrity-photograph-inventory.org][📄 Celebrity Photograph Inventory]].

** Safety
- Planning reads metadata and hashes only; it does not copy or upload photographs.
- Execution verifies source hashes and destination preconditions, refuses overwrites with different contents, and never commits, pushes, deploys, or selects homepage highlights.
- Commons Gallery albums remain a second authenticated browser step after the Commons upload and require action-time approval.

** Verification
- The planner inventoried all eight current TIFF export JPEGs without modifying them. Six have the five required editorial fields; two are missing all five. With Commons enabled and no approved category supplied, all eight correctly remain blocked.
- A destination-disabled reviewed reject was written to a temporary Org inventory twice without duplication or Org ID churn. A ~favorite: true~ photograph with ~quality: fine~ was correctly rejected before any write.
- The publication command, Pywikibot adapter help surface, and Exwalker handoff passed syntax and help checks. All 21 Vitest tests, lint, and the 128-page production build passed. No upload or shared-state action was attempted.

* 2026-08-23 - Add reviewed Commons new-version replacements

** Workflow
- Added an explicit ~new-version~ Commons mode that requires the exact existing filename and a replacement comment.
- Added a read-only ~preview~ command that verifies the unchanged local source, reports local dimensions, resolves the live Commons target, and reports the current Commons revision checksum, dimensions, timestamp, and uploader.
- Kept new-version uploads separate from new-file uploads. A replacement uses Pywikibot's existing ~FilePage~, preserves the description page and categories, ignores only the expected ~exists~ warning, and verifies that Commons created a new binary revision afterward.
- Prepared a local-only Willem Dafoe replacement plan for [[https://commons.wikimedia.org/wiki/File:Willem_Dafoe_at_the_2024_Toronto_International_Film_Festival_02.jpg][the existing TIFF 2024 Commons file]]. The current revision is ~2898x3623~; Jay's corrected export is ~2400x3000~. No upload occurred.

** Verification
- Prettier, Node syntax checking, ESLint, all 21 Vitest tests, the Pywikibot adapter help surface, and the read-only live Commons preview passed.
- The replacement executor still requires an approved manifest and the literal ~--confirm PUBLISH~ action-time gate.

** First completed replacement
- After Jay's explicit approval, uploaded the corrected Willem Dafoe export as the current revision of [[https://commons.wikimedia.org/wiki/File:Willem_Dafoe_at_the_2024_Toronto_International_Film_Festival_02.jpg][the existing Commons file]] through his authenticated ~Jaydixit~ browser session.
- Verified the live revision at ~2400x3000~, timestamped ~2026-08-23T07:04:02Z~, with the approved color-correction and slightly-tighter-crop comment. The prior ~2898x3623~ revision remains in file history.
- Commons' global-usage API currently returns no transclusions for this exact file, so no Wikipedia page changed from this particular replacement.
- Pywikibot could not complete noninteractively because ~site.login()~ waited for an interactive credential. Added ignored stdin and a 60-second timeout so a future missing credential fails instead of hanging; the authenticated browser is the verified fallback.

* 2026-08-24 - Replace unfinished portfolio destinations in public navigation

** Navigation
- Removed Red Carpets Around the World from the homepage desktop navigation, homepage compact menu, and Before & After header until its AI-slop headlines have been replaced with approved copy. The ~/red-carpets/~ route, homepage editorial section, hero action, and internal experiment links remain intact.
- Removed Red Carpet from the homepage, Before & After, and standard desktop/mobile navigation without removing or redirecting the ~/red-carpet/~ directory.
- Promoted the finished Nobel editorial page under the exact navigation label Covering the Nobel Prizes, linking to ~/nobel-2024/~ everywhere.
- Added a compact standard-header regime from ~1240px~ through ~1599px~ so the longer Nobel label and remaining links fit without clipping; widths below ~1240px~ use the existing menu toggle.

** Follow-up
- Recorded ~De-slop Red Carpets Around the World~ under Active Priorities in [[file+emacs:/Users/jay/Dropbox/roam/velocity/goals/current-focus.org][📄 Current Focus]], with restoration to the navigation gated on approved replacement copy.

** Verification
- Browser checks confirmed the updated homepage desktop and compact menus, Before & After header, and standard desktop/mobile navigation contain Covering the Nobel Prizes and omit both removed destinations.
- Verified the standard header at ~1239px~, ~1240px~, ~1280px~, ~1440px~, and ~1600px~ with no clipped navigation, horizontal overflow, framework overlay, or browser errors. Scoped Prettier, lint, all 21 Vitest tests, the 128-page production build, and all 25 Playwright tests pass.

* 2026-08-24 - Add Nobel Peace Prize press credentials photograph

- Added Jay's supplied press-credentials photograph to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/pages/nobel-2024.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/pages/nobel-2024.astro]] as a layered print over the unused left side of John Sears's large Stockholm photograph of Jay, cropped in CSS to remove the empty lower tabletop while preserving the full source for PhotoSwipe, and finalized at ~28.8%~ of the main photograph's desktop width.
- Preserved the original HEIC at [[file+emacs:/Users/jay/Downloads/Photos/IMG_3247.heic][📄 /Users/jay/Downloads/Photos/IMG_3247.heic]] and created a semantic, metadata-complete JPEG copy at [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/nobel-prizes-2024/press-credentials_nobel-peace-prize-oslo_2024.jpg][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/nobel-prizes-2024/press-credentials_nobel-peace-prize-oslo_2024.jpg]].
- Preserved the supplied ceremony-program source at [[file+emacs:/Users/jay/Downloads/IMG_2464.HEIC][📄 /Users/jay/Downloads/IMG_2464.HEIC]] and created the metadata-complete JPEG [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/nobel-prizes-2024/nobel-peace-prize-ceremony-program_oslo-city-hall_2024.jpg][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/nobel-prizes-2024/nobel-peace-prize-ceremony-program_oslo-city-hall_2024.jpg]] as a smaller matching print on the right side of the Stockholm photograph, finalized at ~21.6%~ of the main photograph's desktop width.
- Used Jay's corrected displayed captions exactly: “Covering the Nobel Prizes in Stockholm in the dead of winter. Photo by John Sears”, “My press credentials for the Nobel Peace Prize Ceremony”, and “Program for the Nobel Peace Prize Award Ceremony at Oslo City Hall”. Embedded the corrected wording in each JPEG's title and caption metadata as well.
- Embedded Jay's approved title and caption wording in the four Nobel JPEGs that previously lacked those fields.
- Replaced the main Stockholm photograph with Jay's preferred export from [[file+emacs:/Volumes/Pantheon/lightroom/lightroom-exports/2024-12-08_Stockholm_Nobel_Prize-chemistry_economics/2024-12-08_Stockholm_Nobel_Prize-chemistry_economics_-4.jpg][📄 /Volumes/Pantheon/lightroom/lightroom-exports/2024-12-08_Stockholm_Nobel_Prize-chemistry_economics/2024-12-08_Stockholm_Nobel_Prize-chemistry_economics_-4.jpg]], preserving the approved title, caption, and John Sears credit in the portfolio copy.
- Desktop and ~390x844~ rendered checks confirmed both prints remain inset from the main photograph's outer edges without covering Jay, the page has no horizontal overflow, and PhotoSwipe opens the credentials print as ~3 / 19~, advances to the program as ~4 / 19~, and closes with Escape. All 19 Nobel photographs pass the portfolio metadata validator.
- Added Jay's Conan O'Brien portrait beside the two comic SXSW frames in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/content/blog/wikiportraits-story.mdx][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/content/blog/wikiportraits-story.mdx]], with a three-up desktop and mobile row. Rendered checks confirmed all three photographs remain in document order without horizontal overflow and the added portrait participates in the page's PhotoSwipe sequence.
- The repository lint gate and 130-page production build pass.
- Added five more photographs from the Nobel Peace Prize Ceremony, including an upright JPEG prepared from Jay's high-angle HEIC. The existing award photograph leads at full width; beneath it, the high-angle photograph spans the height of two horizontal photographs stacked to its right, followed by the final two horizontal photographs as a pair. The layout becomes one photograph per row on phones. Per Jay's direction, the five additions use the same general caption, “The Nobel Peace Prize Ceremony in Oslo,” without naming unidentified people.
- Desktop rendering confirmed that the high-angle photograph exactly matches the combined height of the podium and lineup photographs stacked to its right; the ~390x844~ layout has no horizontal overflow. PhotoSwipe opens the high-angle photograph as ~14 / 24~, advances to ~15 / 24~, and closes with Escape. All 24 Nobel photographs pass the metadata validator, and lint plus the 135-page production build pass.

* 2026-08-24 - Refine the mobile homepage photo hierarchy

- Changed the mobile photo wall from a mixture of half-width and full-width cards to one full-width photograph per row while preserving portrait and landscape proportions. The mobile wall now contains 13 non-repeating photographs because photographs promoted into the hero are excluded from the wall at that breakpoint.
- Expanded the mobile hero to six full-width prints in this order: Vanessa Kirby; Jason Bateman and Jude Law; Sydney Sweeney; Steven Yeun; Jason Bateman; Alexander Skarsgård. Replaced the previous contact strip with four small prints: Jason Collett; Kieran Culkin; Dave Franco; Willem Dafoe. Judd Apatow remains in the unchanged desktop collage and is hidden from the mobile hero.
- Extended [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/components/PhotoSwipeLightbox.astro][📄 /Users/jay/Dropbox/github/photos-jaydixit/src/components/PhotoSwipeLightbox.astro]] to support multiple breakpoint-specific exclusions and visual-order sorting, keeping each lightbox sequence aligned with the photographs visible at that breakpoint.
- Changed the displayed homepage title to “Jay Dixit: Red Carpet Photographer” and changed the introduction to say “red carpet photographer for Wikipedia.” On desktop, the introduction follows Jay's specified five-line composition; on mobile, it wraps responsively. Increased the final title line's lower inset so the descender in “Photographer” is fully visible.
- Browser checks at ~1440x1000~ and ~390x844~ confirmed the desktop collage remains limited to its original six photographs, the mobile hero contains the requested six lead prints and four thumbnails, all 13 mobile wall photographs are full width, both lightbox sequences exclude hidden photographs and follow visible order, and neither layout has horizontal overflow or framework errors.
- Prettier, lint, all 21 Vitest tests, and the 135-page production build pass.
- Added ~/tmp/~ to the repository ignore rules because it contains approximately 102 MiB of generated WikiPortraits PDF rendering intermediates. The future deletion is tracked in [[file+emacs:/Users/jay/Dropbox/roam/velocity/goals/current-focus.org][📄 Current Focus]]; the final PDF under ~/output/pdf/~ remains separate and unignored.

* 2026-09-02 - Publish Jack Johnson SXSW 2026 photographs

- Applied Jay-approved metadata to three Downloads exports through Exwalker with verified backups under [[file+emacs:/Users/jay/Downloads/.exwalker-backups/20260902-024736][📁 /Users/jay/Downloads/.exwalker-backups/20260902-024736]]. Jay rated all three ~great~ and selected the wide crowd-and-stage frame as the favorite.
- Published semantic local copies under [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/sxsw-2026][📁 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/photos/events/sxsw-2026]], uploaded and verified three new Wikimedia Commons files, and uploaded matching Cloudinary gallery mirrors.
- Added the favorite to [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights/jack-johnson_sxsw_2026_03.jpg][📷 /Users/jay/Dropbox/github/photos-jaydixit/src/gallery/highlights/jack-johnson_sxsw_2026_03.jpg]], the homepage configuration, exact image metadata, and Cloudinary as ~highlights/jack-johnson_sxsw_2026_03~. The live Cloudinary asset returns HTTP 200 at 2654 by 2199 pixels; the local site changes remain undeployed.
- Corrected Pywikibot BotPassword storage to its native ignored ~.passwd~ file, ignored its account-specific session-cookie files, and verified noninteractive login as ~Jaydixit~. No duplicate environment-secret copy was added.
- Corrected the Commons inventory synchronizer to fetch non-hidden page categories through the MediaWiki ~categories~ property with continuation support. The shared registry now contains 655 distinct Commons files, 70 local portfolio records, the three Jack Johnson Commons pages, and the favorite's configured homepage and Cloudinary-highlight surfaces.
- Verification passed for the three-photo metadata validator, structured-file Prettier checks, synchronizer ESLint, Cloudinary delivery, live Commons page text/categories, and inventory freshness.

** Homepage placement
- Added the favorite Jack Johnson photograph to the homepage photo wall as a full-width landscape immediately before the two Oslo torchlight-procession photographs, using the dedicated ~highlights/jack-johnson_sxsw_2026_03~ Cloudinary asset.
- Desktop and ~390x844~ browser checks confirmed the placement, responsive image delivery, no horizontal overflow or framework overlay, and working PhotoSwipe open, next, and Escape-close behavior. The mobile wall now contains 14 non-repeating photographs: the 13-photo sequence deliberately established in ~a0103b0~ plus Jack.
- Corrected the stale mobile-homepage rules in [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/AGENTS.md][📄 AGENTS.md]] and [[file+emacs:/Users/jay/Dropbox/github/photos-jaydixit/CLAUDE.md][📄 CLAUDE.md]] to match the deliberate six-lead-plus-four-thumbnail hero and ten wall exclusions established in ~a0103b0~. Updated the browser regression test for 14 visible wall photographs and added explicit assertions that Jack precedes the two full-width torchlight photographs.

** Durable publication protocol update
- Added a pre-publication gate that verifies every source hash, owner-only ignored Pywikibot credential storage, noninteractive Commons login, Commons categories, and Cloudinary authentication before any local copy or upload begins. Credential diagnostics report only safe state and never secret values.
- Split EXIF ~capture_date~ from the reviewed event-local ~event_date~ and require explicit approval when they differ. Corrected all three live Commons description pages and all four Cloudinary assets from the March 14 camera date, or a missing date, to the approved March 13 SXSW event date without changing other page text, Cloudinary context fields, or tags.
- Added an independent ~destinations.homepage~ manifest decision. A homepage destination requires a Jay-selected ~great~ favorite and now owns the local highlight copy, dedicated ~highlights/~ Cloudinary public ID, homepage configuration, exact dimensions, and receipt.
- Persisted and reported the phases ~preflight~, ~portfolio~, ~commons~, ~cloudinary~, ~homepage~, ~commons-gallery~, ~inventory-commons~, and ~inventory-site~. The final idempotent Jack Johnson execution completed every enabled phase, skipped only the unused Commons Gallery phase, retained 655 distinct Commons files and 70 local portfolio records in the canonical inventory, and created no duplicate upload.
- Hardened every Cloudinary Admin and Upload API call in the publisher so SDK failures are converted to action-plus-status errors before console output. A standalone diagnostic outside the publisher exposed the current Cloudinary credential in its raw error object; credential rotation remains required before further Cloudinary API work.
- The repository changes remain local and undeployed. Nothing was committed or pushed.
