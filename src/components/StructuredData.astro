---
import { serializeJsonLd } from '@jaydixit/astro-utils/utils/json-ld.ts';

type JsonLdRecord = Record<string, unknown>;
type JsonLd = JsonLdRecord | JsonLdRecord[];

const { data } = Astro.props as { data?: JsonLd | null };

const items = Array.isArray(data) ? data : data ? [data] : [];
---

{items.map((item) => <script type="application/ld+json" set:html={serializeJsonLd(item)} />)}
