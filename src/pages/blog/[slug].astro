---
import MainLayout from '../../layouts/MainLayout.astro';
import RelatedPosts from '../../components/RelatedPosts.astro';
import SocialShare from '../../components/SocialShare.astro';
import WikiPortraitsHero from '../../components/WikiPortraitsHero.astro';
import { getCollection, render } from 'astro:content';
import { smartQuotes } from '@jaydixit/astro-utils';
import { cldUrl, cldSrcset, cldSizes } from '../../lib/cloudinary';
import siteConfig from '../../../site.config.mts';

function formatTagLabel(tag: string) {
	const normalized = tag.trim().toLowerCase();
	const labelMap: Record<string, string> = {
		wikiportraits: 'WikiPortraits',
		sxsw: 'SXSW',
		tiff: 'TIFF',
		nyff: 'NYFF',
		ucb: 'UCB',
	};

	return labelMap[normalized] ?? tag;
}

export async function getStaticPaths() {
	const posts = await getCollection('blog', (entry) => !entry.data.draft);
	return posts.map((post) => ({
		params: { slug: post.id },
		props: { post },
	}));
}

const { post } = Astro.props;
const { Content } = await render(post);
const data = post.data;
const publishDate = data.publishDate ? new Date(data.publishDate) : undefined;
const blogDateFormatOptions: Intl.DateTimeFormatOptions = {
	year: 'numeric',
	month: 'long',
	day: 'numeric',
	timeZone: 'UTC',
};
const formattedDate = publishDate
	? publishDate.toLocaleDateString('en-US', blogDateFormatOptions)
	: null;
const heroImage = data.imagePublicId ? cldUrl(data.imagePublicId, 2000) : (data.image ?? null);
const heroSrcset = data.imagePublicId ? cldSrcset(data.imagePublicId) : undefined;
const excerptSource = data.excerpt ?? data.description;
const excerpt = excerptSource ? smartQuotes(excerptSource) : null;
const siteUrl = Astro.site?.origin ?? siteConfig.siteUrl ?? 'https://photos.jaydixit.com';
const canonicalUrl = new URL(`/blog/${post.id}/`, siteUrl).toString();
const showRelatedPosts = post.id !== 'wikiportraits-story';
const isFeature = data.presentation === 'feature';
const blogStructuredData = {
	'@context': 'https://schema.org',
	'@type': 'BlogPosting',
	headline: data.title,
	description: excerptSource ?? '',
	datePublished: publishDate?.toISOString(),
	dateModified: (data.updateDate ?? data.publishDate)?.toISOString(),
	author: {
		'@type': 'Person',
		name: siteConfig.owner,
	},
	publisher: {
		'@type': 'Person',
		name: siteConfig.owner,
	},
	mainEntityOfPage: canonicalUrl,
	image: heroImage ? [new URL(heroImage, siteUrl).toString()] : undefined,
	keywords: data.tags?.join(', '),
};
---

<MainLayout
	title={data.title}
	description={excerptSource}
	canonical={canonicalUrl}
	image={heroImage ?? undefined}
	imageAlt={data.imageAlt ?? data.title}
	ogType="article"
	publishedTime={publishDate}
	modifiedTime={data.updateDate ?? data.publishDate}
	keywords={data.tags}
	structuredData={blogStructuredData}
>
	<article
		id="blog-lightbox-gallery"
		class:list={[
			'blog-article',
			isFeature ? 'blog-article--feature' : 'mx-auto max-w-3xl px-6 pb-24 pt-44 md:pt-56',
		]}
	>
		{
			heroImage && (
				<figure class="relative mb-12 overflow-hidden rounded-[2.5rem] bg-slate-100 shadow-[0_25px_70px_-30px_rgba(15,23,42,0.35)]">
					<img
						src={heroImage}
						srcset={heroSrcset}
						sizes={cldSizes('(max-width: 900px) 100vw, 80vw')}
						alt={data.imageAlt ?? data.title}
						loading="eager"
						decoding="async"
						class="blog-hero-image h-full w-full object-cover"
					/>
				</figure>
			)
		}

		{
			isFeature ? (
				<div class="feature-masthead">
					<header class="blog-header feature-header">
						<p class="blog-kicker eyebrow">Essay</p>
						<h1 class="blog-title">{data.title}</h1>
						{excerpt && <p class="blog-excerpt">{excerpt}</p>}
						{formattedDate && (
							<time datetime={publishDate?.toISOString()} class="blog-meta">
								{formattedDate}
							</time>
						)}
						<SocialShare
							title={data.title}
							text={excerptSource ?? `Read ${data.title} on ${siteConfig.title}`}
							className="feature-share"
						/>
					</header>
					<div class="wikiportraits-feature-visual">
						<WikiPortraitsHero />
						<a class="wikiportraits-feature-link" href="/before-and-after/">
							<span>See my other before and after photos</span>
							<span aria-hidden="true">→</span>
						</a>
					</div>
				</div>
			) : (
				<>
					<header class="blog-header text-center">
						<p class="blog-kicker eyebrow">Essay</p>
						<h1 class="blog-title">{data.title}</h1>
						{formattedDate && (
							<time datetime={publishDate?.toISOString()} class="blog-meta">
								{formattedDate}
							</time>
						)}
						{data.tags && (
							<ul class="blog-tags">
								{data.tags.map((tag) => (
									<li>{formatTagLabel(tag)}</li>
								))}
							</ul>
						)}
						{excerpt && <p class="blog-excerpt">{excerpt}</p>}
					</header>
					<SocialShare
						title={data.title}
						text={excerptSource ?? `Read ${data.title} on ${siteConfig.title}`}
						className="mb-10"
					/>
				</>
			)
		}

		<div class="article-content mx-auto max-w-none">
			<Content />
		</div>

		{showRelatedPosts && <RelatedPosts currentSlug={post.id} currentTags={data.tags ?? []} />}
	</article>
</MainLayout>

<script>
	import PhotoSwipeLightbox from 'photoswipe/lightbox';
	import 'photoswipe/style.css';

	function updateLightboxLink(img: HTMLImageElement, link: HTMLAnchorElement) {
		link.href = img.currentSrc || img.src;

		if (img.naturalWidth > 0) {
			link.setAttribute('data-pswp-width', String(img.naturalWidth));
		}

		if (img.naturalHeight > 0) {
			link.setAttribute('data-pswp-height', String(img.naturalHeight));
		}

		if (img.alt) {
			link.setAttribute('data-pswp-caption', img.alt);
		}
	}

	function wrapBlogImage(img: HTMLImageElement) {
		const existingLink = img.closest('a');

		if (existingLink instanceof HTMLAnchorElement) {
			existingLink.classList.add('portfolio-lightbox');
			updateLightboxLink(img, existingLink);
			return;
		}

		const link = document.createElement('a');
		link.className = 'portfolio-lightbox block';
		link.setAttribute('aria-label', img.alt || 'Open image');
		img.parentNode?.insertBefore(link, img);
		link.appendChild(img);
		updateLightboxLink(img, link);
	}

	function enhanceBlogImages() {
		const gallery = document.querySelector('#blog-lightbox-gallery');
		if (!(gallery instanceof HTMLElement)) return;

		const targets = gallery.querySelectorAll<HTMLImageElement>(
			'.blog-hero-image, .wikiportraits-feature-hero img, .article-content img',
		);
		targets.forEach((img) => {
			img.classList.add('cursor-zoom-in');
			wrapBlogImage(img);

			if (!img.complete || img.naturalWidth === 0 || img.naturalHeight === 0) {
				img.addEventListener(
					'load',
					() => {
						const link = img.closest('a.portfolio-lightbox');
						if (link instanceof HTMLAnchorElement) {
							updateLightboxLink(img, link);
						}
					},
					{ once: true },
				);
			}
		});

		const lightbox = new PhotoSwipeLightbox({
			gallery: '#blog-lightbox-gallery',
			children: '.portfolio-lightbox',
			pswpModule: () => import('photoswipe'),
			padding: { top: 20, bottom: 80, left: 20, right: 20 },
			wheelToZoom: true,
			imageClickAction: 'close',
			tapAction: 'toggle-controls',
			doubleTapAction: 'zoom',
			preloaderDelay: 0,
			showHideAnimationType: 'fade',
		});

		lightbox.addFilter('itemData', (itemData) => {
			const linkEl = itemData.element;
			if (!(linkEl instanceof HTMLAnchorElement)) return itemData;

			const img = linkEl.querySelector('img');
			const width = parseInt(linkEl.getAttribute('data-pswp-width') || '', 10);
			const height = parseInt(linkEl.getAttribute('data-pswp-height') || '', 10);
			const caption = linkEl.getAttribute('data-pswp-caption') || '';

			if (Number.isFinite(width) && width > 0) {
				itemData.w = width;
			} else if (img instanceof HTMLImageElement && img.naturalWidth > 0) {
				itemData.w = img.naturalWidth;
			}

			if (Number.isFinite(height) && height > 0) {
				itemData.h = height;
			} else if (img instanceof HTMLImageElement && img.naturalHeight > 0) {
				itemData.h = img.naturalHeight;
			}

			if (caption) {
				itemData.title = caption;
			}

			return itemData;
		});

		lightbox.init();
	}

	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', enhanceBlogImages, { once: true });
	} else {
		enhanceBlogImages();
	}
</script>

<style>
	.blog-article {
		color: var(--ink);
	}

	.blog-header {
		margin-bottom: 3rem;
	}

	.blog-kicker {
		margin: 0 0 1.3rem;
	}

	.blog-title {
		margin: 0;
		font-family: var(--serif);
		font-weight: 300;
		font-size: clamp(3rem, 6.5vw, 5rem);
		line-height: 1.02;
		letter-spacing: -0.015em;
		color: #161310;
	}

	.blog-meta {
		display: block;
		margin-top: 1.35rem;
		font-family: var(--sans);
		font-size: 0.75rem;
		font-weight: 500;
		letter-spacing: 0.24em;
		text-transform: uppercase;
		color: var(--muted);
	}

	.blog-tags {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 0.65rem;
		margin: 1.35rem 0 0;
		padding: 0;
		list-style: none;
		font-family: var(--sans);
		font-size: 0.72rem;
		font-weight: 500;
		letter-spacing: 0.16em;
		text-transform: uppercase;
		color: #6e675c;
	}

	.blog-tags li {
		border: 1px solid var(--hairline);
		border-radius: 999px;
		padding: 0.32rem 0.78rem;
	}

	.blog-excerpt {
		max-width: 40rem;
		margin: 1.75rem auto 0;
		font-family: var(--serif);
		font-weight: 300;
		font-size: clamp(1.35rem, 2.5vw, 1.8rem);
		line-height: 1.4;
		color: #5c554c;
	}

	.article-content {
		font-family: var(--serif);
		font-weight: 300;
		color: #3a352e;
	}

	.article-content :global(p),
	.article-content :global(li) {
		margin: 0 0 1.5rem;
		font-family: var(--serif);
		font-weight: 300;
		font-size: 1.5rem;
		line-height: 1.5;
		color: #3a352e;
	}

	.article-content :global(ul),
	.article-content :global(ol) {
		margin: 0 0 1.5rem;
		padding-left: 1.4rem;
	}

	.article-content :global(li) {
		margin-bottom: 0.6rem;
	}

	.article-content :global(h2) {
		margin: 4rem 0 1.5rem;
		font-family: var(--serif);
		font-weight: 400;
		font-size: clamp(2rem, 4vw, 3rem);
		line-height: 1.08;
		letter-spacing: -0.015em;
		color: #161310;
	}

	.article-content :global(h3) {
		margin: 3rem 0 1.25rem;
		font-family: var(--serif);
		font-weight: 400;
		font-size: 1.8rem;
		line-height: 1.1;
		color: #161310;
	}

	.article-content :global(a:not(.portfolio-lightbox)) {
		color: var(--accent);
		text-decoration: underline;
		text-decoration-thickness: 1px;
		text-underline-offset: 0.16em;
	}

	.article-content :global(figure) {
		margin: 2.75rem 0;
	}

	.article-content :global(img) {
		display: block;
		width: 100%;
		height: auto;
	}

	.article-content :global(blockquote) {
		margin: 3rem 0;
		padding-left: 1.5rem;
		border-left: 1px solid var(--accent);
		font-family: var(--serif);
		font-weight: 300;
		font-style: italic;
		font-size: 1.5rem;
		line-height: 1.5;
		color: #2a2620;
	}

	.article-content :global(figcaption) {
		max-width: 34rem;
		margin: 0.85rem auto 0;
		font-family: var(--sans);
		font-size: 0.88rem;
		font-weight: 400;
		line-height: 1.5;
		text-align: center;
		color: #6e675c;
	}

	.article-content :global(figcaption p) {
		margin: 0;
		font: inherit;
		line-height: inherit;
		color: inherit;
	}

	.article-content :global(.portfolio-lightbox) {
		text-decoration: none;
	}

	.article-content :global(.article-image-small) {
		max-width: 34rem;
		margin-inline: auto;
	}

	.article-content :global(.article-image-small .portfolio-lightbox) {
		display: block;
		max-width: 18rem;
		margin-inline: auto;
	}

	.article-content :global(.article-image-small img) {
		width: 100%;
		max-width: 18rem;
		height: auto;
		margin-inline: auto;
	}

	.article-content :global(.article-image-medium) {
		max-width: 24rem;
		margin-inline: auto;
	}

	.article-content :global(.article-image-medium .portfolio-lightbox) {
		display: block;
	}

	.article-content :global(.before-after-grid) {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
		gap: 1rem;
		align-items: center;
	}

	.article-content :global(.before-after-grid img) {
		width: 100%;
		height: auto;
		aspect-ratio: 3 / 4;
		object-fit: cover;
	}

	.article-content :global(.before-after-arrow) {
		color: #44403c;
		font-size: 2.5rem;
		line-height: 1;
	}

	/* WikiPortraits feature essay */
	.blog-article--feature {
		max-width: none;
		padding: 0 0 clamp(6rem, 9vw, 10rem);
	}

	.feature-masthead {
		display: grid;
		grid-template-columns: minmax(23rem, 0.72fr) minmax(40rem, 1.28fr);
		gap: clamp(3rem, 6vw, 9rem);
		align-items: center;
		min-height: calc(100vh - 6rem);
		padding: clamp(9rem, 12vw, 14rem) clamp(1.5rem, 4vw, 5rem) clamp(6rem, 8vw, 9rem);
		border-bottom: 1px solid var(--hairline);
	}

	.feature-header {
		max-width: 52rem;
		margin: 0;
		text-align: left;
	}

	.feature-header .blog-kicker {
		margin-bottom: 1.6rem;
		color: #96773f;
	}

	.feature-header .blog-title {
		max-width: 11ch;
		font-size: clamp(4.75rem, 7.1vw, 9rem);
		line-height: 0.9;
		letter-spacing: -0.035em;
	}

	.feature-header .blog-excerpt {
		max-width: 31rem;
		margin: clamp(2rem, 3vw, 3.5rem) 0 0;
		font-size: clamp(1.45rem, 1.8vw, 2rem);
		line-height: 1.35;
		color: #4f493f;
	}

	.feature-header .blog-meta {
		margin-top: 2rem;
	}

	.feature-header :global(.feature-share) {
		margin: 2.25rem 0 0;
	}

	.feature-header :global(.feature-share .social-share-button) {
		border: 0;
		border-top: 1px solid #cfc6b3;
		border-bottom: 1px solid #cfc6b3;
		border-radius: 0;
		padding: 0.75rem 0;
		background: transparent;
		backdrop-filter: none;
		box-shadow: none;
		color: #6e675c;
	}

	.feature-header :global(.feature-share .social-share-button:hover) {
		border-color: #96773f;
		background: transparent;
		box-shadow: none;
		color: #96773f;
	}

	.wikiportraits-feature-visual {
		min-width: 0;
	}

	.wikiportraits-feature-link {
		display: flex;
		gap: 2rem;
		align-items: center;
		justify-content: space-between;
		margin-top: 1.5rem;
		padding: 1.35rem 0 1.1rem;
		border-top: 1px solid #b9aa92;
		border-bottom: 1px solid #b9aa92;
		font-family: var(--serif);
		font-size: clamp(1.75rem, 2.35vw, 3rem);
		font-weight: 300;
		line-height: 1.05;
		letter-spacing: -0.02em;
		color: #211d18;
		text-decoration: none;
	}

	.wikiportraits-feature-link:hover {
		color: #c0261f;
	}

	.blog-article--feature .article-content {
		max-width: none;
		margin: 0;
	}

	.blog-article--feature .article-content :global(section) {
		box-sizing: border-box;
	}

	.blog-article--feature .article-content :global(p:empty) {
		display: none;
	}

	.blog-article--feature .article-content :global(.wikiportraits-evidence) {
		padding: clamp(6rem, 9vw, 10rem) clamp(1.5rem, 4vw, 5rem);
		background: #f1ebe1;
		border-block: 1px solid #ded4c4;
	}

	.blog-article--feature .article-content :global(.wikiportraits-evidence-lede) {
		max-width: 58rem;
		margin: 0 auto clamp(4rem, 7vw, 7rem);
	}

	.blog-article--feature .article-content :global(.wikiportraits-evidence-lede p) {
		margin: 0;
		font-size: clamp(2.25rem, 3.35vw, 4.6rem);
		line-height: 1.08;
		letter-spacing: -0.025em;
		text-align: center;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-sequence) {
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		gap: clamp(2rem, 3.5vw, 3.5rem);
		max-width: 52rem;
		margin: 0 auto;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-sequence-note) {
		margin: 0;
		font-size: clamp(2.25rem, 2.8vw, 3.5rem);
		line-height: 1.14;
		letter-spacing: -0.015em;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-sequence-note > p) {
		margin: 0;
		font: inherit;
		letter-spacing: inherit;
		color: inherit;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-figure) {
		margin: 0;
		min-width: 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-figure .portfolio-lightbox) {
		display: grid;
		place-items: center;
		width: 100%;
		aspect-ratio: 4 / 5;
		padding: clamp(1rem, 2vw, 2.5rem);
		background: #e7dfd3;
		box-shadow: 0 24px 52px -36px rgba(42, 32, 23, 0.48);
	}

	.blog-article--feature
		.article-content
		:global(.wikiportraits-pete-figure--landscape .portfolio-lightbox) {
		aspect-ratio: 700 / 366;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-figure img) {
		width: 100%;
		height: 100%;
		max-width: 100%;
		max-height: 100%;
		min-width: 0;
		min-height: 0;
		object-fit: contain;
	}

	.blog-article--feature .article-content :global(.wikiportraits-pete-figure figcaption),
	.blog-article--feature .article-content :global(.wikiportraits-emil figcaption),
	.blog-article--feature .article-content :global(.wikiportraits-jeremy figcaption) {
		max-width: none;
		margin: 1rem 0 0;
		font-family: var(--sans);
		font-size: clamp(1.05rem, 1.2vw, 1.3rem);
		line-height: 1.45;
		text-align: left;
		color: #5c554c;
	}

	.blog-article--feature .article-content :global(.wikiportraits-evidence-context) {
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		align-items: center;
		max-width: 64rem;
		margin: clamp(6rem, 9vw, 10rem) auto 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-evidence-copy) {
		display: grid;
		gap: 1.75rem;
	}

	.blog-article--feature .article-content :global(.wikiportraits-evidence-copy p) {
		margin: 0;
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		line-height: 1.42;
	}

	.blog-article--feature .article-content :global(.wikiportraits-kyle-bartley) {
		width: 100%;
		max-width: 48rem;
		margin: 1rem auto 2rem;
	}

	.blog-article--feature .article-content :global(.wikiportraits-emil) {
		width: 100%;
		max-width: 24rem;
		margin: 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin) {
		max-width: 112rem;
		margin: 0 auto;
		padding: clamp(7rem, 10vw, 12rem) clamp(1.5rem, 4vw, 5rem);
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-grid) {
		display: grid;
		grid-template-columns: minmax(22rem, 0.85fr) minmax(30rem, 1fr);
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-lede) {
		padding-right: clamp(3rem, 6vw, 8rem);
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-lede p) {
		margin: 0;
		font-size: clamp(2.65rem, 3.5vw, 4.75rem);
		line-height: 1.08;
		letter-spacing: -0.025em;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-copy) {
		display: grid;
		gap: 1.75rem;
		align-content: start;
		padding-left: clamp(3rem, 6vw, 8rem);
		border-left: 1px solid #cfc6b3;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-copy p) {
		margin: 0;
		font-size: clamp(1.75rem, 1.9vw, 2.35rem);
		line-height: 1.42;
	}

	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-origin-copy p) {
		font-size: clamp(1.75rem, 1.9vw, 2.35rem);
		line-height: 1.42;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-copy p:first-child) {
		font-size: clamp(2rem, 2.15vw, 2.65rem);
		font-style: italic;
		line-height: 1.35;
		color: #2a2620;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-conan-pair) {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: clamp(1rem, 1.5vw, 1.5rem);
		margin-top: clamp(2rem, 3vw, 3rem);
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-conan-pair figure) {
		margin: 0;
		min-width: 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-conan-pair img) {
		display: block;
		width: 100%;
		height: auto;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-travel),
	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-origin-travel) {
		width: 100%;
		margin: clamp(5rem, 8vw, 9rem) 0 0;
		font-size: clamp(2.75rem, 4.25vw, 5.5rem);
		line-height: 1.08;
		letter-spacing: -0.025em;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-travel > p) {
		margin: 0;
		font: inherit;
		letter-spacing: inherit;
		color: inherit;
	}

	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-origin-copy p:first-child) {
		font-size: clamp(2rem, 2.15vw, 2.65rem);
		line-height: 1.35;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-photo) {
		width: 100%;
		max-width: none;
		margin: clamp(4rem, 7vw, 7rem) 0 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-photo img) {
		display: block;
		width: 100%;
		height: auto;
	}

	.blog-article--feature .article-content :global(.wikiportraits-origin-photo figcaption) {
		max-width: none;
		margin: 1rem 0 0;
		font-family: var(--sans);
		font-size: clamp(1.05rem, 1.2vw, 1.3rem);
		line-height: 1.45;
		text-align: left;
		color: #5c554c;
	}

	.blog-article--feature .article-content :global(.wikiportraits-stats) {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0;
		max-width: 112rem;
		margin: 0 auto;
		padding: 0 clamp(1.5rem, 4vw, 5rem) clamp(7rem, 10vw, 11rem);
	}

	.blog-article--feature .article-content :global(.wikiportraits-stat) {
		padding: clamp(2rem, 3vw, 3.5rem);
		border-top: 1px solid #b9aa92;
		border-bottom: 1px solid #b9aa92;
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		line-height: 1.45;
		color: #4f493f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-stat + .wikiportraits-stat) {
		border-left: 1px solid #b9aa92;
	}

	.blog-article--feature .article-content :global(.wikiportraits-stat p) {
		margin: 0;
		font: inherit;
		line-height: inherit;
		color: inherit;
	}

	.blog-article--feature .article-content :global(.wikiportraits-stats strong) {
		display: block;
		margin: 0.45rem 0;
		font-family: var(--serif);
		font-size: clamp(2.35rem, 3.3vw, 4.5rem);
		font-weight: 300;
		line-height: 0.98;
		letter-spacing: -0.03em;
		color: #c0261f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-stats span) {
		display: block;
		margin-top: 1rem;
		font-style: italic;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application) {
		display: grid;
		grid-template-columns: minmax(20rem, 0.85fr) auto minmax(30rem, 1.15fr);
		gap: clamp(1.5rem, 3vw, 4rem);
		align-items: center;
		max-width: 100rem;
		margin: 0 auto clamp(5rem, 7vw, 8rem);
		padding: clamp(3rem, 5vw, 6rem);
		background: #ece7dc;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-intro p) {
		margin: 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-title) {
		font-family: var(--serif);
		font-size: clamp(2.5rem, 3.25vw, 4.4rem);
		font-weight: 300;
		line-height: 1.02;
		letter-spacing: -0.025em;
		color: #17130f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-title p) {
		margin: 0;
		font: inherit;
		line-height: inherit;
		letter-spacing: inherit;
		color: inherit;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-arrow) {
		display: grid;
		place-items: center;
		width: 3.25rem;
		height: 3.25rem;
		align-self: center;
		margin: 0;
		border: 1px solid #cfc6b3;
		border-radius: 50%;
		font-family: var(--sans);
		font-size: 1.65rem;
		line-height: 1;
		color: #c0261f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-context) {
		max-width: 34rem;
		font-family: var(--serif);
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		font-weight: 400;
		line-height: 1.35;
		color: #3e3932;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-context p) {
		margin: 0;
		font: inherit;
		line-height: inherit;
		color: inherit;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-form) {
		display: grid;
		gap: 1.5rem;
		padding: clamp(2rem, 3vw, 3.5rem);
		background: #fbfaf6;
		box-shadow: 0 1.4rem 4rem rgb(70 55 31 / 10%);
		transform: rotate(0.5deg);
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-form h3) {
		margin: 0 0 0.75rem;
		font-family: var(--serif);
		font-size: clamp(1.5rem, 2vw, 2.35rem);
		font-weight: 400;
		line-height: 1.1;
		letter-spacing: -0.015em;
		color: #17130f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-field span) {
		display: block;
		margin-bottom: 0.55rem;
		font-family: var(--sans);
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		font-weight: 650;
		color: #29251f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-application-field strong) {
		display: block;
		padding: 0.8rem 1rem;
		border: 1.5px solid #8b867e;
		font-family: var(--sans);
		font-size: clamp(1.75rem, 2.4vw, 3rem);
		font-weight: 450;
		line-height: 1.15;
		letter-spacing: 0.01em;
		color: #17130f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-jeremy) {
		display: grid;
		grid-template-columns: minmax(24rem, 0.72fr) minmax(38rem, 1.28fr);
		gap: clamp(3rem, 7vw, 10rem);
		align-items: center;
		max-width: 112rem;
		margin: 0 auto;
		padding: clamp(7rem, 9vw, 11rem) clamp(1.5rem, 4vw, 5rem);
		border-top: 1px solid var(--hairline);
	}

	.blog-article--feature .article-content :global(.wikiportraits-jeremy-copy h2) {
		margin: 0 0 clamp(2rem, 3vw, 3.5rem);
		font-size: clamp(3.25rem, 4.65vw, 6.35rem);
		font-weight: 300;
		line-height: 0.98;
		letter-spacing: -0.035em;
	}

	.blog-article--feature .article-content :global(.wikiportraits-jeremy-copy p) {
		margin: 0;
		font-size: clamp(1.85rem, 1.9vw, 2.25rem);
		line-height: 1.42;
	}

	.blog-article--feature .article-content :global(.wikiportraits-jeremy figure) {
		margin: 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-jeremy-visual) {
		display: grid;
		gap: 1.5rem;
	}

	.blog-article--feature .article-content :global(.wikiportraits-before-after-link) {
		justify-self: end;
		padding-bottom: 0.35rem;
		border-bottom: 1px solid currentColor;
		color: #17130f;
		font-family: var(--sans);
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		font-weight: 700;
		letter-spacing: 0.12em;
		text-decoration: none;
		text-transform: uppercase;
	}

	.blog-article--feature .article-content :global(.wikiportraits-before-after-link:hover) {
		color: #c0261f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-jeremy .before-after-arrow) {
		color: #c0261f;
	}

	.blog-article--feature .article-content :global(.wikiportraits-closing) {
		max-width: 112rem;
		margin: 0 auto;
		padding: clamp(6rem, 8vw, 9rem) clamp(1.5rem, 4vw, 5rem) 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-technique) {
		display: grid;
		grid-template-columns: minmax(22rem, 0.85fr) minmax(30rem, 1.15fr);
		gap: clamp(3rem, 7vw, 8rem);
		align-items: start;
		max-width: 112rem;
		margin: 0 auto;
	}

	.blog-article--feature .article-content :global(.wikiportraits-technique-body > p) {
		margin: 0;
		font-size: clamp(1.75rem, 1.7vw, 2.05rem);
		line-height: 1.38;
		letter-spacing: -0.015em;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-technique-body) {
		display: grid;
		gap: clamp(1.25rem, 2vw, 2rem);
	}

	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-technique-body > p) {
		margin: 0;
		font-size: clamp(1.75rem, 1.7vw, 2.05rem);
		line-height: 1.38;
	}

	.blog-article--feature .article-content :global(.wikiportraits-ana-photo) {
		margin: 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-ana-photo img) {
		display: block;
		width: 100%;
		height: auto;
	}

	.blog-article--feature .article-content :global(.wikiportraits-ana-photo figcaption) {
		margin: 0.9rem 0 0;
		font-family: var(--sans);
		font-size: clamp(1.05rem, 1.2vw, 1.3rem);
		line-height: 1.45;
		color: #5c554c;
	}

	.blog-article--feature .article-content :global(.wikiportraits-mission) {
		display: grid;
		grid-template-columns: 1fr;
		margin-top: clamp(5rem, 8vw, 9rem);
		padding-top: clamp(4rem, 6vw, 7rem);
		border-top: 1px solid #b9aa92;
	}

	.blog-article--feature .article-content :global(.wikiportraits-mission-primary) {
		max-width: 86rem;
	}

	.blog-article--feature .article-content :global(.wikiportraits-mission-primary p) {
		margin: 0;
		font-size: clamp(2.2rem, 3vw, 4rem);
		line-height: 1.12;
		letter-spacing: -0.02em;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-mission-secondary) {
		margin-top: clamp(4rem, 6vw, 7rem);
		padding-top: clamp(3rem, 4vw, 4.5rem);
		border-top: 1px solid #cfc6b3;
	}

	.blog-article--feature .article-content :global(.wikiportraits-mission-secondary p) {
		max-width: 52rem;
		margin: 0;
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		line-height: 1.42;
	}

	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-evidence-copy p),
	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-application-context p),
	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-stat p),
	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-mission-secondary p) {
		font-size: clamp(1.75rem, 1.9vw, 2.25rem);
		line-height: 1.42;
	}

	:global(body.reading-face-arno[data-reading-face='arno'])
		.blog-article--feature
		.article-content
		:global(.wikiportraits-jeremy-copy p) {
		font-size: clamp(1.85rem, 1.9vw, 2.25rem);
		line-height: 1.42;
	}

	.blog-article--feature .article-content :global(.wikiportraits-founders-photo) {
		max-width: 88rem;
		margin: clamp(3rem, 5vw, 5rem) 0 0;
	}

	.blog-article--feature .article-content :global(.wikiportraits-founders-photo img) {
		display: block;
		width: 100%;
		height: auto;
	}

	.blog-article--feature .article-content :global(.wikiportraits-founders-photo figcaption) {
		margin: 0.9rem 0 0;
		font-family: var(--sans);
		font-size: clamp(1.05rem, 1.2vw, 1.3rem);
		line-height: 1.45;
		color: #5c554c;
	}

	.blog-article--feature .article-content :global(.wikiportraits-cta) {
		max-width: 54rem;
		margin: clamp(6rem, 9vw, 10rem) auto 0;
		padding-top: clamp(3rem, 4vw, 4.5rem);
		border-top: 1px solid #b9aa92;
	}

	.blog-article--feature .article-content :global(.wikiportraits-cta p) {
		margin: 0;
		font-size: clamp(2rem, 2.8vw, 3.5rem);
		line-height: 1.25;
		letter-spacing: -0.015em;
		text-align: center;
		color: #211d18;
	}

	.blog-article--feature .article-content :global(.wikiportraits-cta a) {
		color: #c0261f;
		text-decoration-thickness: 1px;
		text-underline-offset: 0.18em;
	}

	@media (max-width: 1050px) {
		.feature-masthead {
			grid-template-columns: 1fr;
			gap: 4rem;
			min-height: 0;
			padding-top: 10rem;
		}

		.feature-header {
			max-width: 48rem;
		}

		.feature-header .blog-title {
			max-width: 10ch;
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin-grid),
		.blog-article--feature .article-content :global(.wikiportraits-jeremy),
		.blog-article--feature .article-content :global(.wikiportraits-technique),
		.blog-article--feature .article-content :global(.wikiportraits-mission) {
			grid-template-columns: 1fr;
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin-lede),
		.blog-article--feature .article-content :global(.wikiportraits-mission-primary) {
			padding-right: 0;
			padding-bottom: 3.5rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin-copy),
		.blog-article--feature .article-content :global(.wikiportraits-mission-secondary) {
			padding: 3.5rem 0 0;
			border-top: 1px solid #cfc6b3;
			border-left: 0;
		}

		.blog-article--feature .article-content :global(.wikiportraits-jeremy) {
			gap: 4rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-technique) {
			gap: 4rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-evidence-context) {
			grid-template-columns: minmax(0, 1fr) 20rem;
			gap: 4rem;
		}
	}

	@media (max-width: 760px) {
		.feature-masthead {
			padding: 8.5rem 1.5rem 5rem;
		}

		.feature-header .blog-title {
			font-size: clamp(3.5rem, 15.5vw, 5.5rem);
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin),
		.blog-article--feature .article-content :global(.wikiportraits-jeremy),
		.blog-article--feature .article-content :global(.wikiportraits-closing) {
			padding-inline: 1.5rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin-lede p),
		.blog-article--feature .article-content :global(.wikiportraits-jeremy-copy h2) {
			font-size: clamp(2.9rem, 13vw, 4.5rem);
		}

		.blog-article--feature .article-content :global(.wikiportraits-evidence) {
			padding-inline: 1.5rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-evidence-lede) {
			text-align: left;
		}

		.blog-article--feature .article-content :global(.wikiportraits-evidence-context) {
			grid-template-columns: 1fr;
		}

		.blog-article--feature .article-content :global(.wikiportraits-emil) {
			margin-inline: auto;
		}

		.blog-article--feature .article-content :global(.wikiportraits-stats) {
			grid-template-columns: 1fr;
			padding-inline: 1.5rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-application) {
			grid-template-columns: 1fr;
			margin-inline: 1.5rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-application-arrow) {
			justify-self: center;
			transform: rotate(90deg);
		}

		.blog-article--feature .article-content :global(.wikiportraits-stat + .wikiportraits-stat) {
			border-top: 0;
			border-left: 0;
		}

		.blog-article--feature .article-content :global(.wikiportraits-mission) {
			margin-top: 5rem;
		}

		.blog-article--feature .article-content :global(.wikiportraits-cta) {
			text-align: left;
		}
	}

	@media (max-width: 640px) {
		.blog-title {
			font-size: 3rem;
		}

		.blog-article:not(.blog-article--feature) .article-content :global(p),
		.blog-article:not(.blog-article--feature) .article-content :global(li),
		.blog-article:not(.blog-article--feature) .article-content :global(blockquote) {
			font-size: 1.25rem;
		}

		.article-content :global(.before-after-grid) {
			grid-template-columns: 1fr;
		}

		.article-content :global(.before-after-arrow) {
			justify-self: center;
			transform: rotate(90deg);
		}
	}

	@media print {
		:global(.site-nav),
		:global(footer) {
			display: none !important;
		}

		.feature-masthead {
			min-height: 0;
			padding: 0.45in 0 0.35in;
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin-grid),
		.blog-article--feature .article-content :global(.wikiportraits-jeremy),
		.blog-article--feature .article-content :global(.wikiportraits-technique),
		.blog-article--feature .article-content :global(.wikiportraits-mission),
		.blog-article--feature .article-content :global(.wikiportraits-application) {
			display: block;
		}

		.blog-article--feature .article-content :global(.wikiportraits-origin-copy),
		.blog-article--feature .article-content :global(.wikiportraits-mission-secondary) {
			padding: 0.3in 0 0;
			border-top: 1px solid #cfc6b3;
			border-left: 0;
		}

		.blog-article--feature .article-content :global(.wikiportraits-application-arrow) {
			display: none;
		}

		.blog-article--feature .article-content :global(figure),
		.blog-article--feature .article-content :global(.portfolio-lightbox),
		.blog-article--feature .article-content :global(.before-after-grid),
		.blog-article--feature .article-content :global(.wikiportraits-origin-conan-pair),
		.blog-article--feature .article-content :global(.wikiportraits-application-form),
		.blog-article--feature .article-content :global(.wikiportraits-evidence-lede) {
			break-inside: avoid;
			page-break-inside: avoid;
		}

		.blog-article--feature .article-content :global(figure .portfolio-lightbox) {
			width: 100%;
			height: auto !important;
			min-height: 0 !important;
			aspect-ratio: auto !important;
			padding: 0.15in !important;
		}

		.blog-article--feature .article-content :global(figure img) {
			display: block;
			width: auto !important;
			height: auto !important;
			max-width: 100% !important;
			max-height: 7.2in !important;
			margin-inline: auto;
			object-fit: contain;
		}

		.blog-article--feature .article-content :global(.wikiportraits-pete-sequence) {
			gap: 0.3in;
		}
	}
</style>
