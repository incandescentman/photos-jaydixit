---
import rawPeople from '../../data/people.json';

export function getStaticPaths() {
	return (rawPeople as { slug: string }[]).map((person) => ({
		params: { person: person.slug },
	}));
}

const { person } = Astro.params;
return Astro.redirect(`/portraits/${person}`, 301);
---
